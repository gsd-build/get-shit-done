---
phase: 07-phase-execution-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/autonomous.md
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, EXEC-04]

must_haves:
  truths:
    - "After discuss completes, plan-phase is auto-invoked without user triggering it"
    - "After plan completes, execute-phase is auto-invoked with --no-transition flag"
    - "When verification passes, workflow continues to next phase with no user prompt"
    - "When verification needs human testing, user is asked to validate or skip"
    - "When gaps are found, user is offered gap closure, continue, or stop"
    - "Gap closure is limited to 1 retry to prevent infinite loops"
    - "Execute-phase failure (no VERIFICATION.md) routes to handle_blocker"
  artifacts:
    - path: "get-shit-done/workflows/autonomous.md"
      provides: "Full phase execution chain with post-execution verification routing"
      contains: "--no-transition"
  key_links:
    - from: "step 3c Skill call"
      to: "execute-phase.md --no-transition handling"
      via: "args flag"
      pattern: '--no-transition'
    - from: "step 3d routing"
      to: "VERIFICATION.md status field"
      via: "grep + cut parsing"
      pattern: 'grep.*status.*VERIFICATION'
    - from: "gap closure cycle"
      to: "plan-phase --gaps + execute-phase --no-transition"
      via: "Skill() invocations"
      pattern: 'plan-phase.*--gaps'
---

<objective>
Wire the full discuss→plan→execute chain in autonomous.md with proper flags, post-execution verification routing, and error recovery.

Purpose: Currently steps 3c and 3d in autonomous.md invoke execute-phase without `--no-transition` (risking internal chaining to transition.md) and use a simple transition message instead of reading verification results. This plan adds the `--no-transition` flag, replaces the transition step with VERIFICATION.md-based routing on three outcomes (passed/human_needed/gaps_found), adds a gap closure cycle with 1-retry limit, and updates the success criteria.

Output: Modified `get-shit-done/workflows/autonomous.md` with complete phase execution chain logic.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phase-execution-chain/07-CONTEXT.md
@.planning/phases/06-smart-discuss/06-01-SUMMARY.md

<interfaces>
<!-- Current code to modify in get-shit-done/workflows/autonomous.md -->
<!-- Lines 157-173: Steps 3c and 3d — the exact content to replace -->

**Step 3c (line 157-161):**
```markdown
**3c. Execute**

\```
Skill(skill="gsd:execute-phase", args="${PHASE_NUM}")
\```
```

**Step 3d (lines 163-173):**
```markdown
**3d. Transition**

Display transition message:

\```
Phase {N} ✅ {Name}
→ Phase {N+1}: {Next Name} — {1-line goal}
\```

Proceed to iterate step.
```

**Success criteria section (lines 534-542):**
```markdown
<success_criteria>
- [ ] All incomplete phases executed in order (smart discuss → plan → execute each)
- [ ] Smart discuss proposes grey area answers in tables, user accepts or overrides per area
- [ ] Progress banners displayed between phases
- [ ] ROADMAP.md re-read after each phase (catches inserted phases)
- [ ] STATE.md checked for blockers before each phase
- [ ] Blockers handled via user choice (retry / skip / stop)
- [ ] Final completion or stop summary displayed
</success_criteria>
```

<!-- Reference from execute-phase.md — how VERIFICATION.md status is parsed (lines 319-321) -->
```bash
grep "^status:" "$PHASE_DIR"/*-VERIFICATION.md | cut -d: -f2 | tr -d ' '
```
Three possible values: `passed`, `human_needed`, `gaps_found`

<!-- Reference from execute-phase.md — --no-transition behavior (lines 390-409) -->
When `--no-transition` is present, execute-phase returns a PHASE COMPLETE block instead of chaining to transition.md. Autonomous reads VERIFICATION.md after this return.

<!-- Reference from autonomous.md — handle_blocker step (lines 499-530) -->
handle_blocker presents 3 options via AskUserQuestion: "Fix and retry" / "Skip this phase" / "Stop autonomous mode". Already exists — route failures there.

<!-- Reference from autonomous.md — iterate step (lines 458-497) -->
iterate step re-reads ROADMAP.md, re-filters incomplete phases, checks STATE.md for blockers, loops to next phase or displays completion banner. Step 3d must "proceed to iterate step" for all passing paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --no-transition flag and replace step 3d with verification routing</name>
  <files>get-shit-done/workflows/autonomous.md</files>
  <action>
Modify the `execute_phase` step (step 3) in autonomous.md. Make three targeted edits:

**Edit 1 — Step 3c: Add `--no-transition` flag**

Replace the current step 3c content (lines 157-161):
```
**3c. Execute**

Skill(skill="gsd:execute-phase", args="${PHASE_NUM}")
```

With:
```
**3c. Execute**

Skill(skill="gsd:execute-phase", args="${PHASE_NUM} --no-transition")
```

The `--no-transition` flag prevents execute-phase from chaining to transition.md internally — autonomous manages all phase-to-phase transitions via its iterate step.

Do NOT add `--auto` to this call (per user decision — autonomous controls flow externally, not via execute-phase's internal auto-advance).

**Edit 2 — Step 3d: Replace transition with post-execution verification routing**

Replace the entire step 3d content (lines 163-173, from `**3d. Transition**` through `Proceed to iterate step.`) with the following new step 3d. Write this EXACTLY (it's a markdown workflow instruction, not code):

```markdown
**3d. Post-Execution Routing**

After execute-phase returns, read the verification result:

\```bash
VERIFY_STATUS=$(grep "^status:" "${PHASE_DIR}"/*-VERIFICATION.md 2>/dev/null | head -1 | cut -d: -f2 | tr -d ' ')
\```

Where `PHASE_DIR` comes from the `init phase-op` call already made in step 3a. If the variable is not in scope, re-fetch:

\```bash
PHASE_STATE=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" init phase-op ${PHASE_NUM})
\```

Parse `phase_dir` from the JSON.

**If VERIFY_STATUS is empty** (no VERIFICATION.md or no status field):

Go to handle_blocker: "Execute phase ${PHASE_NUM} did not produce verification results."

**If `passed`:**

Display:
\```
Phase ${PHASE_NUM} ✅ ${PHASE_NAME} — Verification passed
\```

Proceed to iterate step.

**If `human_needed`:**

Read the human_verification section from VERIFICATION.md to get the count and items requiring manual testing.

Display the items, then ask user via AskUserQuestion:
- **question:** "Phase ${PHASE_NUM} has items needing manual verification. Validate now or continue to next phase?"
- **options:** "Validate now" / "Continue without validation"

On **"Validate now"**: Present the specific items from VERIFICATION.md's human_verification section. After user reviews, ask:
- **question:** "Validation result?"
- **options:** "All good — continue" / "Found issues"

On "All good — continue": Display `Phase ${PHASE_NUM} ✅ Human validation passed` and proceed to iterate step.

On "Found issues": Go to handle_blocker with the user's reported issues as the description.

On **"Continue without validation"**: Display `Phase ${PHASE_NUM} ⏭ Human validation deferred` and proceed to iterate step.

**If `gaps_found`:**

Read gap summary from VERIFICATION.md (score and missing items). Display:
\```
⚠ Phase ${PHASE_NUM}: ${PHASE_NAME} — Gaps Found
Score: {N}/{M} must-haves verified
\```

Ask user via AskUserQuestion:
- **question:** "Gaps found in phase ${PHASE_NUM}. How to proceed?"
- **options:** "Run gap closure" / "Continue without fixing" / "Stop autonomous mode"

On **"Run gap closure"**: Execute gap closure cycle (limit: 1 attempt):

\```
Skill(skill="gsd:plan-phase", args="${PHASE_NUM} --gaps")
\```

Verify gap plans were created — re-run `init phase-op ${PHASE_NUM}` and check `has_plans`. If no new gap plans → go to handle_blocker: "Gap closure planning for phase ${PHASE_NUM} did not produce plans."

Re-execute:
\```
Skill(skill="gsd:execute-phase", args="${PHASE_NUM} --no-transition")
\```

Re-read verification status:
\```bash
VERIFY_STATUS=$(grep "^status:" "${PHASE_DIR}"/*-VERIFICATION.md 2>/dev/null | head -1 | cut -d: -f2 | tr -d ' ')
\```

If `passed` or `human_needed`: Route normally (continue or ask user as above).

If still `gaps_found` after this retry: Display "Gaps persist after closure attempt." and ask via AskUserQuestion:
- **question:** "Gap closure did not fully resolve issues. How to proceed?"
- **options:** "Continue anyway" / "Stop autonomous mode"

On "Continue anyway": Proceed to iterate step.
On "Stop autonomous mode": Go to handle_blocker.

This limits gap closure to 1 automatic retry to prevent infinite loops.

On **"Continue without fixing"**: Display `Phase ${PHASE_NUM} ⏭ Gaps deferred` and proceed to iterate step.

On **"Stop autonomous mode"**: Go to handle_blocker with "User stopped — gaps remain in phase ${PHASE_NUM}".
```

**Important formatting notes:**
- Use the same markdown style as the rest of the file (backtick code blocks for bash commands, bold for sub-step headers)
- Keep the `</step>` closing tag on the line after the last content line (line 174 currently)
- The iterate step (step 4) starts at the next `<step name="iterate">` — do not modify it

**Edit 3 — Update success_criteria section**

Replace the current `<success_criteria>` block (lines 534-542) with:

```markdown
<success_criteria>
- [ ] All incomplete phases executed in order (smart discuss → plan → execute each)
- [ ] Smart discuss proposes grey area answers in tables, user accepts or overrides per area
- [ ] Progress banners displayed between phases
- [ ] Execute-phase invoked with --no-transition (autonomous manages transitions)
- [ ] Post-execution verification reads VERIFICATION.md and routes on status
- [ ] Passed verification → automatic continue to next phase
- [ ] Human-needed verification → user prompted to validate or skip
- [ ] Gaps-found → user offered gap closure, continue, or stop
- [ ] Gap closure limited to 1 retry (prevents infinite loops)
- [ ] Plan-phase and execute-phase failures route to handle_blocker
- [ ] ROADMAP.md re-read after each phase (catches inserted phases)
- [ ] STATE.md checked for blockers before each phase
- [ ] Blockers handled via user choice (retry / skip / stop)
- [ ] Final completion or stop summary displayed
</success_criteria>
```
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && grep -c '\-\-no-transition' get-shit-done/workflows/autonomous.md | grep -q '[2-9]' && echo "PASS: --no-transition appears 2+ times (step 3c + gap closure re-execute)" && grep -c 'Post-Execution Routing' get-shit-done/workflows/autonomous.md | grep -q '1' && echo "PASS: Step 3d replaced with Post-Execution Routing" && grep -c 'VERIFY_STATUS' get-shit-done/workflows/autonomous.md | grep -q '[2-9]' && echo "PASS: VERIFY_STATUS parsing present" && grep -c 'gaps_found' get-shit-done/workflows/autonomous.md | grep -q '[1-9]' && echo "PASS: gaps_found routing exists" && grep -c 'human_needed' get-shit-done/workflows/autonomous.md | grep -q '[1-9]' && echo "PASS: human_needed routing exists" && grep -c 'plan-phase.*--gaps' get-shit-done/workflows/autonomous.md | grep -q '[1-9]' && echo "PASS: gap closure plan-phase --gaps call exists" && grep 'Gap closure limited to 1 retry' get-shit-done/workflows/autonomous.md && echo "PASS: success criteria updated" && ! grep -q 'gsd:execute-phase.*args=.*"${PHASE_NUM}"$' get-shit-done/workflows/autonomous.md && echo "PASS: no bare execute-phase call without --no-transition" && npm test 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - Step 3c Skill call includes `--no-transition` flag
    - Step 3d reads VERIFICATION.md status and routes on passed/human_needed/gaps_found
    - `passed` → continues to iterate step without user prompt
    - `human_needed` → asks user via AskUserQuestion (validate now / continue)
    - `gaps_found` → asks user (gap closure / continue / stop), gap closure limited to 1 retry
    - Execute-phase failure (no VERIFICATION.md) → routes to handle_blocker
    - Success criteria updated to reflect all new behaviors
    - `npm test` passes (no regressions in existing tests)
  </done>
</task>

<task type="auto">
  <name>Task 2: Structural verification and file integrity check</name>
  <files>get-shit-done/workflows/autonomous.md</files>
  <action>
After Task 1's edits, verify the full file structural integrity:

1. **Confirm all 6 named steps still exist and are properly opened/closed:**
   - `<step name="initialize">` ... `</step>`
   - `<step name="discover_phases">` ... `</step>`
   - `<step name="execute_phase">` ... `</step>`
   - `<step name="smart_discuss">` ... `</step>`
   - `<step name="iterate">` ... `</step>`
   - `<step name="handle_blocker">` ... `</step>`

2. **Confirm step 3 sub-steps are complete and in order:**
   - `**3a. Smart Discuss**` — unchanged from Phase 6
   - `**3b. Plan**` — unchanged (Skill call without --auto, has_plans check)
   - `**3c. Execute**` — now has `--no-transition`
   - `**3d. Post-Execution Routing**` — new verification routing

3. **Confirm no stale content:**
   - The old `**3d. Transition**` text should NOT appear
   - The old `Phase {N} ✅ {Name}\n→ Phase {N+1}` transition message should NOT appear in step 3d (it may still appear in other contexts — that's fine)
   - No duplicate Skill calls to execute-phase without `--no-transition`

4. **Confirm the `<success_criteria>` section has the new items:**
   - `--no-transition` mentioned
   - Post-execution verification routing mentioned
   - Gap closure 1-retry limit mentioned
   - Both `human_needed` and `gaps_found` routing mentioned

5. **If any structural issues found:** Fix them. Common issues:
   - Missing `</step>` closing tag after step 3d expansion
   - Orphaned blank lines or duplicate headers
   - Broken markdown code fences (must be properly opened and closed)

6. **Line count check:** The file should be approximately 590-640 lines (was 542, adding ~50-100 lines for the new step 3d content). If significantly outside this range, investigate.
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && echo "=== Step count ===" && grep -c 'step name=' get-shit-done/workflows/autonomous.md && echo "=== Step open/close balance ===" && OPENS=$(grep -c '<step name=' get-shit-done/workflows/autonomous.md) && CLOSES=$(grep -c '</step>' get-shit-done/workflows/autonomous.md) && echo "Opens: $OPENS, Closes: $CLOSES" && [ "$OPENS" = "$CLOSES" ] && echo "PASS: balanced" || echo "FAIL: unbalanced" && echo "=== Sub-steps ===" && grep -n '^\*\*3[a-d]\.' get-shit-done/workflows/autonomous.md && echo "=== No stale transition ===" && ! grep -n '3d\. Transition' get-shit-done/workflows/autonomous.md && echo "PASS: old 3d removed" && echo "=== New 3d present ===" && grep -n 'Post-Execution Routing' get-shit-done/workflows/autonomous.md && echo "=== Line count ===" && wc -l get-shit-done/workflows/autonomous.md && echo "=== npm test ===" && npm test 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    - All 6 named steps exist with balanced open/close tags
    - Sub-steps 3a through 3d present in correct order
    - No stale "3d. Transition" content remains
    - New "3d. Post-Execution Routing" present
    - File line count in expected range (590-640)
    - `npm test` passes
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Structural grep checks:**
   - `grep -c '\-\-no-transition' get-shit-done/workflows/autonomous.md` → should be 2+ (step 3c + gap closure re-execute)
   - `grep -c 'VERIFY_STATUS' get-shit-done/workflows/autonomous.md` → should be 2+ (initial read + post-gap-closure read)
   - `grep 'Post-Execution Routing' get-shit-done/workflows/autonomous.md` → should match
   - `grep -c 'step name=' get-shit-done/workflows/autonomous.md` → should be 6

2. **No regressions:** `npm test` passes (existing 645+ tests)

3. **Requirements coverage:**
   - EXEC-01: Step 3b auto-invokes plan-phase (already existed, verified by `grep 'gsd:plan-phase'`)
   - EXEC-02: Step 3c uses `--no-transition` (verified by grep)
   - EXEC-03: Step 3d routes on passed/human_needed/gaps_found (verified by grep)
   - EXEC-04: Skill() surfaces interactions naturally; failures route to handle_blocker (verified by handle_blocker grep)
</verification>

<success_criteria>
- `--no-transition` flag present in execute-phase Skill() call
- VERIFICATION.md status is read and routed on 3 outcomes
- `passed` → auto-continue, `human_needed` → user prompt, `gaps_found` → user choice
- Gap closure cycle limited to 1 retry
- Execute-phase failure detection routes to handle_blocker
- All 6 workflow steps structurally intact
- `npm test` passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-phase-execution-chain/07-01-SUMMARY.md`
</output>
