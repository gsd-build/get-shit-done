---
phase: 11-add-docs-flag-to-discuss-phase-for-document-assisted-context-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/discuss-phase.md
  - get-shit-done/workflows/discuss-phase.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can pass --docs flag with comma-separated document paths"
    - "Invalid document paths display warning and continue with valid docs"
    - "When all docs invalid, workflow falls back to standard discuss-phase"
    - "Valid docs are stored for downstream extraction step"
  artifacts:
    - path: "commands/gsd/discuss-phase.md"
      provides: "--docs flag in argument-hint"
      contains: "--docs"
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "parse_docs_flag step"
      contains: "parse_docs_flag"
  key_links:
    - from: "commands/gsd/discuss-phase.md"
      to: "get-shit-done/workflows/discuss-phase.md"
      via: "argument-hint shows --docs is supported"
      pattern: "--docs"
---

<objective>
Add --docs flag parsing to discuss-phase workflow

Purpose: Enable users to pass reference documents (PRD, spec, etc.) to discuss-phase for auto-extraction of decisions. This plan handles argument parsing and document path validation.

Output: Updated command registration with --docs in argument-hint, workflow with parse_docs_flag step that validates paths and prepares for extraction
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-add-docs-flag-to-discuss-phase-for-document-assisted-context-extraction/11-CONTEXT.md
@.planning/phases/11-add-docs-flag-to-discuss-phase-for-document-assisted-context-extraction/11-RESEARCH.md
@commands/gsd/discuss-phase.md
@get-shit-done/workflows/discuss-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update command registration with --docs flag</name>
  <files>commands/gsd/discuss-phase.md</files>
  <action>
Update the argument-hint in frontmatter to include the --docs flag:

```yaml
argument-hint: "<phase> [--auto] [--docs path/to/doc1.md,path/to/doc2.md]"
```

This informs users the flag is available. The flag accepts comma-separated paths to markdown or text files (.md, .txt).
  </action>
  <verify>Read commands/gsd/discuss-phase.md and confirm argument-hint includes --docs</verify>
  <done>argument-hint shows --docs flag syntax with comma-separated paths example</done>
</task>

<task type="auto">
  <name>Task 2: Add parse_docs_flag step to workflow</name>
  <files>get-shit-done/workflows/discuss-phase.md</files>
  <action>
Add a new step `parse_docs_flag` after `check_existing` and before `analyze_phase`. This step:

1. Check $ARGUMENTS for --docs flag
2. If present, extract the paths string after --docs
3. Split by comma and trim whitespace from each path
4. Validate each path exists using Bash `test -f "$path"`
5. Track valid_docs and invalid_docs lists

**If any path invalid:**
Display warning: "Note: Could not find: {invalid_docs list}. Continuing with: {valid_docs list}"

**If ALL paths invalid:**
Display: "None of the specified documents could be found. Falling back to standard discuss-phase flow."
Set `has_docs=false`, proceed to analyze_phase unchanged.

**If has valid docs:**
Set `has_docs=true`, store `valid_docs` list for extract_from_docs step (added in plan 02).

**If --docs flag not present:**
Set `has_docs=false`, proceed to analyze_phase unchanged.

Insert the step XML:
```xml
<step name="parse_docs_flag">
Check $ARGUMENTS for --docs flag.

**If --docs present:**
Extract paths after --docs flag. Split by comma, trim whitespace.

Validate each path:
```bash
for doc in "${DOC_PATHS[@]}"; do
  doc=$(echo "$doc" | xargs)  # trim whitespace
  if [ -f "$doc" ]; then
    # Add to valid_docs
  else
    # Add to invalid_docs
  fi
done
```

**If any paths invalid:**
```
Note: Could not find: {invalid_docs}
Continuing with: {valid_docs}
```

**If all paths invalid:**
```
None of the specified documents could be found.
Falling back to standard discuss-phase flow.
```
Set `has_docs=false`, proceed to analyze_phase.

**If has valid docs:**
Set `has_docs=true`, `valid_docs` list stored for extract_from_docs step.
Proceed to extract_from_docs (added in plan 02).

**If --docs flag not present:**
Set `has_docs=false`, proceed to analyze_phase.
</step>
```

Also update the process flow in the `<process>` section at the top of the workflow to mention the new conditional path:

Add after "Express path available" note:
```
**Document-assisted path:** If you have reference documents, use `/gsd:discuss-phase {phase} --docs path/to/prd.md,path/to/spec.md` to auto-extract decisions from them.
```

Update the numbered process list to include the conditional:
```
3. **Parse --docs flag** — If present, validate paths and prepare for extraction
3a. **If has_docs** — Extract decisions from documents (4-tier classification)
3b. **If has_docs** — Present extractions, get user confirmation/overrides
```
  </action>
  <verify>
Read the workflow file and verify:
1. parse_docs_flag step exists between check_existing and analyze_phase
2. Step handles: valid paths, partial invalid, all invalid, no flag
3. Process section mentions document-assisted path
  </verify>
  <done>parse_docs_flag step validates document paths and sets has_docs flag for downstream conditional logic</done>
</task>

</tasks>

<verification>
1. Read commands/gsd/discuss-phase.md - confirm argument-hint includes --docs
2. Read get-shit-done/workflows/discuss-phase.md - confirm parse_docs_flag step exists
3. Verify step ordering: check_existing -> parse_docs_flag -> (extract_from_docs if has_docs) -> analyze_phase
</verification>

<success_criteria>
- Command registration shows --docs flag in argument-hint
- Workflow has parse_docs_flag step with path validation logic
- Valid docs stored for extraction step
- Invalid paths warn but don't fail workflow
- Standard flow (no --docs) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-docs-flag-to-discuss-phase-for-document-assisted-context-extraction/11-01-SUMMARY.md`
</output>
