---
phase: 18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/api/routes/projects.ts
  - apps/server/src/api/routes/phases.ts
  - apps/server/src/api/routes/agents.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/lib/endpoints.ts
  - apps/web/src/hooks/useProjects.ts
  - apps/web/src/components/features/dashboard/ProjectGrid.tsx
  - apps/web/tests/e2e/dashboard.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Only GSD-initialized projects are surfaced as workflow-capable projects"
    - "Direct-linkable initialized projects are discoverable in dashboard/search"
    - "Workflow endpoint unavailability is surfaced as explicit MVP errors, not silent fallback"
  artifacts:
    - path: "apps/server/src/api/routes/projects.ts"
      provides: "Project eligibility/discovery contract for initialized projects"
      exports: ["createProjectRoutes"]
    - path: "apps/web/src/lib/api.ts"
      provides: "Normalized workflow API errors used by plan/verify/discuss pages"
      exports: ["fetchPlan", "startResearch", "startVerification", "submitApproval", "submitRejection", "fetchCoverage"]
    - path: "apps/web/src/hooks/useProjects.ts"
      provides: "Dashboard project fetching/filter behavior"
      exports: ["useProjects"]
  key_links:
    - from: "createProjectRoutes"
      to: "useProjects"
      via: "GET /api/projects eligibility + metadata contract"
      pattern: "discoverProjects|fetchProjects"
    - from: "api.ts workflow calls"
      to: "plan/verify/discuss UI states"
      via: "explicit error envelope rendering"
      pattern: "response.ok|error.code|error.message"
---

<objective>
Implement the MVP project/API reliability foundation so add-new-feature workflows only target initialized projects and endpoint failures are explicit and actionable.

Purpose: eliminate hidden capability drift and unclear 404 behavior for workflow actions.
Output: server discovery contract adjustments, client error normalization, and dashboard coverage tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-CONTEXT.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-RESEARCH.md
@apps/server/src/api/routes/projects.ts
@apps/server/src/api/routes/phases.ts
@apps/web/src/lib/api.ts
@apps/web/src/hooks/useProjects.ts
@apps/web/src/components/features/dashboard/ProjectGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for initialized-project discovery and explicit endpoint error handling</name>
  <files>apps/web/tests/e2e/dashboard.spec.ts, apps/web/src/hooks/useProjects.test.ts, apps/server/src/api/routes/projects.test.ts (new if missing)</files>
  <action>
Write RED tests that fail on current behavior:
- Non-initialized projects must not appear as workflow-capable.
- Direct-linkable initialized project IDs (for example `todo`) must appear in dashboard/search results.
- Workflow endpoint errors (404/precondition) must map to explicit UI-facing error state in API layer (not swallowed or silently ignored).

Prefer extending existing tests before introducing new test harnesses.
  </action>
  <verify>
    <automated>pnpm -C apps/server test --run && pnpm -C apps/web test --run && pnpm -C apps/web test:e2e -- --grep "dashboard"</automated>
  </verify>
  <done>Failing tests capture project eligibility/discovery and explicit endpoint error contract</done>
</task>

<task type="auto">
  <name>Task 2: Implement initialized-project discovery and explicit workflow error contract</name>
  <files>apps/server/src/api/routes/projects.ts, apps/server/src/api/routes/phases.ts, apps/server/src/api/routes/agents.ts, apps/web/src/lib/api.ts, apps/web/src/lib/endpoints.ts, apps/web/src/hooks/useProjects.ts</files>
  <action>
Implement MVP contract updates:
- Restrict surfaced workflow-capable projects to initialized GSD projects.
- Ensure initialized direct-linkable IDs remain discoverable.
- Normalize workflow endpoint failures into explicit client-consumable errors with stable codes/messages.
- Keep behavior aligned with locked decision: no fallback endpoint probing in MVP.
- Add `#TODO(post-mvp)` notes where richer capability negotiation/resilience should be introduced.
  </action>
  <verify>
    <automated>pnpm -C apps/server typecheck && pnpm -C apps/web typecheck && pnpm -C apps/server test --run && pnpm -C apps/web test --run</automated>
  </verify>
  <done>Server/client contracts enforce initialized-project support and explicit endpoint error handling</done>
</task>

<task type="auto">
  <name>Task 3: Green tests and finalize dashboard/search behavior</name>
  <files>apps/web/src/components/features/dashboard/ProjectGrid.tsx, apps/web/tests/e2e/dashboard.spec.ts</files>
  <action>
Make dashboard rendering deterministic with updated project discovery contract:
- Confirm initialized projects are rendered and searchable.
- Ensure states/messages are explicit when no supported projects exist.
- Keep UI behavior stable across desktop/mobile breakpoints.
  </action>
  <verify>
    <automated>pnpm -C apps/web test --run && pnpm -C apps/web test:e2e -- --grep "dashboard"</automated>
  </verify>
  <done>Dashboard/search reliably surfaces initialized projects and passes updated tests</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/server typecheck`
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/server test --run`
- `pnpm -C apps/web test --run`
</verification>

<success_criteria>
- Workflow-capable project list is limited to initialized projects
- Initialized direct-linkable projects are discoverable in dashboard/search
- Endpoint failures are explicit MVP errors with actionable UI messaging
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-01-SUMMARY.md`
</output>
