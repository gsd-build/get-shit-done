---
phase: 18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa
plan: 03
type: execute
wave: 2
depends_on: [18.2-02]
files_modified:
  - apps/web/src/components/features/discuss/DecisionOptionsPanel.tsx
  - apps/web/src/stores/discussStore.ts
  - apps/web/src/app/projects/[id]/discuss/page.tsx
  - apps/server/src/api/routes/agents.ts
  - apps/server/src/orchestrator/checkpoint.ts
  - apps/web/src/components/features/discuss/DecisionOptionsPanel.test.tsx
  - apps/web/tests/e2e/discuss-phase.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Discuss presents ranked options with recommended-first ordering and an explicit Accept all defaults action"
    - "Accept all defaults applies recommended options for all unresolved decisions in the phase"
    - "Audit trail records both summary and per-decision default applications"
  artifacts:
    - path: "apps/web/src/components/features/discuss/DecisionOptionsPanel.tsx"
      provides: "Ranked options UI with explicit accept-defaults action"
      exports: ["DecisionOptionsPanel"]
    - path: "apps/web/src/stores/discussStore.ts"
      provides: "Structured decision state, unresolved tracking, and audit events"
      exports: ["useDiscussStore"]
    - path: "apps/server/src/orchestrator/checkpoint.ts"
      provides: "Checkpoint/default decision payload compatibility for discuss flow"
  key_links:
    - from: "DecisionOptionsPanel"
      to: "discussStore"
      via: "recommended selection + accept-all dispatch"
      pattern: "applyRecommended|acceptAllDefaults"
    - from: "discussStore"
      to: "agents/checkpoint APIs"
      via: "persist summary event and per-decision audit records"
      pattern: "audit|checkpoint|response"
---

<objective>
Implement deterministic Discuss progression for defaults by introducing ranked options and an explicit Accept all defaults control.

Purpose: remove repeated follow-up loops when user opts into defaults and ensure auditable progression.
Output: discuss options panel, state logic, backend checkpoint compatibility, and test coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-CONTEXT.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-RESEARCH.md
@apps/web/src/app/projects/[id]/discuss/page.tsx
@apps/server/src/api/routes/agents.ts
@apps/server/src/orchestrator/checkpoint.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for ranked options and accept-all default behavior</name>
  <files>apps/web/src/components/features/discuss/DecisionOptionsPanel.test.tsx, apps/web/tests/e2e/discuss-phase.spec.ts</files>
  <action>
Create RED tests for discuss decision handling:
- options are rendered in order with recommended-first label.
- last action in panel is `Accept all defaults`.
- clicking accept-all applies recommended option to each unresolved decision.
- audit trail contains both summary and per-decision default entries.
  </action>
  <verify>
    <automated>pnpm -C apps/web test --run && pnpm -C apps/web test:e2e -- --grep "discuss.*defaults"</automated>
  </verify>
  <done>Tests fail on current implementation and lock deterministic default behavior</done>
</task>

<task type="auto">
  <name>Task 2: Implement discuss option panel and deterministic accept-defaults state transitions</name>
  <files>apps/web/src/components/features/discuss/DecisionOptionsPanel.tsx, apps/web/src/stores/discussStore.ts, apps/web/src/app/projects/[id]/discuss/page.tsx</files>
  <action>
Implement client-side discuss decision model:
- structured unresolved decision collection with ranked options.
- explicit accept-all action that selects recommended option for each unresolved decision.
- do not use free-text intent matching for defaults.
- append audit records in two levels: summary + per-decision.

Integrate panel into discuss page without regressing existing streaming chat flow.
  </action>
  <verify>
    <automated>pnpm -C apps/web typecheck && pnpm -C apps/web test --run</automated>
  </verify>
  <done>Discuss UI supports explicit deterministic defaults acceptance with auditable state</done>
</task>

<task type="auto">
  <name>Task 3: Align checkpoint/agent API behavior with discuss defaults workflow and pass E2E</name>
  <files>apps/server/src/api/routes/agents.ts, apps/server/src/orchestrator/checkpoint.ts, apps/web/tests/e2e/discuss-phase.spec.ts</files>
  <action>
Ensure server checkpoint processing supports structured decision payloads from the new discuss panel and preserves ordering/recommendation metadata needed for deterministic default acceptance.
Keep backward compatibility for existing discuss messages where possible.
Finalize E2E assertions for progression after default acceptance.
  </action>
  <verify>
    <automated>pnpm -C apps/server typecheck && pnpm -C apps/server test --run && pnpm -C apps/web test:e2e -- --grep "discuss.*defaults"</automated>
  </verify>
  <done>Discuss defaults progression works end-to-end with audit records and passing tests</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/web test --run`
- `pnpm -C apps/server test --run`
</verification>

<success_criteria>
- Recommended-first options and explicit accept-all action are present and consistent
- Accept-all resolves unresolved discuss decisions deterministically
- Audit trail contains summary + per-decision default entries
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-03-SUMMARY.md`
</output>
