---
phase: 18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa
plan: 05
type: execute
wave: 3
depends_on: [18.2-01, 18.2-04]
files_modified:
  - apps/web/src/hooks/useVerification.ts
  - apps/web/src/stores/verificationStore.ts
  - apps/web/src/app/projects/[id]/verify/page.tsx
  - apps/web/src/components/features/verify/ApprovalBar.tsx
  - apps/web/src/components/features/verify/VerificationPanel.tsx
  - apps/web/src/components/features/verify/ReportHeader.tsx
  - apps/web/src/components/features/verify/ApprovalBar.test.tsx
  - apps/web/src/components/features/verify/VerificationPanel.test.tsx
  - apps/web/tests/e2e/verify-phase.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Approve remains disabled while verification is running"
    - "Approve is only enabled when run reaches terminal state and results count is greater than zero"
    - "When a new verify run starts, previous results stay visible but clearly marked stale until new results complete"
  artifacts:
    - path: "apps/web/src/stores/verificationStore.ts"
      provides: "Run identity, status, result count, and stale-result state model"
      exports: ["useVerificationStore"]
    - path: "apps/web/src/components/features/verify/ApprovalBar.tsx"
      provides: "Approval gating UI behavior tied to terminal+results rule"
      exports: ["ApprovalBar"]
    - path: "apps/web/src/components/features/verify/ReportHeader.tsx"
      provides: "Running vs stale vs terminal visual indicators"
      exports: ["ReportHeader"]
  key_links:
    - from: "useVerification"
      to: "verificationStore"
      via: "run lifecycle + stale-state transitions"
      pattern: "status|runId|resultCount|isStale"
    - from: "verificationStore"
      to: "ApprovalBar"
      via: "derived `canApprove` predicate"
      pattern: "canApprove|disabled"
---

<objective>
Finalize Verify behavior gating and rerun UX to prevent invalid approvals and preserve context during active reruns.

Purpose: ensure verification actions are safe, deterministic, and aligned with locked decision rules.
Output: terminal-state approval gating, stale-result rerun indicator flow, and complete verify regression coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-CONTEXT.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-RESEARCH.md
@apps/web/src/app/projects/[id]/verify/page.tsx
@apps/web/src/hooks/useVerification.ts
@apps/web/src/stores/verificationStore.ts
@apps/web/src/components/features/verify/ApprovalBar.tsx
@apps/web/tests/e2e/verify-phase.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for terminal-state approval gating and stale-rerun behavior</name>
  <files>apps/web/src/components/features/verify/ApprovalBar.test.tsx, apps/web/src/components/features/verify/VerificationPanel.test.tsx, apps/web/tests/e2e/verify-phase.spec.ts</files>
  <action>
Create RED tests that lock the required verify behavior:
- Approve disabled while status is running.
- Approve enabled only when status is terminal and results count > 0.
- Starting a new run preserves prior results with explicit stale indicator.
- Stale indicator clears and new results replace prior view on completion.
  </action>
  <verify>
    <automated>pnpm -C apps/web test --run && pnpm -C apps/web test:e2e -- --grep "verify phase|approve|stale"</automated>
  </verify>
  <done>Verify gating and stale-rerun behavior are encoded as failing regression tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement verification run-state model and approval predicate wiring</name>
  <files>apps/web/src/hooks/useVerification.ts, apps/web/src/stores/verificationStore.ts, apps/web/src/components/features/verify/ApprovalBar.tsx, apps/web/src/components/features/verify/VerificationPanel.tsx, apps/web/src/components/features/verify/ReportHeader.tsx</files>
  <action>
Implement run lifecycle model enhancements:
- track active run identity and result count.
- derive `canApprove` from terminal status + results count > 0.
- on rerun start, preserve old results and mark stale until new run resolves.
- render clear stale/running/terminal states in report header/panel.
  </action>
  <verify>
    <automated>pnpm -C apps/web typecheck && pnpm -C apps/web test --run</automated>
  </verify>
  <done>Verify UI/state behavior matches gating and stale-rerun rules</done>
</task>

<task type="auto">
  <name>Task 3: Green E2E closure for verify safety path and regression prevention</name>
  <files>apps/web/src/app/projects/[id]/verify/page.tsx, apps/web/tests/e2e/verify-phase.spec.ts</files>
  <action>
Finalize page-level wiring so action availability and stale display are consistent with store predicates.
Extend E2E specs for:
- running-state approve disablement
- terminal-state enablement with results
- rerun stale-state banner and eventual replacement.
  </action>
  <verify>
    <automated>pnpm -C apps/web test --run && pnpm -C apps/web test:e2e -- --grep "verify phase|approve|stale"</automated>
  </verify>
  <done>Verify page end-to-end behavior is safe and regression-protected</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/web test --run`
- `pnpm -C apps/web test:e2e -- --grep "verify"`
</verification>

<success_criteria>
- Approval action is blocked during running states
- Approval is only available at terminal state with non-zero results
- Reruns keep previous results visible as stale until replacement data arrives
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-05-SUMMARY.md`
</output>
