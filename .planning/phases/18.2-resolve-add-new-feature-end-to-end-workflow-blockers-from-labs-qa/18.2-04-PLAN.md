---
phase: 18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa
plan: 04
type: execute
wave: 2
depends_on: [18.2-01]
files_modified:
  - apps/web/src/app/projects/[id]/plan/page.tsx
  - apps/web/src/stores/planStore.ts
  - apps/web/src/components/features/plan/PlanKanban.tsx
  - apps/web/src/components/features/plan/FixPlansPanel.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/hooks/usePlanData.ts
  - apps/web/tests/e2e/plan-phase.spec.ts
  - apps/web/src/components/features/plan/FixPlansPanel.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Plan page renders tasks when authoritative server payload contains tasks"
    - "Local stale empty-state does not override non-empty server task payload"
    - "Reject-with-gaps redirect opens fix-plan flow with selected gaps preloaded and visible"
  artifacts:
    - path: "apps/web/src/hooks/usePlanData.ts"
      provides: "Authoritative plan data loading + stale-state reconciliation"
      exports: ["usePlanData"]
    - path: "apps/web/src/components/features/plan/FixPlansPanel.tsx"
      provides: "Fix plan creation entry point with preselected gap chips"
      exports: ["FixPlansPanel"]
    - path: "apps/web/src/app/projects/[id]/plan/page.tsx"
      provides: "Plan page integration for authoritative rendering and gaps flow"
  key_links:
    - from: "usePlanData"
      to: "PlanPhasePage"
      via: "server-first task rendering and empty-state arbitration"
      pattern: "tasks.length|isLoading|error"
    - from: "PlanPhasePage"
      to: "FixPlansPanel"
      via: "query param gaps -> focused fix-plan section"
      pattern: "searchParams.*gaps|autoFocus"
---

<objective>
Fix Plan-phase state binding and gap-fix visibility so generated tasks and reject-with-gaps flows are always visible and actionable.

Purpose: remove false empty states and unblock fix-plan creation from verify rejections.
Output: server-authoritative plan rendering path, fix-plan panel, and regression tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-CONTEXT.md
@.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-RESEARCH.md
@apps/web/src/app/projects/[id]/plan/page.tsx
@apps/web/src/stores/planStore.ts
@apps/web/src/lib/api.ts
@apps/web/tests/e2e/plan-phase.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for authoritative plan payload rendering and gap-preselected fix flow</name>
  <files>apps/web/tests/e2e/plan-phase.spec.ts, apps/web/src/components/features/plan/FixPlansPanel.test.tsx, apps/web/src/hooks/usePlanData.test.ts (new if missing)</files>
  <action>
Write RED tests that fail on current behavior:
- Plan page renders tasks whenever backend payload contains tasks, regardless of stale local empty-state.
- `?gaps=` redirects auto-focus a fix-plan section and show selected gaps as chips/tokens.
- Empty-state only appears when authoritative response is truly empty and no error state exists.
  </action>
  <verify>
    <automated>pnpm -C apps/web test --run && pnpm -C apps/web test:e2e -- --grep "plan phase|fix plans|gaps"</automated>
  </verify>
  <done>Tests fail with current stale-binding behavior and capture expected fixes</done>
</task>

<task type="auto">
  <name>Task 2: Implement server-authoritative plan data flow and stale-state reconciliation</name>
  <files>apps/web/src/hooks/usePlanData.ts, apps/web/src/app/projects/[id]/plan/page.tsx, apps/web/src/stores/planStore.ts, apps/web/src/lib/api.ts</files>
  <action>
Implement a dedicated plan data hook/store flow where server payload is authoritative when present:
- merge/replace local state from fresh payloads.
- prevent stale local empty-state from masking non-empty server tasks.
- preserve loading/error semantics for clear user feedback.
  </action>
  <verify>
    <automated>pnpm -C apps/web typecheck && pnpm -C apps/web test --run</automated>
  </verify>
  <done>Plan UI uses server-authoritative payload and no longer hides valid tasks</done>
</task>

<task type="auto">
  <name>Task 3: Add fix-plan panel integration for reject-with-gaps redirects and green E2E coverage</name>
  <files>apps/web/src/components/features/plan/FixPlansPanel.tsx, apps/web/src/components/features/plan/PlanKanban.tsx, apps/web/src/app/projects/[id]/plan/page.tsx, apps/web/tests/e2e/plan-phase.spec.ts</files>
  <action>
Implement and wire a fix-plan section that:
- auto-focuses on `?gaps=` routes.
- displays selected gaps as preloaded chips.
- provides clear create-fix-plan action affordance.
Ensure behavior coexists with normal plan preview/kanban rendering.
  </action>
  <verify>
    <automated>pnpm -C apps/web test --run && pnpm -C apps/web test:e2e -- --grep "fix plans|gaps"</automated>
  </verify>
  <done>Reject-with-gaps users land on an actionable fix-plan view with selected gaps preloaded</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/web test --run`
- `pnpm -C apps/web test:e2e -- --grep "plan"`
</verification>

<success_criteria>
- Non-empty backend task payloads always render in Plan page
- False "No plan tasks" empty-state is eliminated for stale-local scenarios
- Reject-with-gaps redirects produce visible, preselected fix-plan flows
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa/18.2-04-SUMMARY.md`
</output>
