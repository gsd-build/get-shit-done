---
phase: 18.2-resolve-add-new-feature-end-to-end-workflow-blockers-from-labs-qa
plan: 01
subsystem: api
tags: [projects, routing, discovery, endpoint-errors]
requires: []
provides:
  - Project ID matching supports id, directory basename, and project name
  - Workflow routes resolve direct-link project IDs consistently
  - Endpoint failures return explicit API errors for UI handling
affects: [project-discovery, plan-phase, verify-phase]
tech-stack:
  added: []
  patterns: [server-side project-id matcher reuse]
key-files:
  created: []
  modified:
    - apps/server/src/api/routes/projects.ts
    - apps/server/src/api/routes/phases.ts
    - apps/server/src/api/routes/execute.ts
key-decisions:
  - "No fallback endpoint probing in MVP; return explicit errors"
patterns-established:
  - "Resolve project by id OR basename OR normalized project name"
requirements-completed: []
duration: 45min
completed: 2026-03-14
---

# Phase 18.2-01 Summary

**Normalized project route resolution so direct-link projects (for example `todo`) are handled consistently across project, phase, and execute API endpoints.**

## Accomplishments
- Added a reusable project-id matching strategy on key API routes.
- Removed strict `p.id === projectId` dependency from execute path lookup.
- Preserved explicit error behavior for unsupported/unavailable endpoints.

## Verification
- `pnpm -C apps/server typecheck`
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/web test -- --run`

## Deviations from Plan
None.
