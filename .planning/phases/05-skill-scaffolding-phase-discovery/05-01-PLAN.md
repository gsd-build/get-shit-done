---
phase: 05-skill-scaffolding-phase-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/roadmap.cjs
  - commands/gsd/autonomous.md
  - tests/roadmap.test.cjs
autonomous: true
requirements: [ART-01, ART-03, ORCH-03]

must_haves:
  truths:
    - "roadmap analyze returns non-null goal for **Goal**: format (colon outside bold)"
    - "roadmap get-phase returns non-null goal for **Goal**: format"
    - "roadmap analyze returns non-null depends_on for **Depends on**: format"
    - "commands/gsd/autonomous.md has valid name, description, argument-hint, allowed-tools frontmatter"
    - "Command uses name: gsd:autonomous so installer auto-generates gsd-autonomous skill"
  artifacts:
    - path: "get-shit-done/bin/lib/roadmap.cjs"
      provides: "Fixed regex for goal/depends_on extraction supporting both colon-inside and colon-outside bold"
    - path: "commands/gsd/autonomous.md"
      provides: "Command definition for gsd:autonomous with valid frontmatter and sections"
      contains: "name: gsd:autonomous"
    - path: "tests/roadmap.test.cjs"
      provides: "Tests covering **Goal**: format (colon outside bold)"
  key_links:
    - from: "get-shit-done/bin/lib/roadmap.cjs"
      to: ".planning/ROADMAP.md"
      via: "regex extraction in cmdRoadmapGetPhase and cmdRoadmapAnalyze"
      pattern: "Goal.*:.*\\s*([^\\n]+)"
    - from: "commands/gsd/autonomous.md"
      to: "get-shit-done/workflows/autonomous.md"
      via: "execution_context reference"
      pattern: "@~/.claude/get-shit-done/workflows/autonomous.md"
---

<objective>
Fix the regex bug in roadmap.cjs that prevents goal/depends_on extraction, and create the `gsd:autonomous` command file following existing project conventions.

Purpose: The regex fix is a prerequisite for ORCH-03 (phase discovery returning complete data). The command file establishes the entry point that the installer converts to a Copilot skill (ART-01, ART-03).
Output: Fixed roadmap.cjs + `commands/gsd/autonomous.md` + extended tests
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-scaffolding-phase-discovery/05-CONTEXT.md
@.planning/phases/05-skill-scaffolding-phase-discovery/05-RESEARCH.md

<interfaces>
<!-- Key code the executor needs. Extracted from codebase. -->

From get-shit-done/bin/lib/roadmap.cjs (line 67 — cmdRoadmapGetPhase):
```javascript
// Extract goal if present
const goalMatch = section.match(/\*\*Goal:\*\*\s*([^\n]+)/i);
const goal = goalMatch ? goalMatch[1].trim() : null;
```

From get-shit-done/bin/lib/roadmap.cjs (lines 120-123 — cmdRoadmapAnalyze):
```javascript
const goalMatch = section.match(/\*\*Goal:\*\*\s*([^\n]+)/i);
const goal = goalMatch ? goalMatch[1].trim() : null;

const dependsMatch = section.match(/\*\*Depends on:\*\*\s*([^\n]+)/i);
const depends_on = dependsMatch ? dependsMatch[1].trim() : null;
```

From .planning/ROADMAP.md (actual format — note colon OUTSIDE bold):
```markdown
**Goal**: The autonomous skill files exist following project conventions...
**Depends on**: Phase 4 (v1.23 complete)
```

From tests/roadmap.test.cjs (test pattern):
```javascript
const { test, describe, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const path = require('path');
const { runGsdTools, createTempProject, cleanup } = require('./helpers.cjs');
```

From commands/gsd/new-milestone.md (command file pattern):
```markdown
---
name: gsd:new-milestone
description: Start a new milestone cycle — update PROJECT.md and route to requirements
argument-hint: "[milestone name, e.g., 'v1.1 Notifications']"
allowed-tools:
  - Read
  - Write
  - Bash
  - Task
  - AskUserQuestion
---
<objective>...</objective>
<execution_context>...</execution_context>
<context>...</context>
<process>...</process>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix roadmap.cjs regex for goal/depends_on extraction</name>
  <files>get-shit-done/bin/lib/roadmap.cjs, tests/roadmap.test.cjs</files>
  <behavior>
    - Test: `roadmap get-phase` returns non-null goal when ROADMAP uses `**Goal**: text` (colon outside bold)
    - Test: `roadmap analyze` returns non-null goal when ROADMAP uses `**Goal**: text` (colon outside bold)
    - Test: `roadmap analyze` returns non-null depends_on when ROADMAP uses `**Depends on**: text` (colon outside bold)
    - Test: Existing `**Goal:** text` format (colon inside bold) still works (no regression)
  </behavior>
  <action>
**RED:** Add tests to `tests/roadmap.test.cjs` within the existing `roadmap get-phase command` and `roadmap analyze` describe blocks. Write tests that create a ROADMAP.md using `**Goal**: text` format (colon outside bold) and assert the goal/depends_on fields are non-null. Run tests — they MUST fail (confirming the bug).

**GREEN:** Fix 3 regex patterns in `get-shit-done/bin/lib/roadmap.cjs`:

1. Line 67 (cmdRoadmapGetPhase goal): Change `/\*\*Goal:\*\*\s*([^\n]+)/i` to `/\*\*Goal(?:\*\*:|\*\*\s*:)\s*([^\n]+)/i`
2. Line 120 (cmdRoadmapAnalyze goal): Same fix as above
3. Line 123 (cmdRoadmapAnalyze depends_on): Change `/\*\*Depends on:\*\*\s*([^\n]+)/i` to `/\*\*Depends on(?:\*\*:|\*\*\s*:)\s*([^\n]+)/i`

The fix makes the closing `**` required but allows the colon to appear either before or after it: `**Goal:**` or `**Goal**:`.

**REFACTOR:** Not needed — the regex change is minimal.

Run `node --test tests/roadmap.test.cjs` — all tests must pass including existing tests (no regression).
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && node --test tests/roadmap.test.cjs</automated>
  </verify>
  <done>
    - New tests cover `**Goal**: text` and `**Depends on**: text` formats (colon outside bold)
    - Existing tests for `**Goal:** text` format still pass (no regression)
    - `roadmap analyze` on real ROADMAP.md returns non-null goal and depends_on for all phases
  </done>
</task>

<task type="auto">
  <name>Task 2: Create commands/gsd/autonomous.md command file</name>
  <files>commands/gsd/autonomous.md</files>
  <action>
Create `commands/gsd/autonomous.md` following the exact pattern of `commands/gsd/new-milestone.md`.

**Frontmatter (YAML):**
```yaml
---
name: gsd:autonomous
description: Run all remaining phases autonomously — discuss→plan→execute per phase
argument-hint: "[--from N]"
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - AskUserQuestion
  - Task
---
```

**Sections:**

`<objective>`: Execute all remaining milestone phases autonomously. For each phase: discuss → plan → execute. Pauses only for user decisions (grey area acceptance, blockers, validation requests). Uses ROADMAP.md phase discovery, Skill() flat invocations for each phase command. After: Milestone audit → complete → cleanup (Phase 8 scope).

`<execution_context>`: Reference `@~/.claude/get-shit-done/workflows/autonomous.md` and `@~/.claude/get-shit-done/references/ui-brand.md` — same `@~/.claude/` prefix pattern as all other commands (installer rewrites these per runtime).

`<context>`: Document the `--from N` flag behavior: start from phase N instead of first incomplete phase. Note that context files are resolved in-workflow using init commands.

`<process>`: Single paragraph delegating to the workflow file: "Execute the autonomous workflow from @~/.claude/get-shit-done/workflows/autonomous.md end-to-end. Preserve all workflow gates (phase discovery, per-phase execution, blocker handling, progress display)."

Total file should be ~40-45 lines, matching the size and style of `new-milestone.md` (44 lines).
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && node -e "const fs=require('fs'); const f=fs.readFileSync('commands/gsd/autonomous.md','utf8'); const m=f.match(/^---\n([\s\S]*?)\n---/); if(!m) process.exit(1); const y=m[1]; if(!y.includes('name: gsd:autonomous')) process.exit(1); if(!y.includes('argument-hint:')) process.exit(1); if(!y.includes('allowed-tools:')) process.exit(1); if(!f.includes('execution_context')) process.exit(1); if(!f.includes('workflows/autonomous.md')) process.exit(1); console.log('OK: autonomous.md validated')"</automated>
  </verify>
  <done>
    - `commands/gsd/autonomous.md` exists with `name: gsd:autonomous`, `description`, `argument-hint: "[--from N]"`, `allowed-tools` list
    - File has `<objective>`, `<execution_context>`, `<context>`, `<process>` sections
    - `<execution_context>` references `@~/.claude/get-shit-done/workflows/autonomous.md`
    - File follows the exact pattern of existing commands (new-milestone.md)
  </done>
</task>

</tasks>

<verification>
1. `node --test tests/roadmap.test.cjs` — all tests pass (existing + new regex format tests)
2. `commands/gsd/autonomous.md` exists with valid frontmatter containing `name: gsd:autonomous`
3. Live verification: `node .github/get-shit-done/bin/gsd-tools.cjs roadmap analyze | node -e "const d=JSON.parse(require('fs').readFileSync(0,'utf8')); d.phases.forEach(p => { if(p.goal===null) { console.error('FAIL: null goal for phase '+p.number); process.exit(1) } }); console.log('OK: all goals non-null')"` — confirms regex fix against real ROADMAP.md
</verification>

<success_criteria>
- roadmap.cjs regex handles both `**Goal:**` and `**Goal**:` formats
- `commands/gsd/autonomous.md` exists with valid frontmatter and all required sections
- All existing roadmap tests still pass (no regression)
- Running `roadmap analyze` on the real ROADMAP.md returns non-null goals for all phases
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill-scaffolding-phase-discovery/05-01-SUMMARY.md`
</output>
