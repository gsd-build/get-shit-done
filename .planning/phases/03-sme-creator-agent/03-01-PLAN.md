---
phase: 03-sme-creator-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-sme-creator.md
  - agents/gsd-sme-creator-analyzer.md
autonomous: true
requirements:
  - CREATE-01
  - CREATE-02
  - CREATE-03
  - CREATE-04
must_haves:
  truths:
    - "gsd-sme-creator orchestrator agent exists and can be spawned via Task with subagent_type='gsd-sme-creator'"
    - "gsd-sme-creator-analyzer sub-agent exists and can be spawned by orchestrator with subagent_type='gsd-sme-creator-analyzer'"
    - "Orchestrator includes Task in tools frontmatter for parallel sub-agent spawning (CREATE-02)"
    - "Analyzer does NOT include Task in tools frontmatter (sub-agents do not spawn further sub-agents)"
    - "Orchestrator instructions produce a complete SME document with all 6 required H2 sections and severity-labeled findings (CREATE-03)"
    - "Analyzer instructions mandate git log --follow for every file to capture historical rationale (CREATE-04)"
    - "Orchestrator includes sequential fallback when Task tool is unavailable"
    - "Orchestrator includes severity calibration with inline BLOCKER/WARNING/WATCH examples"
  artifacts:
    - path: "agents/gsd-sme-creator.md"
      provides: "SME creator orchestrator agent definition"
      contains: "name: gsd-sme-creator"
    - path: "agents/gsd-sme-creator-analyzer.md"
      provides: "SME creator analyzer sub-agent definition"
      contains: "name: gsd-sme-creator-analyzer"
  key_links:
    - from: "agents/gsd-sme-creator.md"
      to: "agents/gsd-sme-creator-analyzer.md"
      via: "Task(subagent_type='gsd-sme-creator-analyzer')"
      pattern: 'subagent_type="gsd-sme-creator-analyzer"'
    - from: "agents/gsd-sme-creator.md"
      to: "get-shit-done/templates/sme.md"
      via: "Document structure contract -- orchestrator writes SME files matching the Phase 1 template schema"
      pattern: "Process Overview.*Identified Risks.*Test Gaps.*Outdated Logic.*Edge Cases.*Known Blockers"
---

<objective>
Create both SME creator agent markdown definitions: the orchestrator (gsd-sme-creator) that manages process file discovery, parallel sub-agent spawning, finding synthesis, and atomic SME document writing, and the analyzer sub-agent (gsd-sme-creator-analyzer) that performs per-file code analysis and git archaeology on assigned file partitions.

Purpose: These two agents are the core engine that produces SME documents consumed by all downstream phases (gate, auditor, discuss, refresh). The orchestrator/sub-agent decomposition prevents context budget exhaustion on large codepaths (CREATE-02) while the analyzer's git log --follow mandate ensures findings capture historical rationale (CREATE-04).

Output: Two agent definition files in agents/ ready to be invoked by the Phase 4 create-sme workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sme-creator-agent/03-RESEARCH.md
@.planning/phases/03-sme-creator-agent/03-PATTERNS.md
@.planning/phases/03-sme-creator-agent/03-AI-SPEC.md
@.planning/phases/01-schema-config/01-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase and prior phases. -->
<!-- Executor should use these directly -- no codebase exploration needed beyond read_first files. -->

From get-shit-done/templates/sme.md (Phase 1 template -- the exact output format the agent must produce):

YAML frontmatter fields (between triple-dash delimiters):
  process_name: [PROCESS_NAME]
  last_analyzed_commit: [COMMIT_HASH]
  block_mode: soft
  created_date: [DATE]
  finding_counts:
    blocker: 0
    warning: 0
    watch: 0

Six H2 sections in fixed order:
- ## Process Overview
- ## Identified Risks
- ## Test Gaps
- ## Outdated Logic
- ## Edge Cases
- ## Known Blockers

Finding format per section:
  [SEVERITY] **Bold Title** -- description
  *Evidence:* `file/path.ts:line` -- what was observed
  *Mitigation:* concrete action

From agents/gsd-codebase-mapper.md (frontmatter pattern -- exact structure for agent files):

YAML frontmatter fields (between triple-dash delimiters):
  name: gsd-codebase-mapper
  description: Explores codebase and writes structured analysis documents.
  tools: Read, Bash, Grep, Glob, Write
  color: cyan

Agent file structure order:
1. YAML frontmatter (name, description, tools, color)
2. role block (identity, job summary, constraints)
3. Domain-specific blocks (severity_examples, forbidden_files)
4. process block with named step elements (some with condition= attributes)
5. critical_rules block (hard constraints in bold)
6. success_criteria block (checklist format)

From get-shit-done/workflows/map-codebase.md (Task/TaskOutput spawn pattern):

  Task(
    subagent_type="gsd-codebase-mapper",
    model="{mapper_model}",
    run_in_background=true,
    description="...",
    prompt="..."
  )

  <!-- Collect confirmations -- ALL TaskOutput calls in parallel -->
  TaskOutput tool:
    task_id: "{task_id from Task result}"
    block: true
    timeout: 300000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gsd-sme-creator-analyzer sub-agent definition</name>
  <files>agents/gsd-sme-creator-analyzer.md</files>
  <read_first>
    - agents/gsd-codebase-mapper.md (primary analog -- copy frontmatter structure, role block structure, forbidden_files block, return confirmation pattern)
    - get-shit-done/templates/sme.md (SME document format -- the finding format the analyzer must write to .tmp files)
    - .planning/phases/03-sme-creator-agent/03-PATTERNS.md (exact code excerpts to copy for each section)
  </read_first>
  <action>
Create `agents/gsd-sme-creator-analyzer.md` -- the sub-agent that analyzes a file partition for a named process.

**YAML frontmatter** (copy structure from gsd-codebase-mapper.md, use triple-dash delimiters):
  name: gsd-sme-creator-analyzer
  description: Analyzes a file partition for SME risks, test gaps, outdated logic, and edge cases. Writes findings to .tmp file. Spawned by gsd-sme-creator.
  tools: Read, Bash, Grep, Glob, Write
  color: "#F59E0B"

CRITICAL: `tools:` must NOT include `Task` -- sub-agents do not spawn further sub-agents.
CRITICAL: `name:` must be exactly `gsd-sme-creator-analyzer` -- this is the subagent_type the orchestrator uses.

**`<role>` block** -- announce identity, job, and inputs:

You are a GSD SME analyzer sub-agent. You are spawned by `gsd-sme-creator` to analyze a set of files for a named process.

Your job: Read each file + its git history, identify domain-specific risks, and write compressed findings to a designated output path. Return finding count only -- NOT full content.

You receive three inputs via the prompt:
- **Process name:** The business process being analyzed (e.g., "contribution-processing")
- **File list:** Comma-separated paths to analyze
- **Output path:** Where to write findings (e.g., `.planning/smes/.tmp/{PROCESS_NAME}-part-{N}.md`)

**`<analysis_checklist>` block** -- the core analysis protocol for each file:

For EACH file in the provided file list:

1. **Verify file exists:** `ls <file>` -- if file does not exist, skip it and note in output. NEVER cite a file that does not exist.

2. **Read file for code pattern analysis:** Use Read tool. Look for:
   - Business rule implementations (if/else branches with domain-specific conditions)
   - Magic numbers, hardcoded thresholds, or boundary conditions
   - Error handling gaps in critical paths
   - Data contract assumptions (field names, types, required vs optional)
   - TODO/FIXME/HACK comments that indicate known issues

3. **Run git history -- MANDATORY for every file:**
   `git log --follow -n 50 --format="%H %s" <file>`
   The `--follow` flag is REQUIRED -- it traces file history across renames. Without it, all pre-rename history (where the "why" often lives) is invisible.

4. **Run PR description extraction:**
   `git log --follow --grep="Merge pull request" --format="%H %s%n%b" -n 20 <file>`

5. **NEVER use `git log --all`** -- it includes all branches and produces unbounded output.

6. **Identify findings** across these categories:
   - **Risks:** Patterns that could break under modification (fragile logic, implicit assumptions, race conditions)
   - **Test gaps:** Specific dangerous scenarios with no test coverage (name the scenario, not a coverage percentage)
   - **Outdated logic:** Code correct under old rules/APIs/policies that may no longer apply (cite the original context AND evidence it changed)
   - **Edge cases:** Boundary conditions, empty inputs, overflow paths that are unguarded

**`<finding_format>` block** -- exact output format per finding:

For each finding, write in this exact format:

  [SEVERITY] **Bold Title** -- one-line description of the risk
  File: `path/to/file.ts`, function `name()`, lines X-Y
  Why: [commit hash, PR number, or code comment that explains why this pattern exists]
  Evidence: [specific git log output line, code comment text, or PR description excerpt]
  Mitigation: [concrete action -- name the file, function, and what to change]

Severity definitions:
- **BLOCKER:** Violation would break a known production behavior or data contract. MUST cite historical evidence (commit hash, PR number, or code comment proving real production impact). A BLOCKER without historical evidence is malformed -- downgrade to WARNING.
- **WARNING:** Lower-stakes risk or known limitation with evidence but no current production breakage path.
- **WATCH:** Code smell or theoretical risk with no current production evidence.

FALSE BLOCKER IS THE MORE DANGEROUS ERROR -- it erodes trust and causes developers to bypass the gate entirely.

**`<forbidden_files>` block** -- copy verbatim from gsd-codebase-mapper.md lines 808-828:

  NEVER read or quote contents from: .env, .env.*, *.env, credentials.*, secrets.*,
  *secret*, *credential*, *.pem, *.key, .npmrc, serviceAccountKey.json.

  If you encounter these files: note EXISTENCE only, never quote contents.

**`<process>` block** with the analysis steps:

step name="validate_inputs": Parse the prompt for PROCESS_NAME, file list (comma-separated), and output path. If any input is missing, return an error message and exit.

step name="analyze_files": For each file in the file list, follow the analysis_checklist exactly:
1. Verify existence with `ls`
2. Read file content
3. Run `git log --follow -n 50` (MANDATORY)
4. Run `git log --follow --grep="Merge pull request"` for PR context
5. Identify risks, test gaps, outdated logic, edge cases

step name="write_findings": Write all findings to the designated output path using the finding_format. Group findings by category (### Risks, ### Test Gaps, ### Outdated Logic, ### Edge Cases).

step name="return_confirmation": Return a brief confirmation. DO NOT include findings content. Format: "N findings written to [output_path]"

**`<critical_rules>` block** -- hard constraints in bold:

**ALWAYS USE git log --follow.** The --follow flag traces file history across renames. Without it, pre-rename history is invisible -- and that is exactly where the "why" behind patterns lives.

**VERIFY FILE PATHS BEFORE CITING.** For each file path you analyze or cite in a finding, verify it exists with `ls <path>`. Never cite a file that does not exist.

**BLOCKER SEVERITY IS STRICT.** Only apply BLOCKER when you have evidence of a real production failure path -- a commit hash, PR number, or code comment proving the risk is real. BLOCKER inflation destroys trust in the entire SME system.

**RETURN FINDING COUNT ONLY.** Write findings to the output file. Return only "N findings written to [path]". The orchestrator reads the file -- do not include findings in your response.

**NEVER READ SECRETS.** See forbidden_files -- note existence only, never read or quote content.

**`<success_criteria>` block** -- checklist format:

- [ ] Every file in the file list was analyzed (or noted as non-existent)
- [ ] `git log --follow` was run for every existing file
- [ ] All findings use the exact format from finding_format
- [ ] Every BLOCKER finding cites a commit hash, PR number, or code comment
- [ ] Findings written to the designated output path
- [ ] Response contains only the finding count confirmation

The complete file should be approximately 120-160 lines following the structure order: frontmatter, role, analysis_checklist, finding_format, forbidden_files, process, critical_rules, success_criteria.
  </action>
  <verify>
    <automated>grep -c "name: gsd-sme-creator-analyzer" agents/gsd-sme-creator-analyzer.md && grep "tools:" agents/gsd-sme-creator-analyzer.md | grep -v "Task" && grep -c "git log --follow" agents/gsd-sme-creator-analyzer.md && grep -c "forbidden_files" agents/gsd-sme-creator-analyzer.md && grep -c "BLOCKER" agents/gsd-sme-creator-analyzer.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-sme-creator-analyzer.md exists
    - File contains `name: gsd-sme-creator-analyzer` in YAML frontmatter
    - File contains `tools: Read, Bash, Grep, Glob, Write` (NO Task)
    - File contains `color: "#F59E0B"` in frontmatter
    - File contains `<role>` block with "GSD SME analyzer sub-agent" identity
    - File contains `git log --follow` at least 3 times (checklist, process step, critical rules)
    - File contains `<forbidden_files>` block with .env, .pem, .key patterns
    - File contains `<finding_format>` block with BLOCKER/WARNING/WATCH definitions
    - File contains `<critical_rules>` block with bold constraints
    - File contains `<success_criteria>` block with checklist items
    - File does NOT contain `Task` in the tools: line
    - `grep -c "## " agents/gsd-sme-creator-analyzer.md` is 0 (no H2 sections -- those belong in the output, not the agent definition)
  </acceptance_criteria>
  <done>Analyzer sub-agent definition exists at agents/gsd-sme-creator-analyzer.md with correct name for subagent_type matching, no Task tool, mandatory git log --follow in analysis protocol, forbidden files block, severity calibration definitions, and confirmation-only return pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Create gsd-sme-creator orchestrator agent definition</name>
  <files>agents/gsd-sme-creator.md</files>
  <read_first>
    - agents/gsd-sme-creator-analyzer.md (just created -- verify name: field for subagent_type matching)
    - agents/gsd-codebase-mapper.md (primary analog -- role block, critical_rules block, success_criteria block structure)
    - get-shit-done/workflows/map-codebase.md (canonical Task/TaskOutput spawn/collect pattern -- read lines 139-289 for spawn_agents and collect_confirmations steps)
    - get-shit-done/templates/sme.md (exact SME document structure the orchestrator must produce)
    - .planning/phases/03-sme-creator-agent/03-PATTERNS.md (exact code excerpts for each section)
    - .planning/phases/03-sme-creator-agent/03-RESEARCH.md (Pattern 4: sequential fallback, Pattern 7: self-check, Pattern 8: severity calibration)
  </read_first>
  <action>
Create `agents/gsd-sme-creator.md` -- the orchestrator agent that discovers process files, spawns analyzer sub-agents, synthesizes findings, and writes the final SME document.

**YAML frontmatter** (use triple-dash delimiters):
  name: gsd-sme-creator
  description: Analyzes a codebase process and produces a structured SME document by spawning parallel analyzer sub-agents and synthesizing their findings.
  tools: Read, Bash, Grep, Glob, Write, Task
  color: "#F59E0B"

CRITICAL: `tools:` MUST include `Task` -- this agent spawns sub-agents.
CRITICAL: `name:` must be exactly `gsd-sme-creator` -- the Phase 4 workflow uses this as the subagent_type.

**`<role>` block** with severity calibration:

You are a GSD SME creator. Given a PROCESS_NAME, you analyze its code paths, git history, PR descriptions, and docs to produce `.planning/smes/{PROCESS_NAME}-SME.md`.

You are an orchestrator -- you discover files belonging to the process, partition them, and spawn `gsd-sme-creator-analyzer` sub-agents to do the heavy analysis. Sub-agents write findings to temp files. You synthesize their findings into the final SME document.

**You NEVER read raw file content or git logs yourself (in parallel mode).** Sub-agents handle all file-level analysis. You read only their compressed findings from .tmp files after they complete.

**Severity definitions:**
- **BLOCKER:** Apply ONLY when violation would break a known production behavior or data contract. MUST have historical evidence (commit hash, PR, code comment).
- **WARNING:** Lower-stakes risk or known limitation with evidence.
- **WATCH:** Code smell or theoretical risk with no current production evidence.

FALSE BLOCKER IS THE MORE DANGEROUS ERROR. It erodes trust and causes developers to bypass the gate.

**`<severity_examples>` block** -- exactly 3 inline few-shot examples (one per level):

BLOCKER example:
> [BLOCKER] **phase-runner.ts allows concurrent phase execution**
> File: `sdk/src/phase-runner.ts`, function `run()`, lines 91-200
> Why: Concurrent phase guard was removed in commit a4b2c1d (PR #47) to fix CLI timeout. The state machine was never made thread-safe.
> Evidence: git log --grep="concurrent" sdk/src/phase-runner.ts -- PR #47: "deferred locking fix"
> Mitigation: Restore the concurrent execution guard in run() with a file-based lock; reference PR #47 for the original implementation.

WARNING example:
> [WARNING] **sme.detect-processes uses keyword matching only**
> File: `sdk/src/query/sme.ts`
> Why: MVP keyword match introduced in Phase 2. Processes named with abbreviations (e.g. "contrib" vs "contribution") will not match.
> Evidence: // TODO: semantic matching -- keyword MVP only (phase-2)
> Mitigation: Accept for now; plan semantic matching in a future phase.

WATCH example:
> [WATCH] **No input length guard on process name parameter**
> File: `sdk/src/query/sme.ts`, function `smeDetectProcesses()`
> Why: Process name is passed through to grep without length validation. Theoretical DoS if extremely long string passed.
> Evidence: Code inspection -- no explicit length check before grep invocation.
> Mitigation: Add a max-length guard (256 chars) at the handler boundary.

**`<process>` block** with all steps -- this is the core orchestration logic:

**Step 1: `detect_capabilities`** -- Check if Task tool is available. If not, route to sequential_analysis step. Copy the detection pattern exactly from get-shit-done/workflows/map-codebase.md lines 129-137 (detect_runtime_capabilities step).

**Step 2: `discover_process_files`** -- Find all files belonging to the named process:
  grep -rl "{PROCESS_NAME}" --include="*.ts" --include="*.md" sdk/src/ agents/ get-shit-done/ 2>/dev/null
  find . -name "*{PROCESS_NAME}*" -not -path "*/node_modules/*" -not -path "*/.planning/*" -not -path "*/.git/*" -not -path "*/.claude/*"
Deduplicate results. If zero files found: produce a minimal SME document with "Process not yet implemented" in Process Overview and exit (no sub-agents needed).
Get HEAD commit for frontmatter: `git rev-parse HEAD`

**Step 3: `partition_files`** -- Group files into partitions of 5-10 files. Rules:
- Any single file >500 lines gets its own partition
- Files in the same directory prefer the same partition
- Create the temp directory: `mkdir -p .planning/smes/.tmp`

**Step 4: `spawn_analyzers`** (condition="Task tool available") -- For each partition N:
  Task(
    subagent_type="gsd-sme-creator-analyzer",
    description="Analyze {PROCESS_NAME} files partition {N}",
    run_in_background=true,
    prompt="Process: {PROCESS_NAME}
  Files: {comma_separated_file_list}
  Output: .planning/smes/.tmp/{PROCESS_NAME}-part-{N}.md
  Analyze each file for domain-specific risks, test gaps, outdated logic, and edge cases.
  For EVERY file, run: git log --follow -n 50 <file>
  Capture WHY patterns exist -- cite commit hashes, PR numbers, and code comments.
  Write findings using [SEVERITY] **Title** format. Return finding count only."
  )
CRITICAL: `subagent_type` must be exactly `"gsd-sme-creator-analyzer"` -- must match the `name:` field in `agents/gsd-sme-creator-analyzer.md`.

**Step 5: `collect_results`** -- Call TaskOutput for ALL spawned tasks with block=true, timeout=300000. Call ALL TaskOutput calls in parallel (single message with N TaskOutput calls). Then -- and ONLY then -- read each `.planning/smes/.tmp/{PROCESS_NAME}-part-{N}.md`.
CRITICAL: Do NOT read any .tmp files before ALL TaskOutput calls complete. Reading before completion returns empty or partial content silently.

**Step 6: `sequential_analysis`** (condition="Task tool is NOT available") -- Perform 4-pass in-context analysis, writing partial findings after each pass to stay below 50% context:
- Pass 1: Read source files, identify code patterns and structural risks. Write draft findings to `.planning/smes/.tmp/{PROCESS_NAME}-pass1.md`.
- Pass 2: Run `git log --follow -n 50 <file>` for each file. Annotate draft findings with "why" evidence (commit hashes, PR numbers). Update `.planning/smes/.tmp/{PROCESS_NAME}-pass2.md`.
- Pass 3: Read test files (find files matching `*.test.ts`, `*.spec.ts`). Identify specific untested dangerous scenarios. Write to `.planning/smes/.tmp/{PROCESS_NAME}-pass3.md`.
- Pass 4: Synthesize all passes into the final SME document (next step).

**Step 7: `synthesize_and_write`** -- Merge all findings (from .tmp files). Apply these rules:
- Deduplicate: same file + same line range = same finding (keep the one with more evidence)
- Apply severity: verify every BLOCKER has historical evidence; downgrade to WARNING if not
- Count findings by severity: tally blocker, warning, watch counts
- Write the final document atomically to `.planning/smes/{PROCESS_NAME}-SME.md` using the exact template format from Phase 1:
  - YAML frontmatter with: process_name, last_analyzed_commit (from git rev-parse HEAD), block_mode: soft, created_date (today), finding_counts (blocker/warning/watch counts)
  - Six H2 sections in fixed order: Process Overview, Identified Risks, Test Gaps, Outdated Logic, Edge Cases, Known Blockers
  - Process Overview: prose description of the process synthesized from file analysis
  - Distribute findings into the appropriate section based on their category
  - Sections with no findings: write "No {category} findings identified." (not empty)

**Self-check after write:**
  SECTION_COUNT=$(grep -c "## Process Overview\|## Identified Risks\|## Test Gaps\|## Outdated Logic\|## Edge Cases\|## Known Blockers" .planning/smes/${PROCESS_NAME}-SME.md)
Must be >= 6. On failure: rewrite missing sections. Maximum 2 repair attempts. On second failure: write document with `## INCOMPLETE` marker and note section count in result.

**Step 8: `cleanup`** -- Delete the temp directory: `rm -rf .planning/smes/.tmp`

**Step 9: `return_confirmation`** -- Return a brief confirmation. DO NOT include document contents.
  ## SME Creation Complete
  **Process:** {PROCESS_NAME}
  **Output:** `.planning/smes/{PROCESS_NAME}-SME.md`
  **Findings:** {blocker_count} BLOCKERs, {warning_count} WARNINGs, {watch_count} WATCHes
  **Commit:** {last_analyzed_commit}

**`<critical_rules>` block** -- hard constraints in bold:

**SUB-AGENTS WRITE FILES DIRECTLY.** Do not hold raw code or full git history in orchestrator context. Route all file analysis to sub-agents. Read only compressed findings from .tmp files after TaskOutput completes.

**ALWAYS CALL TaskOutput(block=true) BEFORE READING OUTPUT FILES.** Reading a .tmp file before the sub-agent completes returns empty or partial content silently. No error is raised.

**subagent_type MUST MATCH name: EXACTLY.** Use `subagent_type="gsd-sme-creator-analyzer"` -- any mismatch silently falls back to a generic agent with no role instructions.

**BLOCKER SEVERITY IS STRICT.** Only apply BLOCKER when evidence of a real production failure path exists -- a commit hash, PR number, or code comment. BLOCKER inflation destroys trust in the gate.

**VALIDATE FILE PATHS BEFORE CITING.** For each file path in a finding, verify existence with `ls <path>` before writing the finding to the output document. Never cite a non-existent file.

**PROCESS BOUNDARY.** Only analyze files that match the PROCESS_NAME. Do not crawl the entire codebase. Scope discovery to grep/glob matches.

**ATOMIC FINAL WRITE.** Collect ALL findings first, then write the final SME document in one Write tool call. Partial writes risk a malformed document being consumed by the gate.

**`<success_criteria>` block** -- checklist format:

- [ ] All process files discovered via grep + find
- [ ] Files partitioned into groups of 5-10
- [ ] Sub-agents spawned (parallel) or 4-pass analysis completed (sequential)
- [ ] All sub-agent .tmp files read after TaskOutput confirmation
- [ ] Findings deduplicated and severity-verified
- [ ] Every BLOCKER has historical evidence (commit/PR/comment)
- [ ] Final SME document written to .planning/smes/{PROCESS_NAME}-SME.md
- [ ] Document has all 6 required H2 sections (self-check passed)
- [ ] Frontmatter has last_analyzed_commit, block_mode, finding_counts
- [ ] .tmp directory cleaned up
- [ ] Confirmation returned with finding counts

The complete orchestrator file should be approximately 250-350 lines. It is the larger of the two agents because it contains: role + severity examples + full multi-step process with parallel and sequential paths + critical rules + success criteria.
  </action>
  <verify>
    <automated>grep -c "name: gsd-sme-creator" agents/gsd-sme-creator.md | head -1 && grep "tools:" agents/gsd-sme-creator.md | grep "Task" && grep -c 'subagent_type="gsd-sme-creator-analyzer"' agents/gsd-sme-creator.md && grep -c "sequential" agents/gsd-sme-creator.md && grep -c "severity_examples" agents/gsd-sme-creator.md && grep -c "## Process Overview" agents/gsd-sme-creator.md && grep -c "self-check\|SECTION_COUNT" agents/gsd-sme-creator.md</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-sme-creator.md exists
    - File contains `name: gsd-sme-creator` in YAML frontmatter
    - File contains `tools: Read, Bash, Grep, Glob, Write, Task` (WITH Task)
    - File contains `color: "#F59E0B"` in frontmatter
    - File contains `<role>` block with "GSD SME creator" identity
    - File contains `<severity_examples>` block with at least one BLOCKER and one WARNING example
    - File contains `subagent_type="gsd-sme-creator-analyzer"` at least once (in spawn step)
    - File contains `TaskOutput` with `block: true` or `block=true` (in collect step)
    - File contains a `sequential_analysis` or `sequential` step with condition for Task unavailability
    - File contains 4-pass sequential fallback (code patterns, git history, test gaps, synthesis)
    - File contains self-check: `grep -c` of required section headers with threshold >= 6
    - File contains `## Process Overview` in the synthesize step's document structure
    - File contains all 6 H2 section names: Process Overview, Identified Risks, Test Gaps, Outdated Logic, Edge Cases, Known Blockers
    - File contains `<critical_rules>` block with at least 5 bold rules
    - File contains `<success_criteria>` block with checklist items
    - File contains `rm -rf .planning/smes/.tmp` or equivalent cleanup
    - File contains `git rev-parse HEAD` for last_analyzed_commit
    - File contains `## INCOMPLETE` marker for self-check failure case
    - `grep "subagent_type" agents/gsd-sme-creator.md` shows exactly `gsd-sme-creator-analyzer` (name match verification)
  </acceptance_criteria>
  <done>Orchestrator agent definition exists at agents/gsd-sme-creator.md with Task in tools, correct subagent_type matching the analyzer's name, parallel spawn/collect/synthesize steps, sequential 4-pass fallback, severity calibration with inline examples, self-check with repair loop, atomic document write matching Phase 1 template schema, and cleanup of temp directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process name input -> file path construction | Process name from workflow prompt is used to construct file paths for SME output and grep patterns |
| Agent-generated findings -> SME document | LLM-generated content (findings) is written to .planning/smes/ and consumed by downstream gate |
| Sub-agent .tmp files -> orchestrator | Analyzer sub-agents write findings to .tmp files that the orchestrator reads and trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | gsd-sme-creator.md -- process name in file path | mitigate | Process name must match `[a-zA-Z0-9_-]+` per Phase 1 decision (STATE.md). Agent instructions include grep/find patterns that scope to known directories (sdk/src/, agents/, get-shit-done/) -- no arbitrary path traversal. Orchestrator constructs paths using only the validated process name + fixed directory prefixes. |
| T-03-02 | Spoofing | gsd-sme-creator-analyzer.md -- fabricated file paths in findings | mitigate | Analyzer instructions require `ls <path>` verification before citing any file path in a finding. Orchestrator's synthesize step can also verify cited paths. Promptfoo eval (Plan 02) checks for hallucinated paths post-run. |
| T-03-03 | Tampering | gsd-sme-creator.md -- git command injection via file paths | accept | File paths passed to `git log --follow` come from grep/glob discovery (repo-controlled filenames from readdir), not from user input directly. Risk is minimal -- an attacker would need to create a file with shell metacharacters in its name within the repo. |
| T-03-04 | Information Disclosure | gsd-sme-creator-analyzer.md -- secrets leakage into findings | mitigate | `<forbidden_files>` block in analyzer agent explicitly lists .env, .pem, .key, credentials.*, secrets.* patterns. Agent instructed to note existence only, never read or quote contents. |
| T-03-05 | Spoofing | gsd-sme-creator.md -- BLOCKER inflation (false positives) | mitigate | Inline severity calibration examples in `<severity_examples>` block. BLOCKER definition requires historical evidence. `<critical_rules>` reinforces: "BLOCKER inflation destroys trust in the gate." |
</threat_model>

<verification>
Run all checks:

  # Agent files exist
  ls agents/gsd-sme-creator.md agents/gsd-sme-creator-analyzer.md

  # Orchestrator has Task in tools
  grep "tools:.*Task" agents/gsd-sme-creator.md

  # Analyzer does NOT have Task in tools
  grep "tools:" agents/gsd-sme-creator-analyzer.md | grep -v "Task"

  # subagent_type name match -- orchestrator references the analyzer correctly
  grep 'subagent_type="gsd-sme-creator-analyzer"' agents/gsd-sme-creator.md

  # Name fields match expected values
  grep "name: gsd-sme-creator$" agents/gsd-sme-creator.md
  grep "name: gsd-sme-creator-analyzer" agents/gsd-sme-creator-analyzer.md

  # git log --follow present in analyzer
  grep "git log --follow" agents/gsd-sme-creator-analyzer.md

  # Sequential fallback present in orchestrator
  grep -i "sequential" agents/gsd-sme-creator.md

  # Severity examples present in orchestrator
  grep "severity_examples" agents/gsd-sme-creator.md

  # Self-check present in orchestrator
  grep -i "SECTION_COUNT\|self-check" agents/gsd-sme-creator.md

  # Forbidden files in analyzer
  grep "forbidden_files" agents/gsd-sme-creator-analyzer.md
</verification>

<success_criteria>
- Both agent definition files exist in agents/
- Orchestrator's `subagent_type` exactly matches analyzer's `name:` field
- Orchestrator has Task tool; analyzer does not
- Analyzer mandates `git log --follow` for every file (CREATE-04)
- Orchestrator has parallel spawn and sequential fallback paths (CREATE-02)
- Both agents produce output matching Phase 1 SME template schema (CREATE-03)
- Orchestrator includes severity calibration with inline examples
- Analyzer includes forbidden_files safety block
- All existing tests still pass: `cd sdk && npx vitest run --project unit`
</success_criteria>

<output>
After completion, create `.planning/phases/03-sme-creator-agent/03-01-SUMMARY.md`
</output>
