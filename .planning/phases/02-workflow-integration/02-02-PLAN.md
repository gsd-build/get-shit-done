---
phase: 02-workflow-integration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - get-shit-done/workflows/execute-phase.md
autonomous: true
requirements: [FLOW-01, FLOW-02]

must_haves:
  truths:
    - "execute-phase creates worktree automatically when branching_strategy is 'phase'"
    - "execute-phase switches to existing worktree without error if one already exists"
    - "Worktree path is displayed to user with WORKTREE READY banner"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Updated handle_branching step with phase-worktree.sh integration"
      contains: "phase-worktree.sh"
  key_links:
    - from: "get-shit-done/workflows/execute-phase.md"
      to: "get-shit-done/bin/phase-worktree.sh"
      via: "handle_branching step calls phase-worktree.sh create"
      pattern: "phase-worktree\\.sh.*create"
---

<objective>
Update execute-phase.md workflow to use phase-worktree.sh for worktree creation and switching.

Purpose: The workflow currently references a non-existent script path (.planning/scripts/phase-worktree.sh). Update to use the actual Phase 1 implementation at get-shit-done/bin/phase-worktree.sh.
Output: Updated execute-phase.md with correct script path and idempotent worktree handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-workflow-integration/02-RESEARCH.md
@.planning/phases/02-workflow-integration/02-01-SUMMARY.md
@get-shit-done/workflows/execute-phase.md
@get-shit-done/bin/phase-worktree.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update handle_branching step with correct script path</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <action>
Replace the entire `<step name="handle_branching">` section with the updated version that:

1. Uses correct script path: `get-shit-done/bin/phase-worktree.sh` (not `.planning/scripts/`)
2. Locates script from both project repo and home-installed GSD
3. Uses idempotent `phase-worktree.sh create` which returns existing path or creates new
4. Handles the returned path properly (cd to worktree)
5. Falls back to simple branch checkout if script not found

Replace the handle_branching step with:

```markdown
<step name="handle_branching">
Check `branching_strategy` from init:

**"none":** Skip, continue on current branch.

**"phase" or "milestone":** Create worktree using phase-worktree.sh:

```bash
# Locate phase-worktree.sh script
# Check project repo first, then home-installed GSD
REPO_ROOT=$(git rev-parse --show-toplevel)
PHASE_WORKTREE="${REPO_ROOT}/get-shit-done/bin/phase-worktree.sh"

if [ ! -f "$PHASE_WORKTREE" ]; then
  PHASE_WORKTREE="${HOME}/.claude/get-shit-done/bin/phase-worktree.sh"
fi

if [ -f "$PHASE_WORKTREE" ]; then
  # create is idempotent: returns existing path or creates new (FLOW-01, FLOW-02)
  # Post-create hooks (npm install, .env copy) run automatically for new worktrees
  WORKTREE_PATH=$("$PHASE_WORKTREE" create "${PHASE_NUMBER}" "${PHASE_SLUG}")
  WORKTREE_EXIT=$?

  if [ $WORKTREE_EXIT -eq 0 ] && [ -n "$WORKTREE_PATH" ]; then
    echo "Switching to worktree: $WORKTREE_PATH"
    cd "$WORKTREE_PATH"
    BRANCH_NAME=$(git branch --show-current)
  else
    echo "Error: Failed to create/access worktree" >&2
    exit 1
  fi
else
  # Fallback: simple branch checkout (no worktree isolation)
  BRANCH_NAME="phase-${PHASE_NUMBER}-${PHASE_SLUG}"
  git checkout -b "$BRANCH_NAME" 2>/dev/null || git checkout "$BRANCH_NAME"
fi
```

Display to user:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD > WORKTREE READY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Working directory: {WORKTREE_PATH}
Branch: {BRANCH_NAME}

Parallel sessions can work on other phases without conflicts.
```

All subsequent commits go to this branch in the worktree. Use `/gsd:finalize-phase {N}` after completion to merge to main and cleanup.
</step>
```

Key changes from current:
- Script path: `.planning/scripts/phase-worktree.sh` -> `get-shit-done/bin/phase-worktree.sh`
- Simplified logic: `phase-worktree.sh create` is idempotent (handles existing detection internally)
- Removed separate status/path checks (create handles this)
- Added fallback branch name using Phase 1 naming convention (no gsd/ prefix)
- Added comment noting FLOW-01, FLOW-02 requirements and post-create hooks
  </action>
  <verify>
grep -q "get-shit-done/bin/phase-worktree.sh" get-shit-done/workflows/execute-phase.md && echo "Script path updated"
  </verify>
  <done>handle_branching step uses correct script path and idempotent create command</done>
</task>

<task type="auto">
  <name>Task 2: Verify fallback branch naming matches Phase 1 convention</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <action>
Verify that the fallback branch checkout (when phase-worktree.sh is not available) uses the correct branch naming convention established in Phase 1.

The current workflow may use `gsd/phase-{N}-{slug}` but Phase 1 established `phase-{N}-{slug}` (no gsd/ prefix per user decision).

Search for any remaining references to the old branch pattern and update if found:
- `gsd/phase-` should become `phase-`

Also verify the banner uses `>` instead of the Unicode arrow to avoid encoding issues (standardize on ASCII).

Check and update if needed:
```bash
grep -n "gsd/phase-" get-shit-done/workflows/execute-phase.md
grep -n "►" get-shit-done/workflows/execute-phase.md
```

The fallback should construct: `BRANCH_NAME="phase-${PHASE_NUMBER}-${PHASE_SLUG}"`
  </action>
  <verify>
! grep -q "gsd/phase-" get-shit-done/workflows/execute-phase.md && echo "No old branch pattern found"
  </verify>
  <done>Branch naming uses phase-{N}-{slug} pattern (no gsd/ prefix) throughout</done>
</task>

</tasks>

<verification>
1. `grep "get-shit-done/bin/phase-worktree.sh" get-shit-done/workflows/execute-phase.md` shows script path
2. `grep "FLOW-01\|FLOW-02" get-shit-done/workflows/execute-phase.md` shows requirement comments
3. No references to `.planning/scripts/phase-worktree.sh` remain
4. No references to `gsd/phase-` branch prefix remain
</verification>

<success_criteria>
1. execute-phase.md references correct script path (get-shit-done/bin/phase-worktree.sh)
2. handle_branching uses idempotent `phase-worktree.sh create` command
3. Fallback branch naming uses `phase-{N}-{slug}` (no gsd/ prefix)
4. WORKTREE READY banner displays after successful worktree setup
5. Script checks both project repo and home-installed paths
</success_criteria>

<output>
After completion, create `.planning/phases/02-workflow-integration/02-02-SUMMARY.md`
</output>
