---
phase: 07-discuss-phase-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/sme-discuss-phase.test.cjs
  - get-shit-done/workflows/discuss-phase/sme-step.md
  - get-shit-done/workflows/discuss-phase.md
  - get-shit-done/workflows/discuss-phase/templates/context.md
autonomous: true
requirements:
  - DISCUSS-01
  - DISCUSS-02
  - DISCUSS-03

must_haves:
  truths:
    - "When use_sme_agents is true and milestone.active_smes is populated in STATE.md, discuss-phase reads sme-step.md and executes the SME check"
    - "The SME check reads active_smes via frontmatter.get (not state.json), fetches context blocks via sme.context-block, and spawns gsd-sme-auditor in probing-questions mode"
    - "SME probing questions are captured as sme_risk_areas and injected as pre-loaded domain risk areas before gray-area analysis"
    - "CONTEXT.md includes an <sme_context> block containing the surfaced domain risks when sme_risk_areas is non-empty"
    - "When use_sme_agents is false or active_smes is empty, the SME step is silently skipped -- never blocks"
  artifacts:
    - path: "tests/sme-discuss-phase.test.cjs"
      provides: "Structural validation tests for DISCUSS-01, DISCUSS-02, DISCUSS-03"
      contains: "describe.*DISCUSS-01"
    - path: "get-shit-done/workflows/discuss-phase/sme-step.md"
      provides: "Full SME check logic as lazy-loaded sub-workflow step"
      contains: "frontmatter.get"
    - path: "get-shit-done/workflows/discuss-phase.md"
      provides: "Dispatch reference to sme-step.md in progressive_disclosure table"
      contains: "sme-step.md"
    - path: "get-shit-done/workflows/discuss-phase/templates/context.md"
      provides: "Conditional <sme_context> section in CONTEXT.md template"
      contains: "sme_context"
  key_links:
    - from: "get-shit-done/workflows/discuss-phase.md"
      to: "get-shit-done/workflows/discuss-phase/sme-step.md"
      via: "progressive_disclosure table row + Read() dispatch after cross_reference_todos"
      pattern: "sme-step\\.md"
    - from: "get-shit-done/workflows/discuss-phase/sme-step.md"
      to: "SDK query handlers (frontmatter.get, sme.context-block, config-get)"
      via: "gsd-sdk query calls"
      pattern: "frontmatter\\.get.*STATE\\.md"
    - from: "get-shit-done/workflows/discuss-phase/sme-step.md"
      to: "agents/gsd-sme-auditor.md"
      via: "Task() spawn with probing-questions prompt"
      pattern: "gsd-sme-auditor"
    - from: "get-shit-done/workflows/discuss-phase/sme-step.md"
      to: "get-shit-done/workflows/discuss-phase/templates/context.md"
      via: "sme_risk_areas variable consumed by write_context step"
      pattern: "sme_risk_areas"
---

<objective>
TDD implementation of SME domain knowledge injection into discuss-phase. When active SMEs exist,
the discuss-phase spawns the auditor in probing-questions mode to surface domain-specific risks
before planning begins. Questions are written to CONTEXT.md under an <sme_context> block so the
planner sees which domain risks were surfaced.

Purpose: Surface domain-specific risks BEFORE the user locks planning decisions, closing the loop
between SME knowledge capture (Phases 1-3) and pre-planning risk awareness.

Output: Structural tests (RED then GREEN), sme-step.md sub-workflow, discuss-phase.md dispatch
reference, context.md template update with <sme_context> conditional section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-discuss-phase-integration/07-RESEARCH.md
@.planning/phases/07-discuss-phase-integration/07-PATTERNS.md
@.planning/phases/06-plan-phase-gate/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From tests/sme-gate-plan-phase.test.cjs (Phase 6 analog -- exact CJS test pattern):
```javascript
'use strict';
const { describe, test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');
const ROOT = path.join(__dirname, '..');
// File paths constructed via path.join(ROOT, ...)
// Tests use: describe('REQ-ID: description', () => { test('assertion name', () => { ... }); });
// Assertions use: assert.ok(condition, 'failure message');
```

From get-shit-done/workflows/discuss-phase.md (progressive_disclosure table, lines 18-31):
```markdown
| When | Read |
|---|---|
| `--power` in $ARGUMENTS | `workflows/discuss-phase/modes/power.md` (then exit standard flow) |
...
| no flags above | `workflows/discuss-phase/modes/default.md` |
| in `write_context` step | `workflows/discuss-phase/templates/context.md` |
| in `git_commit` step | `workflows/discuss-phase/templates/discussion-log.md` |
| writing checkpoints | `workflows/discuss-phase/templates/checkpoint.json` |
```

From get-shit-done/workflows/discuss-phase.md (step ordering, lines 264-289):
```
<step name="cross_reference_todos">  (line 264)
...
</step>                               (line 278)

<step name="scout_codebase">          (line 280)
```

The SME step dispatch goes AFTER cross_reference_todos (line 278) and BEFORE scout_codebase (line 280).

From get-shit-done/workflows/discuss-phase/templates/context.md (conditional sections, lines 19-24):
```markdown
## Conditional sections

- **`<spec_lock>`** -- include only when `spec_loaded = true` (a `*-SPEC.md`
  was found by `check_spec`). Otherwise omit the entire `<spec_lock>` block.
- **Folded Todos / Reviewed Todos** -- include subsections only when the
  `cross_reference_todos` step folded or reviewed at least one todo.
```

From get-shit-done/workflows/discuss-phase/templates/context.md (template body conditional block pattern, lines 40-51):
```markdown
[If spec_loaded = true, insert this section:]
<spec_lock>
## Requirements (locked via SPEC.md)
...
</spec_lock>
```

Budget constraint: discuss-phase.md is 497 lines. Test enforces `lines < 500` (strictly less than).
Maximum 2 new inline lines allowed. All SME logic goes in sme-step.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write structural tests for SME discuss-phase integration</name>
  <files>tests/sme-discuss-phase.test.cjs</files>
  <read_first>
    - tests/sme-gate-plan-phase.test.cjs (exact analog -- copy this CJS test pattern)
    - tests/workflow-size-budget.test.cjs (verify budget constraint details: DISCUSS_PHASE_TARGET = 500, line 99)
    - .planning/phases/07-discuss-phase-integration/07-RESEARCH.md (requirement test map, lines 440-465)
    - .planning/phases/07-discuss-phase-integration/07-PATTERNS.md (test pattern excerpts, lines 177-251)
  </read_first>
  <behavior>
    DISCUSS-01 tests (3 tests):
    - Test 1: sme-step.md contains both "frontmatter.get" and "STATE.md" (reads active_smes via frontmatter.get)
    - Test 2: sme-step.md contains "use_sme_agents" (config flag guard)
    - Test 3: discuss-phase.md contains "sme-step.md" (lazy-load dispatch reference exists)

    DISCUSS-02 tests (3 tests):
    - Test 4: sme-step.md contains "sme.context-block" (fetches context blocks per active SME)
    - Test 5: sme-step.md contains "gsd-sme-auditor" (spawns auditor for probing questions)
    - Test 6: sme-step.md does NOT contain "SME_APPROVED" AND does NOT contain "SME_CONCERNS" (probing mode only -- no plan-audit markers)

    DISCUSS-03 tests (2 tests):
    - Test 7: context.md template contains "sme_context" (conditional section exists in template)
    - Test 8: sme-step.md or discuss-phase.md contains "sme_risk_areas" (output variable for write_context consumption)

    Positioning test (1 test):
    - Test 9: In discuss-phase.md, indexOf("cross_reference_todos") < indexOf("sme-step.md") (SME step reference after cross_reference_todos)
  </behavior>
  <action>
Create `tests/sme-discuss-phase.test.cjs` following the exact CJS pattern from `tests/sme-gate-plan-phase.test.cjs`.

File header comment block:
```
/**
 * Structural validation tests for Phase 7 SME Discuss-Phase Integration.
 *
 * Tests verify static structure of discuss-phase.md, sme-step.md, and context.md template --
 * not runtime behavior. All tests read files from the repo root using absolute paths.
 *
 * Requirements covered:
 *   DISCUSS-01 -- sme-step.md uses frontmatter.get to read STATE.md for active_smes
 *   DISCUSS-02 -- sme-step.md calls sme.context-block per active SME and spawns gsd-sme-auditor
 *   DISCUSS-03 -- context.md template includes <sme_context> section
 */
```

Imports (same as analog):
```javascript
'use strict';
const { describe, test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');
```

Constants (3 file paths):
```javascript
const ROOT = path.join(__dirname, '..');
const DISCUSS_PHASE = path.join(ROOT, 'get-shit-done', 'workflows', 'discuss-phase.md');
const SME_STEP = path.join(ROOT, 'get-shit-done', 'workflows', 'discuss-phase', 'sme-step.md');
const CONTEXT_TPL = path.join(ROOT, 'get-shit-done', 'workflows', 'discuss-phase', 'templates', 'context.md');
```

9 tests across 4 describe blocks:

1. `describe('DISCUSS-01: discuss-phase checks for active_smes in STATE.md', () => {...})`
   - 3 tests: frontmatter.get + STATE.md, use_sme_agents, sme-step.md dispatch

2. `describe('DISCUSS-02: auditor generates probing questions from SME context', () => {...})`
   - 3 tests: sme.context-block, gsd-sme-auditor, no SME_APPROVED/SME_CONCERNS markers

3. `describe('DISCUSS-03: sme_context block appended to CONTEXT.md', () => {...})`
   - 2 tests: template sme_context section, sme_risk_areas variable reference

4. `describe('Positioning: SME step reference after cross_reference_todos', () => {...})`
   - 1 test: indexOf ordering check (same pattern as GATE-01 positioning test)

All tests use `assert.ok(condition, 'descriptive failure message')`.
The anti-pattern test (no SME_APPROVED/SME_CONCERNS) uses `assert.ok(!content.includes(...))`.

After creating the file, run `node --test tests/sme-discuss-phase.test.cjs`. All 9 tests MUST fail (RED) because sme-step.md does not exist yet and discuss-phase.md does not reference it yet.

Commit message: `test(07-01): add structural tests for SME discuss-phase integration (RED)`
  </action>
  <verify>
    <automated>node --test tests/sme-discuss-phase.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/sme-discuss-phase.test.cjs exists and contains exactly 4 describe blocks
    - File contains `require('node:test')` and `require('node:assert/strict')` (CJS pattern, not Vitest)
    - File contains `const SME_STEP = path.join(ROOT, 'get-shit-done', 'workflows', 'discuss-phase', 'sme-step.md')`
    - File contains `describe('DISCUSS-01` and `describe('DISCUSS-02` and `describe('DISCUSS-03`
    - File contains `!content.includes('SME_APPROVED')` (anti-pattern assertion)
    - File contains `indexOf('cross_reference_todos')` (positioning test)
    - `node --test tests/sme-discuss-phase.test.cjs` runs and all 9 tests FAIL (exit code non-zero)
    - Commit exists with message containing "RED"
  </acceptance_criteria>
  <done>9 structural tests created and all failing RED. Commit made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement SME discuss-phase integration to pass all tests</name>
  <files>
    get-shit-done/workflows/discuss-phase/sme-step.md,
    get-shit-done/workflows/discuss-phase.md,
    get-shit-done/workflows/discuss-phase/templates/context.md
  </files>
  <read_first>
    - tests/sme-discuss-phase.test.cjs (the RED tests that must pass)
    - get-shit-done/workflows/discuss-phase.md (current 497-line file -- MUST stay under 500 after edit)
    - get-shit-done/workflows/discuss-phase/templates/context.md (current template -- understand conditional section pattern)
    - .planning/phases/07-discuss-phase-integration/07-PATTERNS.md (all 4 pattern assignments with exact code excerpts)
    - .planning/phases/07-discuss-phase-integration/07-RESEARCH.md (architecture diagram lines 70-103, code examples lines 280-377, anti-patterns lines 201-209)
    - get-shit-done/workflows/plan-phase.md lines 1278-1420 (Phase 6 step 12.6 analog -- the closest precedent for this work)
  </read_first>
  <behavior>
    - Test 1 passes: sme-step.md contains "frontmatter.get" and "STATE.md"
    - Test 2 passes: sme-step.md contains "use_sme_agents"
    - Test 3 passes: discuss-phase.md contains "sme-step.md"
    - Test 4 passes: sme-step.md contains "sme.context-block"
    - Test 5 passes: sme-step.md contains "gsd-sme-auditor"
    - Test 6 passes: sme-step.md does NOT contain "SME_APPROVED" or "SME_CONCERNS"
    - Test 7 passes: context.md template contains "sme_context"
    - Test 8 passes: sme-step.md or discuss-phase.md contains "sme_risk_areas"
    - Test 9 passes: "cross_reference_todos" appears before "sme-step.md" in discuss-phase.md
  </behavior>
  <action>
**File 1: Create `get-shit-done/workflows/discuss-phase/sme-step.md`**

This is the core deliverable. Follow the architecture from RESEARCH.md lines 70-103 and the code
examples from PATTERNS.md. The file structure:

Header (lazy-load gate comment, same style as `discuss-phase/modes/advisor.md` lines 1-7):
```markdown
# SME Domain Probing — lazy-loaded by discuss-phase

> **Lazy-loaded and gated.** `workflows/discuss-phase.md` Reads this file ONLY
> when `use_sme_agents: true` AND `milestone.active_smes` is non-empty in STATE.md.
> Skip the Read entirely when either condition is false.
```

Step 1 -- Config guard (copy from plan-phase.md line 1283 pattern):
```markdown
## 1. Check feature flag

```bash
SME_AGENTS=$(gsd-sdk query config-get workflow.use_sme_agents --raw 2>/dev/null || echo "false")
```

**If `SME_AGENTS` is not `true`:** Skip this entire step. Return to discuss-phase.md.
```

Step 2 -- Read active_smes from STATE.md frontmatter (CRITICAL: use `frontmatter.get`, NOT `state.json`):
```markdown
## 2. Read active SMEs from STATE.md

```bash
STATE_FM=$(gsd-sdk query frontmatter.get .planning/STATE.md 2>/dev/null || echo '{"data":{}}')
ACTIVE_SMES=$(echo "$STATE_FM" | python3 -c "
import json, sys
d = json.load(sys.stdin)
data = d.get('data', d)
smes = data.get('milestone', {}).get('active_smes', [])
print('\n'.join(smes) if isinstance(smes, list) else '')
" 2>/dev/null || echo "")
```

**If `ACTIVE_SMES` is empty:** Skip silently. Phase 8 has not yet run, or no SMEs were queued for this milestone. Return to discuss-phase.md.
```

Step 3 -- Fetch context blocks per active SME:
```markdown
## 3. Fetch SME context blocks

```bash
SME_CONTEXT_BLOCKS=""
for PROCESS_NAME in $ACTIVE_SMES; do
  CTX=$(gsd-sdk query sme.context-block "${PROCESS_NAME}" 2>/dev/null || echo '{"data":{"found":false}}')
  FOUND=$(echo "$CTX" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('data',{}).get('found', False))" 2>/dev/null || echo "False")
  if [ "$FOUND" = "True" ]; then
    BLOCK=$(echo "$CTX" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('data',{}).get('block',''))" 2>/dev/null || echo "")
    SME_CONTEXT_BLOCKS="${SME_CONTEXT_BLOCKS}${BLOCK}"
  fi
done
```

**If `SME_CONTEXT_BLOCKS` is empty:** All active SMEs had missing documents. Log a note: `[sme-step] No SME documents found for active processes — skipping probing questions.` Return to discuss-phase.md. Never block.
```

Step 4 -- Spawn auditor in probing-questions mode (CRITICAL: no PLAN.md path, no SME_APPROVED/SME_CONCERNS routing):
```markdown
## 4. Generate probing questions

Spawn the auditor with a probing-questions prompt. The auditor's role here is DIFFERENT from a plan audit.

```
Task(
  prompt="""${SME_CONTEXT_BLOCKS}

Phase goal: ${PHASE_GOAL}

Your role here is DIFFERENT from a plan audit: there is no PLAN.md to review.
Instead, produce 3-5 domain-specific probing questions that surface the highest-severity
risks from the SME findings above. The user will answer these during the discussion.

Focus on BLOCKERs first, then WARNINGs. Format as a numbered list. No ## markers.
""",
  subagent_type="gsd-sme-auditor",
  description="SME probing questions Phase ${PHASE}"
)
```

Capture the numbered question list as `sme_risk_areas`. Do NOT route on `## SME_APPROVED` or `## SME_CONCERNS` markers -- this is probing-questions mode, not plan-audit mode.
```

Step 5 -- Store for downstream consumption:
```markdown
## 5. Inject into discussion

Store `sme_risk_areas` as pre-loaded domain risk areas for injection into the `present_gray_areas` step. These appear as additional risk areas the user should address during discussion.

When `write_context` runs (later in discuss-phase.md), if `sme_risk_areas` is non-empty, include the `<sme_context>` block in CONTEXT.md per the template's conditional section documentation.
```

CRITICAL CONSTRAINTS for sme-step.md:
- The string "SME_APPROVED" must NOT appear anywhere in this file
- The string "SME_CONCERNS" must NOT appear anywhere in this file
- The string "frontmatter.get" MUST appear (for reading STATE.md)
- The string "STATE.md" MUST appear
- The string "use_sme_agents" MUST appear
- The string "sme.context-block" MUST appear
- The string "gsd-sme-auditor" MUST appear
- The string "sme_risk_areas" MUST appear

**File 2: Modify `get-shit-done/workflows/discuss-phase.md`**

Two changes, adding exactly 2 lines total (keeping the file at 499 lines, under the 500 budget):

Change A -- Add one row to the progressive_disclosure table (insert between the `| no flags above |` row at line 28 and the `| in write_context step |` row at line 29):
```markdown
| `use_sme_agents: true` AND `active_smes` non-empty | `workflows/discuss-phase/sme-step.md` |
```

Change B -- Add one dispatch line between `</step>` at line 278 (end of cross_reference_todos) and `<step name="scout_codebase">` at line 280. Insert at line 279 (the current blank line):
```markdown
If `use_sme_agents` is true and `milestone.active_smes` is non-empty in STATE.md frontmatter: Read `workflows/discuss-phase/sme-step.md` and execute its steps. Store the returned `sme_risk_areas` for `present_gray_areas` and `write_context`.
```

After editing, verify `wc -l` shows 499 (497 original + 2 added lines). If 500 or more, the budget test will fail.

**File 3: Modify `get-shit-done/workflows/discuss-phase/templates/context.md`**

Two additions:

Addition A -- Add one bullet to the `## Conditional sections` block (after the "Folded Todos / Reviewed Todos" bullet at line 23, before the blank line at line 24):
```markdown
- **`<sme_context>`** -- include only when `sme_risk_areas` is non-empty (SME
  check ran and produced probing questions). Otherwise omit entirely.
```

Addition B -- Add the `<sme_context>` conditional block to the template body. Insert after the `</deferred>` closing tag (line 130) and before the `---` separator (line 132):
```markdown

[If sme_risk_areas exist (SME check ran and produced questions):]
<sme_context>
## SME Domain Risks Surfaced

Domain-specific risks from active SME documents, surfaced during discussion.
The planner must address these risks in the plan.

{numbered list of probing questions/risk areas surfaced by the auditor}

**Active SMEs:** {list of process names}
</sme_context>
```

After all three files are created/modified, run:
1. `node --test tests/sme-discuss-phase.test.cjs` -- all 9 tests MUST pass (GREEN)
2. `node --test tests/workflow-size-budget.test.cjs` -- discuss-phase.md budget test MUST pass (< 500 lines)
3. `node --test tests/sme-gate-plan-phase.test.cjs` -- Phase 6 tests MUST still pass (no regression)

Commit message: `feat(07-01): add SME domain probing to discuss-phase with sme-step.md and context template`
  </action>
  <verify>
    <automated>node --test tests/sme-discuss-phase.test.cjs && node --test tests/workflow-size-budget.test.cjs && node --test tests/sme-gate-plan-phase.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/workflows/discuss-phase/sme-step.md exists
    - sme-step.md contains "frontmatter.get" and "STATE.md" (DISCUSS-01)
    - sme-step.md contains "use_sme_agents" (DISCUSS-01)
    - sme-step.md contains "sme.context-block" (DISCUSS-02)
    - sme-step.md contains "gsd-sme-auditor" (DISCUSS-02)
    - sme-step.md does NOT contain "SME_APPROVED" (probing mode, not plan-audit)
    - sme-step.md does NOT contain "SME_CONCERNS" (probing mode, not plan-audit)
    - sme-step.md contains "sme_risk_areas" (output variable)
    - discuss-phase.md contains "sme-step.md" (dispatch reference)
    - discuss-phase.md line count is less than 500 (budget test: `wc -l` output < 500)
    - In discuss-phase.md, "cross_reference_todos" appears before "sme-step.md" (positioning)
    - context.md template contains "sme_context" (conditional section)
    - `node --test tests/sme-discuss-phase.test.cjs` exits 0 (all 9 tests pass)
    - `node --test tests/workflow-size-budget.test.cjs` exits 0 (budget not exceeded)
    - `node --test tests/sme-gate-plan-phase.test.cjs` exits 0 (no Phase 6 regression)
    - Commit exists with message containing "feat(07-01)"
  </acceptance_criteria>
  <done>All 9 structural tests pass GREEN. discuss-phase.md stays under 500-line budget. sme-step.md implements the full SME check flow. context.md template includes <sme_context> conditional block. No regressions in Phase 6 tests. Commit made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STATE.md frontmatter -> sme-step.md | `active_smes` values read from YAML frontmatter and used as arguments to `sme.context-block` |
| SME document content -> auditor prompt | SME document body injected into auditor prompt via context blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `active_smes` values used as filesystem paths | accept | `sme.context-block` handler constructs the path internally from `planningPaths(projectDir) + '/smes/' + name + '-SME.md'` -- user-supplied names are not raw-concatenated; handler validates file exists and returns `found: false` for unknown names (VERIFIED: sdk/src/query/sme.ts). Same trust boundary as Phase 6. |
| T-07-02 | Tampering | STATE.md path traversal via `frontmatter.get` | accept | `frontmatterGet` rejects null bytes (line 364) and uses `resolvePathUnderProject` which restricts paths to project directory (VERIFIED: sdk/src/query/frontmatter.ts). Same mitigation as all other `frontmatter.get` callers. |
| T-07-03 | Information Disclosure | SME content injected into auditor prompt | accept | SME documents are author-created project files at same trust level as PLAN.md files read by other handlers. Same trust model as Phase 6 gate. No PII, low-value target. |
| T-07-04 | Denial of Service | Auditor fails or hangs in probing-questions mode | accept | Workflow continues if auditor returns empty or errors -- `sme_risk_areas` will be empty and `<sme_context>` block is omitted. Discuss-phase is never blocked by SME step failure. |
</threat_model>

<verification>
1. `node --test tests/sme-discuss-phase.test.cjs` -- all 9 tests pass
2. `node --test tests/workflow-size-budget.test.cjs` -- discuss-phase.md budget test passes (< 500 lines)
3. `node --test tests/sme-gate-plan-phase.test.cjs` -- Phase 6 tests still pass (no regression)
4. `wc -l get-shit-done/workflows/discuss-phase.md` shows < 500
5. `grep -c 'sme_context' get-shit-done/workflows/discuss-phase/templates/context.md` returns >= 2
6. `grep 'SME_APPROVED\|SME_CONCERNS' get-shit-done/workflows/discuss-phase/sme-step.md` returns empty (exit 1)
</verification>

<success_criteria>
- DISCUSS-01: sme-step.md checks `use_sme_agents` config flag and reads `milestone.active_smes` from STATE.md via `frontmatter.get`
- DISCUSS-02: sme-step.md fetches context blocks via `sme.context-block` and spawns `gsd-sme-auditor` in probing-questions mode (no plan-audit markers)
- DISCUSS-03: context.md template includes conditional `<sme_context>` block; `sme_risk_areas` variable connects sme-step.md output to write_context step
- discuss-phase.md stays under 500-line budget (currently 497, adding 2 lines = 499)
- All structural tests pass; no regressions in Phase 6 or budget tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-discuss-phase-integration/07-01-SUMMARY.md`
</output>
