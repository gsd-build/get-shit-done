---
phase: 05-refinement-of-docs-output
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - get-shit-done/workflows/docs-update.md
autonomous: true
requirements:
  - REFINE-01
  - REFINE-02
  - REFINE-03

must_haves:
  truths:
    - "Generated docs (except README.md and CONTRIBUTING.md) are written to docs/ directory by default"
    - "Existing non-canonical docs found by the recursive scan are queued for verification only (not rewriting)"
    - "New CONTRIBUTING.md creation requires user confirmation unless --force is passed"
    - "All path references throughout the workflow use docs/ prefix for non-root doc types"
  artifacts:
    - path: "get-shit-done/workflows/docs-update.md"
      provides: "Inverted path table, existing doc review queue, CONTRIBUTING confirmation gate"
      contains: "docs/ARCHITECTURE.md"
  key_links:
    - from: "get-shit-done/workflows/docs-update.md resolve_modes"
      to: "get-shit-done/workflows/docs-update.md dispatch_wave_1"
      via: "resolved paths flow from table to agent spawn commands"
      pattern: "docs/ARCHITECTURE\\.md"
    - from: "get-shit-done/workflows/docs-update.md build_doc_queue"
      to: "get-shit-done/workflows/docs-update.md verify_docs"
      via: "review_queue of non-canonical docs passed to verification step"
      pattern: "review_queue"
    - from: "get-shit-done/workflows/docs-update.md build_doc_queue"
      to: "user confirmation"
      via: "CONTRIBUTING.md creation prompt"
      pattern: "CONTRIBUTING.md does not exist"
---

<objective>
Update the docs-update workflow to fix doc output paths (docs/ by default), add existing doc review queue, and add CONTRIBUTING.md confirmation gate.

Purpose: The workflow currently writes docs to project root instead of docs/, ignores non-canonical existing docs during verification, and auto-creates CONTRIBUTING.md without asking. These three issues were discovered during smoke testing on a real project.

Output: Updated docs-update.md workflow with all three smoke test issues resolved.
</objective>

<execution_context>
@C:/Users/lukaf/Desktop/Dev Work/Contributions/get-shit-done/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukaf/Desktop/Dev Work/Contributions/get-shit-done/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-refinement-of-docs-output/05-CONTEXT.md
@.planning/phases/05-refinement-of-docs-output/05-RESEARCH.md
@.planning/phases/05-refinement-of-docs-output/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert path table and update all path references in docs-update.md</name>
  <files>get-shit-done/workflows/docs-update.md</files>
  <read_first>
    - get-shit-done/workflows/docs-update.md (full file -- understand all 13 steps, find every occurrence of root-level doc paths like ARCHITECTURE.md, CONFIGURATION.md, etc. that need docs/ prefix)
    - .planning/phases/05-refinement-of-docs-output/05-RESEARCH.md (the "Issue 1: Path Table Inversion" section has the exact target state for the table and lists all downstream impacts)
  </read_first>
  <action>
This task makes extensive changes throughout the 927-line workflow file. Every path reference for non-root doc types must be updated. The changes are grouped by step:

**Step: resolve_modes (lines ~130-162)**

1. Replace the path mapping table with this inverted version:

| Type | Primary Path | Fallback Path |
|------|-------------|---------------|
| `readme` | `README.md` | -- |
| `architecture` | `docs/ARCHITECTURE.md` | `ARCHITECTURE.md` |
| `getting_started` | `docs/GETTING-STARTED.md` | `GETTING-STARTED.md` |
| `development` | `docs/DEVELOPMENT.md` | `DEVELOPMENT.md` |
| `testing` | `docs/TESTING.md` | `TESTING.md` |
| `api` | `docs/API.md` | `API.md` |
| `configuration` | `docs/CONFIGURATION.md` | `CONFIGURATION.md` |
| `deployment` | `docs/DEPLOYMENT.md` | `DEPLOYMENT.md` |
| `contributing` | `CONTRIBUTING.md` | -- |

2. After the mode resolution logic, add a `docs/` directory creation instruction:

```
**Ensure docs/ directory exists:**
Before proceeding to the next step, create the `docs/` directory if it does not exist:
```bash
mkdir -p docs/
```
```

3. Update the mode resolution example output to show docs/ paths:
```
Mode resolution:
  README.md               -- create (not found)
  docs/ARCHITECTURE.md    -- update (found at docs/ARCHITECTURE.md)
  docs/GETTING-STARTED.md -- create (not found)
  ...
```

**Step: build_doc_queue (lines ~83-124)**

4. Update the "Present the assembled queue" example to show docs/ paths:
```
Always-on:
  - README.md
  - docs/ARCHITECTURE.md
  - docs/GETTING-STARTED.md
  - docs/DEVELOPMENT.md
  - docs/TESTING.md
  - docs/CONFIGURATION.md
```

**Step: dispatch_wave_1 (lines ~220-330)**

5. In the ARCHITECTURE agent Task block, update the description from "Generate ARCHITECTURE.md" to reflect docs/ path. The agent writes to the resolved path from resolve_modes -- no hardcoded path change needed in the Task call itself since the path comes from resolve_modes.

6. Update the `collect_wave_1` verification command (line ~325-326) from:
```bash
ls -la README.md ARCHITECTURE.md CONFIGURATION.md 2>/dev/null || \
  ls -la docs/ARCHITECTURE.md docs/CONFIGURATION.md 2>/dev/null
```
to:
```bash
ls -la README.md docs/ARCHITECTURE.md docs/CONFIGURATION.md 2>/dev/null
```

**Step: dispatch_wave_2 (lines ~338-530)**

7. In wave_1_outputs references (lines ~360-361, 386-387, 412-413, 438-439, 465-466, 491-492), update from:
```
- ARCHITECTURE.md
- CONFIGURATION.md
```
to:
```
- docs/ARCHITECTURE.md
- docs/CONFIGURATION.md
```

**Step: collect_wave_2 (lines ~530-534)**

8. Update the verification ls command (line ~532-534) from:
```bash
ls -la GETTING-STARTED.md DEVELOPMENT.md TESTING.md 2>/dev/null
# ls -la API.md DEPLOYMENT.md CONTRIBUTING.md 2>/dev/null
```
to:
```bash
ls -la docs/GETTING-STARTED.md docs/DEVELOPMENT.md docs/TESTING.md 2>/dev/null
# ls -la docs/API.md docs/DEPLOYMENT.md CONTRIBUTING.md 2>/dev/null
```

**Step: sequential_generation (lines ~598-641)**

9. Update ARCHITECTURE resolved path reference (line ~611) from:
```
- Write the file to the resolved path (ARCHITECTURE.md or docs/ARCHITECTURE.md)
```
to:
```
- Write the file to the resolved path (docs/ARCHITECTURE.md, or ARCHITECTURE.md if found at root as fallback)
```

10. Update CONFIGURATION resolved path reference (line ~617) similarly.

11. Update wave_1_outputs reference (line ~623) from `[README.md, ARCHITECTURE.md, CONFIGURATION.md]` to `[README.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md]`.

**Step: commit_docs (lines ~836-853)**

12. Update the commit command file list (line ~843) from:
```bash
--files README.md ARCHITECTURE.md CONFIGURATION.md GETTING-STARTED.md DEVELOPMENT.md TESTING.md
```
to:
```bash
--files README.md docs/ARCHITECTURE.md docs/CONFIGURATION.md docs/GETTING-STARTED.md docs/DEVELOPMENT.md docs/TESTING.md
```

13. Update the conditional files comment (line ~845) from:
```
# --files ... API.md DEPLOYMENT.md CONTRIBUTING.md
```
to:
```
# --files ... docs/API.md docs/DEPLOYMENT.md CONTRIBUTING.md
```

**Step: report (lines ~855+)**

14. Update the example report table entries to use docs/ paths. Find lines like `| ARCHITECTURE.md |` and update to `| docs/ARCHITECTURE.md |`. Same for CONFIGURATION.md, GETTING-STARTED.md, DEVELOPMENT.md, TESTING.md, API.md, DEPLOYMENT.md. Leave README.md and CONTRIBUTING.md at root.

**Step: verification checklist (lines ~910+)**

15. Update any verification checklist items that reference root-level paths.

**CRITICAL: After all edits, search the entire file for bare occurrences of these strings NOT preceded by `docs/` and NOT in the readme or contributing rows: `ARCHITECTURE.md`, `GETTING-STARTED.md`, `DEVELOPMENT.md`, `TESTING.md`, `API.md`, `CONFIGURATION.md`, `DEPLOYMENT.md`. Every remaining bare occurrence (except in the fallback path column of the table) is a bug.**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('get-shit-done/workflows/docs-update.md','utf-8');const table=c.match(/Primary Path.*Fallback Path[\s\S]*?\| \`contributing\`[^\n]*/);if(!table)throw new Error('Path table not found');if(!table[0].includes('docs/ARCHITECTURE.md'))throw new Error('ARCHITECTURE not in docs/');if(!table[0].includes('docs/CONFIGURATION.md'))throw new Error('CONFIGURATION not in docs/');const lines=c.split('\n');let bareCount=0;const bareRx=/(?<!docs\/)(?<!Fallback Path[^\n]*)(?<!\`)(ARCHITECTURE|GETTING-STARTED|DEVELOPMENT|TESTING|CONFIGURATION|DEPLOYMENT|API)\.md/g;for(const line of lines){if(line.includes('Fallback Path'))continue;if(line.includes('| `readme`')||line.includes('| `contributing`'))continue;const m=line.match(bareRx);if(m)bareCount+=m.length}if(bareCount>0)console.log('WARNING: '+bareCount+' bare root-level path refs remain');console.log('PASS: path table inverted')"</automated>
  </verify>
  <acceptance_criteria>
    - resolve_modes table row for `architecture` has Primary Path = `docs/ARCHITECTURE.md` and Fallback Path = `ARCHITECTURE.md`
    - resolve_modes table row for `readme` has Primary Path = `README.md` (unchanged)
    - resolve_modes table row for `contributing` has Primary Path = `CONTRIBUTING.md` (unchanged)
    - resolve_modes step contains `mkdir -p docs/` instruction
    - collect_wave_1 ls command references `docs/ARCHITECTURE.md` and `docs/CONFIGURATION.md`
    - collect_wave_2 ls command references `docs/GETTING-STARTED.md` and `docs/DEVELOPMENT.md`
    - commit_docs --files list starts with `README.md docs/ARCHITECTURE.md docs/CONFIGURATION.md`
    - wave_1_outputs arrays reference `docs/ARCHITECTURE.md` and `docs/CONFIGURATION.md`
    - sequential_generation wave_1_outputs reference `docs/ARCHITECTURE.md`
  </acceptance_criteria>
  <done>All doc paths in the workflow default to docs/ directory, with README.md and CONTRIBUTING.md as the only root-level exceptions. The docs/ directory is created before agent dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Add CONTRIBUTING.md confirmation gate and existing doc review queue</name>
  <files>get-shit-done/workflows/docs-update.md</files>
  <read_first>
    - get-shit-done/workflows/docs-update.md (re-read after Task 1 modifications -- specifically build_doc_queue step and verify_docs step)
    - .planning/phases/05-refinement-of-docs-output/05-RESEARCH.md (Issue 2 and Issue 3 sections, plus Pitfall 3 and Pitfall 4)
  </read_first>
  <action>
Two changes to the workflow, both in existing steps:

**Change A: CONTRIBUTING.md confirmation gate in build_doc_queue step**

In the `build_doc_queue` step, after the line "**Conditional docs (add only if signal matched in classify_project):**" and its list, and BEFORE the "Present the assembled queue" block, add this new section:

```markdown
**CONTRIBUTING.md confirmation (new file only):**

If CONTRIBUTING.md is in the conditional queue AND does NOT appear in the `existing_docs` array from init JSON:

1. If `--force` is present in `$ARGUMENTS`: skip this check, include CONTRIBUTING.md in the queue.
2. Otherwise, ask the user before including it:

```
This project appears to be open source (LICENSE file detected).
CONTRIBUTING.md does not exist yet. Would you like to create one? (y/n)
```

If the user answers "n" or "no": remove CONTRIBUTING.md from the doc queue.
If CONTRIBUTING.md already exists in `existing_docs`: skip this prompt entirely, include it for update.
```

**Change B: Existing non-canonical doc review queue in build_doc_queue step**

After the CONTRIBUTING confirmation gate section and before the "Present the assembled queue" block, add this new section:

```markdown
**Existing non-canonical docs (verification queue):**

After assembling the canonical doc queue above, scan the `existing_docs` array from init JSON for files that do NOT match any canonical path in the queue (neither primary nor fallback path from the resolve_modes table). These are hand-written docs like `docs/api/endpoint-map.md` or `docs/frontend/pages/not-found.md`.

For each non-canonical existing doc found:
- Add to a separate `review_queue` (NOT the generation queue)
- These will be passed to gsd-doc-verifier in the verify_docs step for accuracy checking
- They will NOT be dispatched to gsd-doc-writer -- the writer has no template for arbitrary docs

If non-canonical docs are found, display them in the queue presentation:

```
Existing non-canonical docs queued for accuracy review:
  - docs/api/endpoint-map.md (hand-written)
  - docs/api/README.md (hand-written)
  - docs/frontend/pages/not-found.md (hand-written)
```

If none found, omit this section from the queue presentation.
```

**Change C: Pass review_queue to verify_docs step**

In the `verify_docs` step (line ~643+), after the existing instruction "For each doc in the generation queue that was successfully written to disk:", add:

```markdown
**Additionally, verify non-canonical existing docs from the review_queue:**

For each doc in the `review_queue` assembled in build_doc_queue:
- Run the same gsd-doc-verifier verification process (check file paths, commands, endpoints, function signatures)
- Report findings but do NOT dispatch to gsd-doc-writer for fixes -- these docs have no matching template
- Include verification results in the report with a note: "Manual correction recommended" for any failures

Non-canonical docs are excluded from the fix_loop -- only canonical docs with templates are eligible for automated fixes.
```

**Change D: Update report step to include review_queue results**

In the `report` step (line ~855+), add a section after the main doc table for non-canonical doc verification results:

```markdown
{If review_queue was non-empty:}

**Existing doc accuracy review:**

| Doc | Claims Checked | Passed | Failed |
|-----|----------------|--------|--------|
| docs/api/endpoint-map.md | 5 | 4 | 1 |

{For any failures:}
Manual correction recommended for flagged items above.
```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('get-shit-done/workflows/docs-update.md','utf-8');const checks=[];checks.push(['CONTRIBUTING confirmation',c.includes('CONTRIBUTING.md does not exist yet')]);checks.push(['--force skip',c.includes('--force') && c.includes('skip this check')]);checks.push(['review_queue',c.includes('review_queue')]);checks.push(['non-canonical verify',c.includes('non-canonical') && c.includes('verify')]);checks.push(['manual correction',c.includes('Manual correction recommended')]);let pass=true;for(const [name,ok] of checks){if(!ok){console.log('FAIL: '+name);pass=false}else{console.log('OK: '+name)}}if(pass)console.log('PASS: all checks');else{process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - build_doc_queue step contains "CONTRIBUTING.md does not exist yet. Would you like to create one?"
    - build_doc_queue step contains check for `--force` before CONTRIBUTING prompt
    - build_doc_queue step contains "review_queue" for non-canonical existing docs
    - build_doc_queue step contains "NOT be dispatched to gsd-doc-writer" for non-canonical docs
    - verify_docs step contains instruction to verify non-canonical docs from review_queue
    - verify_docs step contains "Manual correction recommended" for non-canonical doc failures
    - verify_docs step contains "excluded from the fix_loop" for non-canonical docs
    - report step contains "Existing doc accuracy review" section template
  </acceptance_criteria>
  <done>CONTRIBUTING.md creation requires user confirmation (skipped with --force or when file already exists). Non-canonical existing docs are queued for verification-only review, not rewriting. Verification results for non-canonical docs are included in the final report with manual correction guidance.</done>
</task>

</tasks>

<verification>
1. `grep -c "docs/ARCHITECTURE.md" get-shit-done/workflows/docs-update.md` returns a count > 5 (appears in table, dispatch, collect, commit, report)
2. `grep -c "CONTRIBUTING.md does not exist yet" get-shit-done/workflows/docs-update.md` returns 1
3. `grep -c "review_queue" get-shit-done/workflows/docs-update.md` returns >= 3 (build_doc_queue, verify_docs, report)
4. `grep -c "Manual correction recommended" get-shit-done/workflows/docs-update.md` returns >= 1
5. `grep "mkdir -p docs" get-shit-done/workflows/docs-update.md` returns 1 match
</verification>

<success_criteria>
- The resolve_modes path table is inverted: docs/ is primary for all types except readme and contributing
- The docs/ directory is created before agent dispatch
- All downstream path references (collect, commit, report, sequential) use docs/ prefix
- CONTRIBUTING.md creation prompts user for confirmation when the file is new
- Non-canonical existing docs are verified for accuracy but not rewritten
- Non-canonical doc failures are reported with "manual correction recommended"
</success_criteria>

<output>
After completion, create `.planning/phases/05-refinement-of-docs-output/05-02-SUMMARY.md`
</output>
