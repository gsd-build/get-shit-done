---
phase: 03-verification-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [cursor-gsd/, GSD-CURSOR-ADAPTATION.md]
autonomous: true

must_haves:
  truths:
    - "cursor-gsd/ directory no longer exists in repository"
    - "GSD-CURSOR-ADAPTATION.md no longer exists in repository root"
    - "Git history shows cleanup commit"
    - "Repository still functions correctly (bin/install.js intact)"
  artifacts:
    - path: "bin/install.js"
      provides: "Unified installer with Cursor support"
      contains: "Cursor runtime code"
    - path: ".planning/phases/03-verification-cleanup/03-02-SUMMARY.md"
      provides: "Cleanup completion record"
  key_links:
    - from: "git rm -r cursor-gsd/"
      to: "repository state"
      via: "git staging and commit"
      pattern: "Deleted files removed from tracking"
---

<objective>
Remove deprecated cursor-gsd subfolder and GSD-CURSOR-ADAPTATION.md from the repository.

Purpose: The unified installer now handles Cursor deployment natively. The cursor-gsd/ folder (manual conversion) and GSD-CURSOR-ADAPTATION.md (conversion guide) are obsolete and should be removed to avoid confusion.
Output: Clean repository with only the unified installer approach
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-cleanup/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove cursor-gsd subfolder</name>
  <files>cursor-gsd/</files>
  <action>
Remove the entire cursor-gsd directory from git tracking:

```bash
git rm -r cursor-gsd/
```

This removes:
- cursor-gsd/src/ (converted commands, agents, templates, workflows, references, hooks)
- cursor-gsd/scripts/ (standalone install scripts)
- cursor-gsd/docs/ (GSD-CURSOR-ADAPTATION.md duplicate)
- cursor-gsd/assets/ (screenshots)
- cursor-gsd/*.md (README, CHANGELOG, MIGRATION, etc.)
- cursor-gsd/.github/ (issue templates)

The unified installer (bin/install.js) now handles all Cursor conversion automatically, making this manual conversion folder obsolete.
  </action>
  <verify>
`ls cursor-gsd/` returns error (directory no longer exists in working tree)
`git status` shows cursor-gsd/ files staged for deletion
  </verify>
  <done>cursor-gsd/ directory removed from repository</done>
</task>

<task type="auto">
  <name>Task 2: Remove GSD-CURSOR-ADAPTATION.md</name>
  <files>GSD-CURSOR-ADAPTATION.md</files>
  <action>
Remove the GSD-CURSOR-ADAPTATION.md file from repository root:

```bash
git rm GSD-CURSOR-ADAPTATION.md
```

This document was the conversion guide for manually adapting GSD to Cursor. With the unified installer handling conversion automatically, this guide is no longer needed.

Note: The content of this guide informed the implementation of `convertClaudeToCursorFrontmatter()` and related functions in bin/install.js. The knowledge has been codified.
  </action>
  <verify>
`ls GSD-CURSOR-ADAPTATION.md` returns error (file no longer exists)
`git status` shows GSD-CURSOR-ADAPTATION.md staged for deletion
  </verify>
  <done>GSD-CURSOR-ADAPTATION.md removed from repository</done>
</task>

<task type="auto">
  <name>Task 3: Commit cleanup</name>
  <files>.git/</files>
  <action>
Commit the cleanup changes with a clear message:

```bash
git commit -m "chore(03-02): remove cursor-gsd/ and GSD-CURSOR-ADAPTATION.md

- Remove cursor-gsd/ subfolder (70 files)
- Remove GSD-CURSOR-ADAPTATION.md conversion guide
- Cursor support now handled by unified installer

The bin/install.js now natively supports --cursor flag,
making the manual conversion folder obsolete."
```

Verify the commit is clean:
- `git log -1` shows the cleanup commit
- `git status` shows working tree clean
  </action>
  <verify>
`git log -1 --oneline` shows cleanup commit
`git status` shows "nothing to commit, working tree clean"
  </verify>
  <done>Cleanup committed to git history</done>
</task>

</tasks>

<verification>
1. `cursor-gsd/` directory does not exist
2. `GSD-CURSOR-ADAPTATION.md` does not exist
3. Git commit present with cleanup message
4. `bin/install.js` still exists and works (not accidentally deleted)
5. `node bin/install.js --help` still shows --cursor option
</verification>

<success_criteria>
- CLN-01: cursor-gsd/ folder deleted from repository
- CLN-02: GSD-CURSOR-ADAPTATION.md removed from root
- Git commit confirms cleanup
- Installer still functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-cleanup/03-02-SUMMARY.md`
</output>
