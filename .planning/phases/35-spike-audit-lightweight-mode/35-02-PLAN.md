---
phase: 35-spike-audit-lightweight-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/run-spike.md
  - agents/gsd-spike-runner.md
  - get-shit-done/references/spike-execution.md
autonomous: true

must_haves:
  truths:
    - "A spike can be created with mode: research that skips BUILD and RUN phases and goes directly from research to DECISION.md"
    - "The research-first advisory in run-spike.md offers a fourth option to run as lightweight research spike"
    - "A lightweight research spike still produces DESIGN.md, DECISION.md, and a KB entry"
  artifacts:
    - path: "get-shit-done/workflows/run-spike.md"
      provides: "Lightweight research spike mode (option 4 in advisory, SPIKE_MODE=research path)"
      contains: "research"
    - path: "agents/gsd-spike-runner.md"
      provides: "Research-only execution path that skips BUILD/RUN"
      contains: "research"
  key_links:
    - from: "get-shit-done/workflows/run-spike.md"
      to: "agents/gsd-spike-runner.md"
      via: "run-spike.md Step 5b handles research inline for interactive option 4; runner uses mode: research from DESIGN.md when invoked by plan-phase step 5.5 auto_trigger path"
      pattern: "research"
---

<objective>
Add lightweight research spike mode that skips BUILD/RUN phases for questions answerable through documentation and codebase analysis.

Purpose: The spike system currently requires full BUILD/RUN/DOCUMENT cycles even for questions that just need research. This excessive ceremony discourages use. The lightweight mode provides a simpler path: Question -> Research -> Decision, with full KB persistence but no experiment code.
Output: Updated run-spike.md with option 4 and research mode path, updated gsd-spike-runner.md with research-only execution.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/workflows/run-spike.md
@agents/gsd-spike-runner.md
@get-shit-done/references/spike-execution.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lightweight research spike mode to run-spike.md</name>
  <files>get-shit-done/workflows/run-spike.md</files>
  <action>
Modify run-spike.md to add a lightweight research spike mode. Three changes needed:

**Change 1: Add option 4 to the Research-First Advisory (Step 2)**

In the interactive mode options presented when a question appears research-suitable, add a fourth option:

```
Options:
1. Proceed with full spike (Build/Run/Document)
2. Cancel -- try research first (/gsd:research-phase)
3. Rephrase question to focus on the empirical aspect
4. Run as lightweight research spike (Question -> Research -> Decision, no BUILD/RUN)
```

Add handling: "If user selects 4: set SPIKE_MODE='research', proceed to workspace creation."

**Change 2: Add mode flag support to Inputs table**

Update the Inputs table to include SPIKE_MODE:

| Input | Source | Required |
|-------|--------|----------|
| spike_mode | --mode argument or research-first advisory selection | no (default: full) |

**Change 3: Add Step 5b for research mode execution**

After Step 5 (User Confirmation) and before Step 6 (Spawn Spike Runner Agent), add:

```markdown
### 5b. Lightweight Research Mode

**If SPIKE_MODE == "research":**

Skip Step 6 (do NOT spawn gsd-spike-runner for BUILD/RUN phases).

Instead, perform research inline:
1. Add `mode: research` to DESIGN.md frontmatter
2. Investigate the question using available tools:
   - Codebase analysis (Grep, Glob, Read)
   - Web research (WebSearch, WebFetch)
   - Documentation lookup (Context7 if available)
3. Gather evidence to answer the question
4. Create DECISION.md in the spike workspace using the standard template from spike-execution.md Section 6
   - Question, Answer, Summary, Findings (from research), Analysis, Decision, Confidence, Implications
   - Set metadata: `Iterations: 0 (research-only)`
5. Update DESIGN.md status to `complete`
6. Proceed to KB persistence (same as Step 7 result handling, but with outcome from research)
7. Continue to Step 8 (Update RESEARCH.md if phase-linked) and Step 9 (Report)

**IMPORTANT:** The lightweight mode still creates:
- DESIGN.md (documents the question)
- DECISION.md (documents the answer)
- KB entry (persists the finding)

It does NOT create:
- experiments/ directory
- FINDINGS.md
```

Also update the "Mode Behaviors" table at the bottom to include research mode:

```
| Mode | Design Confirmation | BUILD/RUN | DECISION.md |
|------|---------------------|-----------|-------------|
| full (default) | User confirms (interactive) / auto (yolo) | Yes | From experiments |
| research | User confirms (interactive) / auto (yolo) | Skipped | From research |
```

**Change 4: Update spike-execution.md reference doc**

In `get-shit-done/references/spike-execution.md`, add documentation for the research mode:
- In the spike types/phases section, add `research` as a valid mode alongside the existing full spike flow
- Note that research-mode spikes skip BUILD and RUN phases entirely
- Note that `mode: research` appears in DESIGN.md frontmatter
- Document that DECISION.md is produced from research findings rather than experimental data
- Note that research-mode spikes do NOT produce experiments/ directory or FINDINGS.md

This ensures the canonical spike reference doc knows about the new mode. Without this update, future spike authors won't discover research mode through the reference docs.
  </action>
  <verify>
Grep run-spike.md for "research" and "5b" and "lightweight" -- all should match. Verify the option 4 text appears in the Research-First Advisory section. Verify Step 5b appears between Steps 5 and 6.
  </verify>
  <done>run-spike.md offers option 4 in research-first advisory for lightweight research spikes. SPIKE_MODE=research path skips BUILD/RUN and goes straight from research to DECISION.md with full KB persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Add research-only execution path to gsd-spike-runner.md</name>
  <files>agents/gsd-spike-runner.md</files>
  <action>
Update gsd-spike-runner.md to handle research-only mode. The spike runner needs to know about this mode even though for lightweight spikes the orchestrator handles most of the work (Step 5b in run-spike.md). The runner may still be invoked for research-mode spikes triggered by the plan-phase orchestrator.

**Change 1: Update the role section**

Add a note to the role description after "Your responsibility": "For research-mode spikes (mode: research in DESIGN.md), you perform research instead of building and running experiments. The Document phase produces DECISION.md from research findings rather than experimental data."

**Change 2: Add mode detection in Step 1 (Load DESIGN.md)**

After parsing DESIGN.md fields, add:

```markdown
Parse mode from DESIGN.md frontmatter:
- If `mode: research` is present: this is a lightweight research spike
- Skip BUILD and RUN phases (Steps 2 and 3)
- Proceed directly to research (Step 2b below) then Document (Step 5)
```

**Change 3: Add Step 2b (Research Phase)**

Insert between Steps 2 and 3:

```markdown
## 2b. RESEARCH Phase (Research-Mode Spikes Only)

**Only executed when DESIGN.md contains `mode: research`.**

Skip Steps 2 (BUILD) and 3 (RUN). Instead:

1. **Investigate the question** using available tools:
   - Read relevant codebase files (Grep, Glob, Read)
   - Check documentation and references
   - Analyze existing patterns and conventions

2. **Gather evidence** organized by the question's success criteria:
   - For each criterion in DESIGN.md, document what research found
   - Note confidence level for each finding (HIGH/MEDIUM/LOW)
   - Flag areas where research is insufficient and experimentation would be needed

3. **Proceed to Step 4 (Evaluate)** with research findings as the "results"

**Principles:**
- Research-mode spikes produce FINDINGS through investigation, not experimentation
- The DECISION.md should clearly state that evidence is from research, not empirical testing
- If research reveals the question actually needs experimentation, note this in DECISION.md
  with confidence: LOW and recommend a follow-up full spike
```

**Change 4: Update Step 4 (Evaluate Results)**

Add a note at the top of Step 4: "For research-mode spikes, 'results' are research findings rather than experimental measurements. Evaluate whether research produced a conclusive answer to the question."

**Change 5: Update constraints section**

Add: "- Research-mode spikes skip BUILD and RUN phases entirely"
  </action>
  <verify>
Grep gsd-spike-runner.md for "research" and "2b" and "RESEARCH Phase" -- all should match. Verify the mode detection is present in Step 1. Verify Step 2b appears between Steps 2 and 3.
  </verify>
  <done>gsd-spike-runner.md handles research-mode spikes by skipping BUILD/RUN and performing research investigation instead, then proceeding to DECISION.md.</done>
</task>

</tasks>

<verification>
1. `grep -c "lightweight" get-shit-done/workflows/run-spike.md` returns at least 1
2. `grep -c "SPIKE_MODE" get-shit-done/workflows/run-spike.md` returns at least 1
3. `grep -c "option 4\|Option 4\|research spike" get-shit-done/workflows/run-spike.md` returns at least 1
4. `grep -c "research" agents/gsd-spike-runner.md` returns at least 5 (multiple occurrences expected)
5. `grep -c "2b" agents/gsd-spike-runner.md` returns at least 1
</verification>

<success_criteria>
- run-spike.md offers lightweight research spike as option 4 in the research-first advisory
- run-spike.md has SPIKE_MODE=research path (Step 5b) that skips BUILD/RUN
- gsd-spike-runner.md has Step 2b research-only path for mode: research spikes
- Lightweight spikes still produce DESIGN.md, DECISION.md, and KB entry
- SPIKE-02 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/35-spike-audit-lightweight-mode/35-02-SUMMARY.md`
</output>
