---
phase: 35-spike-audit-lightweight-mode
plan: 03
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - .planning/spikes/002-claude-code-session-log-location/DESIGN.md
  - .planning/spikes/002-claude-code-session-log-location/DECISION.md
autonomous: true

must_haves:
  truths:
    - "At least one spike has completed end-to-end: DESIGN.md created, research performed, DECISION.md written, KB entry persisted"
    - "The completed spike used the lightweight research mode (mode: research)"
    - "The spike's KB entry exists in ~/.gsd/knowledge/spikes/ and the KB index has been rebuilt"
    - "The reflect-to-spike pipeline is verified: spike candidates were found in reflection output OR pipeline code is confirmed correct with documented findings in SUMMARY"
  artifacts:
    - path: ".planning/spikes/002-claude-code-session-log-location/DESIGN.md"
      provides: "Spike design with mode: research"
      contains: "mode: research"
    - path: ".planning/spikes/002-claude-code-session-log-location/DECISION.md"
      provides: "Research-based spike decision answering the log location question"
      contains: "Decision"
  key_links:
    - from: ".planning/spikes/002-claude-code-session-log-location/DECISION.md"
      to: "~/.gsd/knowledge/spikes/"
      via: "KB entry created and index rebuilt"
      pattern: "spk-"
---

<objective>
Complete a real spike end-to-end using the lightweight research mode, proving the spike system works in practice.

Purpose: No spike has ever been completed end-to-end in this project. Spike 001 (runtime capability verification) stalled at DESIGN.md. This plan creates and completes a new lightweight research spike for the SENSOR-07 log location question, demonstrating the full lifecycle: design -> research -> decision -> KB persistence.
Output: Completed spike 002 with DESIGN.md, DECISION.md, and KB entry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/workflows/run-spike.md
@get-shit-done/references/spike-execution.md
@agents/gsd-spike-runner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and complete lightweight research spike for log location question</name>
  <files>.planning/spikes/002-claude-code-session-log-location/DESIGN.md, .planning/spikes/002-claude-code-session-log-location/DECISION.md</files>
  <action>
This task executes a real spike end-to-end. The question comes from SENSOR-07 (log sensor shipped disabled because the log location was unknown). This is a research-only question -- no experiment code needed.

**Step 1: Create workspace**

```bash
mkdir -p .planning/spikes/002-claude-code-session-log-location
```

**Step 2: Create DESIGN.md**

Write DESIGN.md with the following content:

```yaml
---
created: {today's date ISO format}
status: designing
originating_phase: 35
depends_on: none
round: 1
mode: research
time_estimate: ~10 minutes (research only)
---
```

Body:
- Question: "Where does Claude Code store session logs, and can they be programmatically accessed for signal detection?"
- Type: Exploratory
- Hypothesis: Claude Code stores session logs in a platform-specific location (likely ~/.claude/ or a cache directory) that can be read by a log sensor for pattern detection.
- Success Criteria:
  - [ ] Log file/directory location identified with HIGH confidence
  - [ ] Log format documented (structured JSON, plain text, or other)
  - [ ] Programmatic access feasibility assessed (readable by external tools? permissions?)
  - [ ] Recommendation for log sensor implementation: enable with path, defer, or abandon
- Experiment Plan: Research-only. No experiments. Investigation will use:
  - Codebase inspection of Claude Code's known paths
  - File system exploration of ~/.claude/ and related directories
  - Documentation and community source review

**Step 3: Perform research**

Investigate the question:
1. Check `~/.claude/` directory structure for log-like files
2. Look for session data, conversation history, or debug logs
3. Check common macOS paths: `~/Library/Logs/`, `~/Library/Application Support/`, `~/Library/Caches/`
4. Search for Claude Code log configuration or environment variables
5. Check if `claude --help` or documentation mentions logging

**Step 4: Create DECISION.md**

Write DECISION.md with findings:
- Question and one-line answer
- Summary of research findings
- Analysis of log location, format, and accessibility
- Decision: whether the log sensor should be enabled, deferred, or abandoned
- Confidence level based on evidence quality
- Implications for SENSOR-07

**Step 5: Update DESIGN.md status to `complete`**

Update the frontmatter status field from `designing` to `complete`.

**Step 6: Create KB entry**

First create the KB spikes directory (it doesn't exist yet):

```bash
mkdir -p ~/.gsd/knowledge/spikes/get-shit-done-reflect
```

Create spike KB entry at `~/.gsd/knowledge/spikes/get-shit-done-reflect/claude-code-session-log-location.md`:

```yaml
---
id: spk-{today's date}-claude-code-session-log-location
type: spike
project: get-shit-done-reflect
tags: [logging, sensor, claude-code, signal-collection]
created: {today's ISO timestamp}
updated: {today's ISO timestamp}
durability: convention
status: active
hypothesis: "Claude Code stores session logs in a platform-specific location accessible for signal detection"
outcome: {confirmed|rejected|partial|inconclusive based on findings}
rounds: 1
mode: research
---
```

Body: Hypothesis, Research summary, Results, Decision, Consequences sections from DECISION.md.

**Step 7: Rebuild KB index**

```bash
bash ~/.gsd/bin/kb-rebuild-index.sh
```

IMPORTANT: This is a REAL spike execution, not a mock. Actually investigate the question and document real findings. The decision should be based on what you actually discover about Claude Code's log storage.

**NOTE:** This is a manual spike execution that follows the lightweight research pattern (DESIGN.md -> research -> DECISION.md -> KB). It does NOT invoke `/gsd:spike` or exercise the run-spike.md workflow directly, because the executor agent cannot invoke slash commands. The structural correctness of the lightweight workflow (Plan 02) is verified in Plan 04's human verification checkpoint. The manual execution here proves the spike artifact lifecycle works end-to-end.

**Step 7b: Verify kb-rebuild-index.sh handles spike entries**

Before rebuilding, confirm that `~/.gsd/bin/kb-rebuild-index.sh` indexes the `spikes/` directory (not just `signals/` and `lessons/`). If it doesn't, the KB entry will exist but won't appear in the index. Check with:
```bash
grep -c "spikes" ~/.gsd/bin/kb-rebuild-index.sh
```
If spikes aren't handled, note this as a gap and manually add the entry to `~/.gsd/knowledge/index.md`.
  </action>
  <verify>
1. Verify DESIGN.md exists: `cat .planning/spikes/002-claude-code-session-log-location/DESIGN.md`
2. Verify DECISION.md exists: `cat .planning/spikes/002-claude-code-session-log-location/DECISION.md`
3. Verify DESIGN.md status is complete: `grep "status: complete" .planning/spikes/002-claude-code-session-log-location/DESIGN.md`
4. Verify KB entry exists: `ls ~/.gsd/knowledge/spikes/get-shit-done-reflect/claude-code-session-log-location.md`
5. Verify KB index was rebuilt: `grep "claude-code-session-log" ~/.gsd/knowledge/index.md`
  </verify>
  <done>Spike 002 completed end-to-end: DESIGN.md (mode: research), research performed, DECISION.md written with real findings, KB entry persisted, index rebuilt. SPIKE-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Verify reflect-to-spike pipeline with real spike candidate</name>
  <files>agents/gsd-reflector.md, get-shit-done/references/reflection-patterns.md</files>
  <action>
Verify that the reflect-to-spike pipeline (built in Phase 33) works by checking the most recent reflection output for spike candidates.

**Step 1: Check for existing reflection reports**

```bash
ls ~/.gsd/knowledge/reflections/ 2>/dev/null
```

If reflection reports exist, read the most recent one and look for the "Spike Candidates" section.

**Step 2: If no spike candidates exist in reports, verify the pipeline code is correct**

Read `agents/gsd-reflector.md` Step 8 and `get-shit-done/references/reflection-patterns.md` Section 12 to confirm:
1. The three trigger conditions are correctly specified (investigate triage, low confidence, marginal score)
2. The output format matches what `/gsd:spike` would consume
3. The pipeline is connected end-to-end (reflector outputs -> user acts on candidates -> spike executes)

**Step 3: Document the verification**

If spike candidates were found in a reflection report:
- Note which candidates exist and their trigger conditions
- Confirm the format matches what `/gsd:spike` expects
- Note that the pipeline is verified with real data

If no spike candidates exist (no patterns triggered the conditions):
- Document that the pipeline code is correctly implemented
- Note that no existing signals met the spike candidate trigger conditions (which is valid -- it means the reflector has high confidence in its patterns)
- Confirm the pipeline would produce candidates when trigger conditions are met

This is a verification task, not an implementation task. The pipeline was built in Phase 33; we are confirming it works or identifying issues.

**Step 4: Record findings**

Document verification results in the SUMMARY for this plan. Include:
- Whether real spike candidates were found
- Pipeline code correctness assessment
- Any issues identified and recommendations
  </action>
  <verify>
1. `ls ~/.gsd/knowledge/reflections/ 2>/dev/null | head -5` -- reflection reports directory checked
2. `grep -c "spike" agents/gsd-reflector.md` -- reflector Step 8 spike candidate section exists
3. `grep -c "spike" get-shit-done/references/reflection-patterns.md` -- Section 12 pipeline criteria exists
  </verify>
  <done>Reflect-to-spike pipeline verified: either real spike candidates confirmed in reflection output, or pipeline code confirmed correct with note that no trigger conditions were met. SPIKE-05 satisfied.</done>
</task>

</tasks>

<verification>
1. Spike 002 workspace exists with both DESIGN.md and DECISION.md
2. DESIGN.md has `mode: research` and `status: complete`
3. DECISION.md contains a real answer to the log location question
4. KB entry exists at ~/.gsd/knowledge/spikes/get-shit-done-reflect/
5. KB index includes the new spike entry
6. Reflect-to-spike pipeline verification documented
</verification>

<success_criteria>
- At least one spike completed end-to-end (SPIKE-04)
- The completed spike used lightweight research mode
- KB entry persisted and index rebuilt
- Reflect-to-spike pipeline verified (SPIKE-05)
</success_criteria>

<output>
After completion, create `.planning/phases/35-spike-audit-lightweight-mode/35-03-SUMMARY.md`
</output>
