---
phase: 35-spike-audit-lightweight-mode
plan: 04
type: execute
wave: 2
depends_on: ["35-01", "35-02", "35-03"]
files_modified:
  - .claude/get-shit-done/workflows/plan-phase.md
  - .claude/get-shit-done/workflows/run-spike.md
  - .claude/get-shit-done/feature-manifest.json
  - .claude/agents/gsd-phase-researcher.md
  - .claude/agents/gsd-spike-runner.md
  - .claude/get-shit-done/references/spike-integration.md
  - .claude/get-shit-done/references/spike-execution.md
autonomous: false

must_haves:
  truths:
    - "All source file changes from Plans 01 and 02 are synced to the .claude/ runtime directory"
    - "The installed runtime files match the npm source files after path conversion"
  artifacts:
    - path: ".claude/get-shit-done/workflows/plan-phase.md"
      provides: "Installed runtime copy of plan-phase.md with step 5.5"
      contains: "5.5"
    - path: ".claude/get-shit-done/workflows/run-spike.md"
      provides: "Installed runtime copy of run-spike.md with lightweight mode"
      contains: "research"
    - path: ".claude/get-shit-done/feature-manifest.json"
      provides: "Installed runtime copy of feature-manifest.json with spike section"
      contains: "spike"
    - path: ".claude/agents/gsd-phase-researcher.md"
      provides: "Installed runtime copy of researcher with Genuine Gaps format"
      contains: "Genuine Gaps"
    - path: ".claude/agents/gsd-spike-runner.md"
      provides: "Installed runtime copy of spike runner with research mode"
      contains: "research"
    - path: ".claude/get-shit-done/references/spike-integration.md"
      provides: "Installed runtime copy with reconciled config keys"
      contains: "spike."
    - path: ".claude/get-shit-done/references/spike-execution.md"
      provides: "Installed runtime copy with research mode documented"
      contains: "research"
  key_links:
    - from: "get-shit-done/"
      to: ".claude/get-shit-done/"
      via: "node bin/install.js --local"
      pattern: "install"
---

<objective>
Sync all Phase 35 source changes to the .claude/ runtime directory and verify the spike system works end-to-end.

Purpose: The dual-directory architecture requires syncing npm source files to the .claude/ runtime directory. This plan runs the installer and provides a human verification checkpoint for the complete spike system.
Output: Synced .claude/ runtime, verified spike system.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run installer to sync source to runtime</name>
  <files>.claude/get-shit-done/workflows/plan-phase.md, .claude/get-shit-done/workflows/run-spike.md, .claude/get-shit-done/feature-manifest.json, .claude/agents/gsd-phase-researcher.md, .claude/agents/gsd-spike-runner.md</files>
  <action>
Run the local installer to sync all npm source changes to the .claude/ runtime directory:

```bash
node bin/install.js --local
```

After the installer completes, verify that the key changes made in Plans 01 and 02 are present in the installed runtime files:

1. Verify step 5.5 in installed plan-phase.md:
   ```bash
   grep "5.5" .claude/get-shit-done/workflows/plan-phase.md
   ```

2. Verify Genuine Gaps format in installed researcher:
   ```bash
   grep "Genuine Gaps" .claude/agents/gsd-phase-researcher.md
   ```

3. Verify spike section in installed manifest:
   ```bash
   grep "spike" .claude/get-shit-done/feature-manifest.json
   ```

4. Verify lightweight mode in installed run-spike.md:
   ```bash
   grep "research" .claude/get-shit-done/workflows/run-spike.md
   ```

5. Verify research mode in installed spike runner:
   ```bash
   grep "research" .claude/agents/gsd-spike-runner.md
   ```

6. Verify spike-integration.md config keys reconciled:
   ```bash
   grep "spike\." .claude/get-shit-done/references/spike-integration.md
   ```

7. Verify spike-execution.md has research mode:
   ```bash
   grep "research" .claude/get-shit-done/references/spike-execution.md
   ```

All seven checks must pass. If any fail, investigate the installer to ensure the files were copied correctly. The installer's `replacePathsInContent()` converts `~/.claude/` paths to `./.claude/` paths during install -- verify this conversion happened.
  </action>
  <verify>
All seven grep checks above return matches. The installer ran without errors. `npm test` passes (the test suite should not be broken by these changes since they are agent spec files, not code).
  </verify>
  <done>All Phase 35 source changes synced to .claude/ runtime directory. Installed files contain step 5.5, Genuine Gaps format, spike manifest section, and lightweight research mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete spike system</name>
  <files></files>
  <action>
Present the complete Phase 35 deliverables for human verification. This checkpoint covers all five SPIKE requirements:
1. SPIKE-01: plan-phase.md step 5.5 and researcher Genuine Gaps format
2. SPIKE-02: Lightweight research spike mode in run-spike.md and gsd-spike-runner.md
3. SPIKE-03: Spike section in feature manifest
4. SPIKE-04: Spike 002 completed end-to-end
5. SPIKE-05: Reflect-to-spike pipeline verified
  </action>
  <verify>
Human reviews and approves:
1. `cat get-shit-done/workflows/plan-phase.md | grep -A 30 "5.5"` -- step 5.5 content (includes --skip-research skip condition)
2. `cat agents/gsd-phase-researcher.md | grep -A 10 "Genuine Gaps"` -- format change
3. `cat get-shit-done/feature-manifest.json | grep -A 20 '"spike"'` -- manifest section
4. `cat get-shit-done/workflows/run-spike.md | grep -A 5 "lightweight"` -- option 4
5. `cat get-shit-done/references/spike-integration.md | grep "spike\."` -- config keys reconciled to manifest format
6. `cat get-shit-done/references/spike-execution.md | grep -A 5 "research"` -- research mode documented
7. `ls .planning/spikes/002-*/` and `cat .planning/spikes/002-*/DECISION.md` -- completed spike
8. `ls ~/.gsd/knowledge/spikes/get-shit-done-reflect/ | grep claude-code` -- KB entry
9. Structural check: confirm run-spike.md Step 5b lightweight path would produce the same artifact set (DESIGN.md, DECISION.md, KB entry) as the manually executed spike in Plan 03
10. `npm test` -- tests pass
  </verify>
  <done>Human approves all Phase 35 deliverables. Spike system is wired, configurable, has lightweight mode, and has been exercised end-to-end.</done>
</task>

</tasks>

<verification>
1. Installer ran without errors
2. All seven key files synced to .claude/ with correct content
3. npm test passes
4. Lightweight mode structural check confirms artifact parity with manual spike
5. Human verification approved
</verification>

<success_criteria>
- .claude/ runtime files match npm source files (after path conversion)
- Human has verified the spike system changes are correct
- All Phase 35 requirements (SPIKE-01 through SPIKE-05) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/35-spike-audit-lightweight-mode/35-04-SUMMARY.md`
</output>
