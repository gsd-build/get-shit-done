---
phase: 18.1-ui-controls-gsd-end-to-end-orchestration
plan: 03
type: execute
wave: 2
depends_on: [18.1-01]
files_modified:
  - apps/web/src/components/features/orchestration/ControlActionConfirmDialog.tsx
  - apps/web/src/components/features/orchestration/RevalidationSummaryPanel.tsx
  - apps/web/src/components/features/orchestration/OrchestrationControlBar.tsx
  - apps/web/src/hooks/useOrchestrationControl.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/stores/planStore.ts
  - apps/web/src/stores/executionStore.ts
  - apps/server/src/api/routes/agents.ts
  - apps/server/src/orchestrator/retry.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All major orchestration actions require explicit confirmation"
    - "Plan editing is locked while a run is active and only allowed in pause-edit-resume flow"
    - "Resume-after-edit executes full revalidation and blocks resume on failed checks"
    - "Failure path offers both Retry and Open fix plan"
  artifacts:
    - path: "apps/web/src/components/features/orchestration/ControlActionConfirmDialog.tsx"
      provides: "Unified confirmation UX for high-impact lifecycle actions"
      exports: ["ControlActionConfirmDialog"]
    - path: "apps/web/src/components/features/orchestration/RevalidationSummaryPanel.tsx"
      provides: "Expanded revalidation results before resume"
      exports: ["RevalidationSummaryPanel"]
    - path: "apps/server/src/api/routes/agents.ts"
      provides: "Lifecycle action routes with full revalidation gate support"
  key_links:
    - from: "useOrchestrationControl"
      to: "agents API"
      via: "confirm + execute action workflow"
      pattern: "POST.*agents"
    - from: "RevalidationSummaryPanel"
      to: "resume action"
      via: "resume blocked until revalidation passes"
      pattern: "(revalidation|resume)"
---

<objective>
Implement lifecycle safety and recovery UX: mandatory confirmations, pause-edit-resume locking policy, and full revalidation gating before resume.

Purpose: Enforce safe orchestration behavior while preserving fast recovery options.
Output: Confirmation/revalidation UX and server-backed gating for resume/retry behaviors.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-ui-controls-gsd-end-to-end-orchestration/18.1-CONTEXT.md
@.planning/phases/18.1-ui-controls-gsd-end-to-end-orchestration/18.1-RESEARCH.md
@apps/web/src/hooks/useAgentControl.ts
@apps/web/src/hooks/useErrorRecovery.ts
@apps/web/src/stores/planStore.ts
@apps/web/src/stores/executionStore.ts
@apps/server/src/api/routes/agents.ts
@apps/server/src/orchestrator/retry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centralized confirmations for all major lifecycle actions</name>
  <files>apps/web/src/components/features/orchestration/ControlActionConfirmDialog.tsx, apps/web/src/components/features/orchestration/OrchestrationControlBar.tsx, apps/web/src/hooks/useOrchestrationControl.ts</files>
  <action>
Implement a single confirmation dialog policy for:
- Abort
- Stop now
- Cancel current and start new
- Re-run from checkpoint
- Retry

Wire `OrchestrationControlBar` and shared control hook so these actions cannot execute without explicit confirm acknowledgment.
Allow expandable detail sections in dialog content for dependency-related or destructive impacts.
  </action>
  <verify>pnpm -C apps/web typecheck && pnpm -C apps/web test --run</verify>
  <done>All required high-impact actions are confirmation-gated through one reusable mechanism</done>
</task>

<task type="auto">
  <name>Task 2: Enforce pause-edit-resume lock policy with full revalidation</name>
  <files>apps/web/src/components/features/orchestration/RevalidationSummaryPanel.tsx, apps/web/src/stores/planStore.ts, apps/web/src/stores/executionStore.ts, apps/web/src/lib/api.ts</files>
  <action>
Implement policy-aligned editing lifecycle:
- Active run => plan editing disabled.
- Paused run => editing enabled.
- Resume attempt after edits => run full revalidation first.

Add `RevalidationSummaryPanel` with expandable details for failed checks and changed task impact summary.
Resume must be blocked until revalidation succeeds.
  </action>
  <verify>pnpm -C apps/web typecheck && pnpm -C apps/web test --run</verify>
  <done>Pause-edit-resume flow is enforced and resume is guarded by full revalidation</done>
</task>

<task type="auto">
  <name>Task 3: Add server support for revalidation-gated resume and dual recovery paths</name>
  <files>apps/server/src/api/routes/agents.ts, apps/server/src/orchestrator/retry.ts</files>
  <action>
Extend server lifecycle handling to support:
- Full revalidation check before resume-after-edit.
- Structured revalidation response payload for expandable UI details.
- Both recovery paths available from failure state: retry current path and open fix-plan route metadata.

Keep existing retry semantics stable; add explicit branching only where needed for the new policy.
  </action>
  <verify>pnpm -C apps/server typecheck && pnpm -C apps/server test --run</verify>
  <done>Server enforces revalidation gating and supports both retry + fix-plan recovery paths</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/server typecheck`
- `pnpm -C apps/web test --run`
- `pnpm -C apps/server test --run`
</verification>

<success_criteria>
- All designated lifecycle actions require confirmation
- Plan editing lock follows pause-edit-resume only
- Resume-after-edit runs full revalidation and blocks on failure
- Recovery UX supports Retry and Open fix plan
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-ui-controls-gsd-end-to-end-orchestration/18.1-03-SUMMARY.md`
</output>
