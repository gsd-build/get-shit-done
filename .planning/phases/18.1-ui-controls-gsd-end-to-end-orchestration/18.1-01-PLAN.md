---
phase: 18.1-ui-controls-gsd-end-to-end-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/stores/orchestrationStore.ts
  - apps/web/src/hooks/useOrchestrationControl.ts
  - apps/web/src/components/features/orchestration/OrchestrationControlBar.tsx
  - apps/web/src/components/features/orchestration/RunStatusStrip.tsx
  - apps/web/src/components/features/orchestration/index.ts
  - apps/server/src/api/routes/phases.ts
  - apps/server/src/api/routes/projects.ts
  - apps/server/src/orchestrator/types.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "A single project-level orchestration control state is available across Discuss/Plan/Execute/Verify"
    - "Control actions are gated by server-side orchestration policy checks"
    - "Page-level compact status strips reflect the same run state as the top control bar"
  artifacts:
    - path: "apps/web/src/components/features/orchestration/OrchestrationControlBar.tsx"
      provides: "Persistent top-level orchestration controls"
      exports: ["OrchestrationControlBar"]
    - path: "apps/web/src/stores/orchestrationStore.ts"
      provides: "Shared orchestration run registry and UI state"
      exports: ["useOrchestrationStore"]
    - path: "apps/server/src/api/routes/phases.ts"
      provides: "Parallelism/dependency check endpoint for run gating"
  key_links:
    - from: "OrchestrationControlBar"
      to: "phases API"
      via: "parallelism assessment + lifecycle action requests"
      pattern: "fetch.*phases.*(parallel|assess|orchestr)"
    - from: "RunStatusStrip"
      to: "orchestrationStore"
      via: "shared selected run and status"
      pattern: "useOrchestrationStore"
---

<objective>
Create the orchestration control-plane foundation: shared state, shared controls, and server contracts for dependency-aware run gating.

Purpose: Establish a single orchestration control model used by all workflow pages and ready for safety/recovery policy layers.
Output: New orchestration store/hooks/components plus server-side parallelism assessment contract.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.1-ui-controls-gsd-end-to-end-orchestration/18.1-CONTEXT.md
@.planning/phases/18.1-ui-controls-gsd-end-to-end-orchestration/18.1-RESEARCH.md
@apps/web/src/hooks/useAgentControl.ts
@apps/web/src/stores/executionStore.ts
@apps/server/src/api/routes/phases.ts
@apps/server/src/api/routes/projects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared orchestration state and action hook</name>
  <files>apps/web/src/stores/orchestrationStore.ts, apps/web/src/hooks/useOrchestrationControl.ts</files>
  <action>
Implement a dedicated orchestration store that tracks multi-run project state, selected run context, and gating results:
- Run registry keyed by run/agent ID with status (`active`, `paused`, `blocked`, `complete`, `error`)
- Project-scoped selected run
- Parallelism assessment results and assessed timestamp
- Pause/edit lock signals (editing allowed only when run paused)

Implement `useOrchestrationControl` as the abstraction layer over lifecycle actions (`start`, `pause`, `resume`, `abort`, `retryFailedStep`, `rerunFromCheckpoint`) and server checks.

Important: keep server as source of truth for eligibility and parallel safety; client should not infer dependency safety locally.
  </action>
  <verify>pnpm -C apps/web typecheck && pnpm -C apps/web test --run</verify>
  <done>Shared store and hook expose control/lifecycle APIs with server-backed gating</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable control surfaces and wire exports</name>
  <files>apps/web/src/components/features/orchestration/OrchestrationControlBar.tsx, apps/web/src/components/features/orchestration/RunStatusStrip.tsx, apps/web/src/components/features/orchestration/index.ts</files>
  <action>
Create two reusable components:
- `OrchestrationControlBar`: persistent top control surface with lifecycle actions and current run summary.
- `RunStatusStrip`: compact per-page strip exposing status and 1-2 context-aware quick actions.

Ensure behavior follows locked decisions:
- Shared state between top bar and strips.
- Dashboard remains status-only (no destructive actions).
- Click UI primary; keyboard hooks can be non-blocking stubs for later iteration.
  </action>
  <verify>pnpm -C apps/web typecheck && pnpm -C apps/web test --run</verify>
  <done>Shared control components exist and are ready for page-level integration in later plans</done>
</task>

<task type="auto">
  <name>Task 3: Add server endpoints for orchestration gating and paused visibility baseline</name>
  <files>apps/server/src/api/routes/phases.ts, apps/server/src/api/routes/projects.ts, apps/server/src/orchestrator/types.ts</files>
  <action>
Extend server API to support dependency-aware run gating and dashboard visibility:
- Add phase-level assessment endpoint returning `allowed`, blockers, and expandable dependency detail payload.
- Extend project summary payload with paused-run visibility fields needed by dashboard cards and project-level control bar.
- Align response types in orchestrator/server type definitions.

Do not implement full policy UI here; only provide complete API contract and deterministic outputs.
  </action>
  <verify>pnpm -C apps/server typecheck && pnpm -C apps/server test --run</verify>
  <done>Server exposes orchestration gating + paused visibility data needed by downstream plans</done>
</task>

</tasks>

<verification>
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/server typecheck`
- `pnpm -C apps/web test --run`
- `pnpm -C apps/server test --run`
</verification>

<success_criteria>
- One shared orchestration state model drives top bar and per-page strips
- Lifecycle actions route through a unified control hook
- Server exposes dependency/parallelism assessment contract
- Project payload includes paused-run visibility baseline
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-ui-controls-gsd-end-to-end-orchestration/18.1-01-SUMMARY.md`
</output>
