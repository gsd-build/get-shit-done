---
phase: 09-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "User can see Upstream Sync subsection in README GSD Enhancements"
    - "User understands what sync features exist from README summary"
    - "User knows to check USER-GUIDE.md for full details"
  artifacts:
    - path: "README.md"
      provides: "Upstream Sync summary in GSD Enhancements"
      contains: "### Upstream Sync"
  key_links:
    - from: "README.md"
      to: "docs/USER-GUIDE.md"
      via: "User Guide link"
      pattern: "User Guide"
---

<objective>
Add Upstream Sync section to README.md GSD Enhancements

Purpose: Provide a concise summary of upstream sync features for README readers, with link to full USER-GUIDE.md documentation.

Output: README.md with Upstream Sync subsection under GSD Enhancements, matching the Worktree Isolation pattern.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation/09-RESEARCH.md
@README.md

<interfaces>
<!-- Existing GSD Enhancements pattern from README.md -->

```markdown
## GSD Enhancements

### Worktree Isolation (v1.x)

**Parallel phase execution through git worktree isolation** — multiple AI sessions can work on different phases simultaneously without file conflicts.

| Feature | Description |
|---------|-------------|
| **Phase Worktrees** | Each phase gets its own isolated worktree with a dedicated branch |
...

**New Commands:**

| Command | What it does |
|---------|--------------|
| `/gsd:health` | Diagnose and fix worktree health issues interactively |
...

**Configuration:**
[config json block]

**Exit Codes** (for CI integration):
[list]
```

New section should follow same pattern:
- `### Upstream Sync (v1.1)`
- Bold tagline with em-dash
- Feature table
- New Commands table
- Link to User Guide for full details
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Upstream Sync section to README GSD Enhancements</name>
  <files>README.md</files>
  <action>
Add a new "### Upstream Sync (v1.1)" subsection to the GSD Enhancements section in README.md.

Insert AFTER the Worktree Isolation section (after the Exit Codes list, before the `---` horizontal rule that leads to Community Ports).

Add this content:

```markdown
### Upstream Sync (v1.1)

**Stay current with upstream while preserving fork customizations** — intelligent sync tooling that makes fork maintenance painless.

| Feature | Description |
|---------|-------------|
| **Fetch & Status** | Check commits behind upstream without modifying local branches |
| **Commit Analysis** | View changes grouped by directory or conventional commit type |
| **Conflict Preview** | Predict merge conflicts with risk scoring (EASY/MODERATE/HARD) |
| **Structural Detection** | Detect rename/delete conflicts requiring acknowledgment |
| **Safe Merge** | Automatic backup branch, atomic rollback on failure |
| **Post-Merge Verification** | Run tests on fork-modified files to catch regressions |

**New Commands:**

| Command | What it does |
|---------|--------------|
| `/gsd:sync-analyze` | Show upstream commits grouped by directory or feature |
| `/gsd:sync-preview` | Preview conflicts with risk assessment before merge |
| `/gsd:sync-resolve` | Address structural conflicts (renames/deletes) |

**CLI Commands:**

| Command | What it does |
|---------|--------------|
| `gsd-tools upstream configure <url>` | Set upstream remote (auto-detects existing `upstream`) |
| `gsd-tools upstream fetch` | Fetch upstream changes (24-hour cache) |
| `gsd-tools upstream status` | Show commits behind and last fetch time |
| `gsd-tools upstream merge` | Merge upstream with backup and verification |
| `gsd-tools upstream abort` | Abort incomplete sync and restore state |

See the [User Guide](docs/USER-GUIDE.md#gsd-enhancements) for full workflow documentation and Mermaid diagrams.
```

Ensure the formatting matches the existing Worktree Isolation section exactly:
- Same heading level (`###`)
- Same table formats
- Same bold + em-dash pattern for tagline
- Tables use `| Header | Header |` format with header separator row
  </action>
  <verify>grep -c "Upstream Sync" README.md returns at least 1 && grep -c "gsd-tools upstream" README.md returns at least 3</verify>
  <done>README.md contains Upstream Sync (v1.1) section with feature table, new commands table, CLI commands table, and link to User Guide</done>
</task>

</tasks>

<verification>
After task completes:
1. README.md contains "### Upstream Sync (v1.1)" section
2. Section appears after Worktree Isolation, before Community Ports
3. Feature table and command tables render correctly
4. Link to User Guide is present
5. Formatting matches existing GSD Enhancements style
</verification>

<success_criteria>
1. README GSD Enhancements section includes Upstream Sync subsection
2. Users can quickly understand what sync features exist from the summary
3. Users are directed to USER-GUIDE.md for full documentation
4. Section style matches existing Worktree Isolation pattern
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-02-SUMMARY.md`
</output>
