---
phase: 17-execute-phase-ui
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/features/execute/ToolCard.tsx
  - apps/web/src/components/features/execute/ToolCard.test.tsx
  - apps/web/src/components/features/execute/ToolCardList.tsx
  - apps/web/src/components/ui/CodePreview.tsx
autonomous: true
requirements: [EXEC-02]

must_haves:
  truths:
    - "Tool calls display as collapsible cards with tool type icons"
    - "File operations show syntax-highlighted code preview"
    - "Bash output streams with auto-scroll"
    - "Cards show live elapsed timer while running, final duration when complete"
  artifacts:
    - path: "apps/web/src/components/features/execute/ToolCard.tsx"
      provides: "Collapsible tool call card"
      exports: ["ToolCard"]
    - path: "apps/web/src/components/ui/CodePreview.tsx"
      provides: "Syntax-highlighted code with truncation"
      exports: ["CodePreview"]
  key_links:
    - from: "ToolCard"
      to: "CodePreview"
      via: "renders for Read/Write tools"
      pattern: "toolName.*Read|Write.*CodePreview"
---

<objective>
Build tool call visualization with collapsible cards, icons, and code preview.

Purpose: Show tool execution details per CONTEXT.md (collapsible cards with icons, syntax highlighting, live timers).
Output: ToolCard component with code preview for file operations and streaming for Bash.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-execute-phase-ui/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for tool card components (RED)</name>
  <files>apps/web/src/components/features/execute/ToolCard.test.tsx</files>
  <action>
Create test file defining expected ToolCard behavior:

1. **Icon tests:**
   - Read tool shows document icon
   - Write tool shows pencil icon
   - Bash tool shows terminal icon
   - Edit tool shows edit icon
   - Glob tool shows search icon
   - Grep tool shows search icon

2. **Collapse/expand tests:**
   - Card is collapsed by default
   - Clicking header expands card
   - Clicking again collapses
   - Expanded card shows tool input/output

3. **Timing tests:**
   - Running tool shows live elapsed timer
   - Completed tool shows "2.3s" duration
   - Error tool shows error indicator

4. **Content tests:**
   - Read/Write tools show CodePreview with file content
   - Bash tool shows output in scrollable container
   - Code preview truncates at ~10 lines with "show more" link
   - "show more" expands to full content

5. **Status tests:**
   - Running tool has blue border/accent
   - Success tool has green checkmark
   - Error tool has red X and error message

Use @testing-library/react and mock tool data.
  </action>
  <verify>pnpm --filter @gsd/web test -- ToolCard.test --run 2>&1 | grep -E "(FAIL|Error|Cannot find)"</verify>
  <done>ToolCard tests exist and fail because component doesn't exist</done>
</task>

<task type="auto">
  <name>Task 2: Implement ToolCard and CodePreview components (GREEN)</name>
  <files>apps/web/src/components/features/execute/ToolCard.tsx, apps/web/src/components/ui/CodePreview.tsx, apps/web/src/components/features/execute/ToolCardList.tsx</files>
  <action>
**CodePreview.tsx:**
- Props: content (string), language (string), maxLines (number, default 10)
- Uses `prism-react-renderer` or simple pre with syntax highlighting via Tailwind
- Truncates to maxLines with "Show more" button
- Language detection from file extension

**ToolCard.tsx:**
- Props: tool (ToolCall from store types)
- Accordion pattern using Radix Accordion or custom useState
- Header shows: icon, tool name, status indicator, duration
- Icon mapping:
  - Read: FileText from lucide-react
  - Write: Pencil
  - Bash: Terminal
  - Edit: FileEdit
  - Glob: Search
  - Grep: FileSearch

- Body content based on tool type:
  - Read/Write/Edit: CodePreview with file content
  - Bash: LogStream-style scrollable output
  - Glob/Grep: List of file paths

- Live timer for running tools (reuse pattern from PlanCard)
- Status indicators: running (blue spinner), success (green check), error (red x)

**ToolCardList.tsx:**
- Renders list of ToolCard components
- Groups by status (running first, then completed)

Per CONTEXT.md:
- "Syntax-highlighted code preview for file operations (Read/Write/Edit), truncated with show more"
- "Live streaming with auto-scroll for Bash output"
- "Live elapsed timer while tool runs, final duration shown on completion"
  </action>
  <verify>pnpm --filter @gsd/web test -- ToolCard.test --run</verify>
  <done>All ToolCard tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Add dependencies and polish tool cards</name>
  <files>apps/web/package.json, apps/web/src/components/features/execute/ToolCard.tsx</files>
  <action>
1. Install required dependencies in apps/web:
   ```bash
   pnpm --filter @gsd/web add lucide-react @radix-ui/react-accordion
   ```

2. Polish ToolCard styling:
   - Proper spacing and borders per Tailwind design system
   - Dark mode support via CSS variables
   - Hover states on header
   - Smooth expand/collapse animation

3. Add keyboard navigation:
   - Enter/Space to toggle expand
   - Proper focus states

4. Update test for accessibility:
   - Test aria-expanded attribute
   - Test keyboard navigation
  </action>
  <verify>pnpm --filter @gsd/web test -- ToolCard.test --run && pnpm --filter @gsd/web typecheck</verify>
  <done>Tool cards fully functional with accessibility and styling</done>
</task>

</tasks>

<verification>
- All tests pass: `pnpm --filter @gsd/web test -- ToolCard --run`
- TypeScript compiles: `pnpm --filter @gsd/web typecheck`
- Dependencies installed: lucide-react, @radix-ui/react-accordion in package.json
</verification>

<success_criteria>
- Tool calls render as collapsible cards with appropriate icons
- Read/Write/Edit tools show syntax-highlighted code preview
- Bash tools show streaming output with auto-scroll
- Live timer while running, final duration when complete
- Keyboard accessible with proper ARIA attributes
</success_criteria>

<output>
After completion, create `.planning/phases/17-execute-phase-ui/17-03-SUMMARY.md`
</output>
