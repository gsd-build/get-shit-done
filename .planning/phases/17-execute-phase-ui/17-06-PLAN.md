---
phase: 17-execute-phase-ui
plan: 06
type: tdd
wave: 2
depends_on: [17-01]
files_modified:
  - apps/web/src/components/features/execute/ExecutionControls.tsx
  - apps/web/src/components/features/execute/ExecutionControls.test.tsx
  - apps/web/src/components/features/execute/AbortConfirmDialog.tsx
  - apps/web/src/components/features/execute/AbortConfirmDialog.test.tsx
  - apps/web/src/hooks/useAgentControl.ts
  - apps/web/src/hooks/useAgentControl.test.ts
autonomous: true
requirements: [EXEC-06, EXEC-07]

must_haves:
  truths:
    - "User can pause execution and resume"
    - "User can abort execution with confirmation dialog"
    - "Abort dialog shows files modified and commits made"
    - "Rollback option available in abort dialog"
  artifacts:
    - path: "apps/web/src/components/features/execute/ExecutionControls.tsx"
      provides: "Fixed header control bar"
      exports: ["ExecutionControls"]
    - path: "apps/web/src/components/features/execute/AbortConfirmDialog.tsx"
      provides: "Abort confirmation modal"
      exports: ["AbortConfirmDialog"]
  key_links:
    - from: "ExecutionControls"
      to: "REST API"
      via: "PATCH /api/agents/:id"
      pattern: "fetch.*api/agents.*PATCH"
    - from: "AbortConfirmDialog"
      to: "REST API"
      via: "DELETE /api/agents/:id"
      pattern: "fetch.*api/agents.*DELETE"
---

<objective>
Build pause/resume and abort controls for execution management.

Purpose: Allow users to control execution flow per CONTEXT.md (fixed header bar, abort with rollback option).
Output: ExecutionControls header and AbortConfirmDialog.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-execute-phase-ui/17-RESEARCH.md
@.planning/phases/17-execute-phase-ui/17-01-SUMMARY.md

<interfaces>
From Phase 14 Backend:
```typescript
// REST API endpoints
POST /api/agents         - Start new agent
GET /api/agents          - List agents
GET /api/agents/:id      - Get agent status
DELETE /api/agents/:id   - Cancel agent

// PATCH endpoint needed for pause/resume (Open Question from RESEARCH.md)
// May need to be added: PATCH /api/agents/:id with { status: 'paused' | 'running' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for control components (RED)</name>
  <files>apps/web/src/components/features/execute/ExecutionControls.test.tsx, apps/web/src/components/features/execute/AbortConfirmDialog.test.tsx</files>
  <action>
Create test files defining expected component behavior:

**ExecutionControls.test.tsx:**
1. Renders in fixed position at top
2. Shows Pause button when status is 'running'
3. Shows Resume button when status is 'paused'
4. Shows Abort button always (enabled when running/paused)
5. Pause button disabled when not running
6. Clicking Pause calls onPause callback
7. Clicking Resume calls onResume callback
8. Clicking Abort opens AbortConfirmDialog
9. Shows current plan name and status

**AbortConfirmDialog.test.tsx:**
1. Renders when open prop is true
2. Shows list of files modified during execution
3. Shows number of commits made
4. Has "Cancel" button that closes dialog
5. Has "Abort" button that calls onConfirm
6. Has "Abort & Rollback" button that calls onConfirm with rollback flag
7. Rollback button only shown when commits exist
8. Dialog is modal (blocks background interaction)

Mock fetch for API calls.
  </action>
  <verify>pnpm --filter @gsd/web test -- "(ExecutionControls|AbortConfirmDialog).test" --run 2>&1 | grep -E "(FAIL|Error|Cannot find)"</verify>
  <done>Tests exist and fail because components don't exist</done>
</task>

<task type="auto">
  <name>Task 2: Implement useAgentControl hook</name>
  <files>apps/web/src/hooks/useAgentControl.ts, apps/web/src/hooks/useAgentControl.test.ts</files>
  <action>
**useAgentControl.ts:**
- Takes agentId (string | null)
- Returns: { pause, resume, abort, isLoading, error }

Functions:
- pause(): PATCH /api/agents/:id with { status: 'paused' }
  - Updates executionStore.status to 'paused'
- resume(): PATCH /api/agents/:id with { status: 'running' }
  - Updates executionStore.status to 'running'
- abort(rollback?: boolean): DELETE /api/agents/:id?rollback=true
  - If rollback, includes query param
  - Resets executionStore

Note: PATCH endpoint may not exist yet. Create the hook to call the expected endpoint. If backend doesn't support pause/resume, the API call will fail gracefully.

**useAgentControl.test.ts:**
- Mock fetch with vi.fn()
- Test pause() makes PATCH request with paused status
- Test resume() makes PATCH request with running status
- Test abort() makes DELETE request
- Test abort(true) makes DELETE request with rollback query param
- Test error handling for failed requests
  </action>
  <verify>pnpm --filter @gsd/web test -- useAgentControl.test --run</verify>
  <done>Agent control hook handles pause/resume/abort API calls</done>
</task>

<task type="auto">
  <name>Task 3: Implement ExecutionControls and AbortConfirmDialog (GREEN)</name>
  <files>apps/web/src/components/features/execute/ExecutionControls.tsx, apps/web/src/components/features/execute/AbortConfirmDialog.tsx</files>
  <action>
**ExecutionControls.tsx:**
- Fixed position header bar (position: sticky, top: 0, z-index: 50)
- Uses useAgentControl hook for actions
- Reads status from executionStore
- Layout per CONTEXT.md: "Fixed header bar for pause/resume/abort controls - always visible at top of execution panel"

Buttons:
- Pause (Pause icon): visible when running, calls control.pause()
- Resume (Play icon): visible when paused, calls control.resume()
- Abort (X icon): always visible, opens abort dialog

Status display:
- Current plan name
- Status badge (running/paused/complete/error)
- Elapsed time for current execution

**AbortConfirmDialog.tsx:**
- Uses @radix-ui/react-dialog
- Props: open, onClose, onConfirm (with rollback boolean), filesModified, commitCount
- Per CONTEXT.md: "Confirmation dialog for abort - shows files modified and commits made, offers rollback option"

Content:
- Warning icon and "Abort Execution?" title
- List of files modified (max 10, then "+N more")
- "{commitCount} commits will be kept" message
- If commitCount > 0: "Abort & Rollback" button (red, destructive)
- "Abort" button (orange)
- "Cancel" button

Per CONTEXT.md color semantics:
- Abort with rollback is destructive (red)
- Abort without rollback is cautionary (orange/yellow)
  </action>
  <verify>pnpm --filter @gsd/web test -- "(ExecutionControls|AbortConfirmDialog).test" --run</verify>
  <done>Execution controls work with pause/resume/abort functionality</done>
</task>

</tasks>

<verification>
- All tests pass: `pnpm --filter @gsd/web test -- "(ExecutionControls|AbortConfirmDialog|useAgentControl)" --run`
- TypeScript compiles: `pnpm --filter @gsd/web typecheck`
- Controls render in fixed header position
</verification>

<success_criteria>
- Pause/Resume buttons toggle based on execution status
- Abort button opens confirmation dialog
- Abort dialog shows modified files and commit count
- Rollback option available when commits exist
- API calls made correctly to backend endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/17-execute-phase-ui/17-06-SUMMARY.md`
</output>
