---
phase: 17-execute-phase-ui
plan: 05
type: tdd
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - apps/web/src/components/features/execute/DiffPanel.tsx
  - apps/web/src/components/features/execute/DiffPanel.test.tsx
  - apps/web/src/components/features/execute/DiffEditor.tsx
  - apps/web/src/components/features/execute/FileTree.tsx
  - apps/web/src/components/features/execute/CommitTimeline.tsx
  - apps/web/src/components/features/execute/CommitTimeline.test.tsx
autonomous: true
requirements: [EXEC-04, EXEC-05]

must_haves:
  truths:
    - "User can view file changes in Monaco DiffEditor"
    - "Toggle between unified and side-by-side diff views"
    - "Files grouped by directory tree in diff panel"
    - "Git commit timeline shows commits made during execution"
  artifacts:
    - path: "apps/web/src/components/features/execute/DiffPanel.tsx"
      provides: "Right sidebar diff panel"
      exports: ["DiffPanel"]
    - path: "apps/web/src/components/features/execute/DiffEditor.tsx"
      provides: "Monaco diff editor wrapper"
      exports: ["DiffEditor"]
    - path: "apps/web/src/components/features/execute/CommitTimeline.tsx"
      provides: "Collapsible commit list"
      exports: ["CommitTimeline"]
  key_links:
    - from: "DiffPanel"
      to: "DiffEditor"
      via: "renders Monaco editor"
      pattern: "selectedFile.*DiffEditor"
    - from: "DiffPanel"
      to: "executionStore"
      via: "reads selectedFile"
      pattern: "useExecutionStore.*selectedFile"
---

<objective>
Build Monaco DiffEditor integration and git commit timeline for file change visualization.

Purpose: Show file changes per CONTEXT.md (Monaco diff, unified/side-by-side toggle, commit timeline).
Output: DiffPanel with Monaco editor, FileTree navigation, and CommitTimeline.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-execute-phase-ui/17-RESEARCH.md
@.planning/phases/17-execute-phase-ui/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for diff components (RED)</name>
  <files>apps/web/src/components/features/execute/DiffPanel.test.tsx, apps/web/src/components/features/execute/CommitTimeline.test.tsx</files>
  <action>
Create test files defining expected component behavior:

**DiffPanel.test.tsx:**
1. Renders "No file selected" when selectedFile is null
2. Renders DiffEditor when selectedFile is provided
3. Shows file path in header
4. Toggle button switches between "Unified" and "Side-by-side"
5. Default view is unified per CONTEXT.md
6. FileTree component renders modified files grouped by directory

**CommitTimeline.test.tsx:**
1. Renders "X commits made [View]" header (collapsed by default)
2. Clicking [View] expands to show commit list
3. Each commit shows: sha (abbreviated), message, relative timestamp
4. Empty commits array shows "No commits yet"
5. Commits sorted newest first

Note: Monaco editor cannot be fully tested in jsdom. Mock @monaco-editor/react:
```typescript
vi.mock('@monaco-editor/react', () => ({
  DiffEditor: ({ original, modified, language }: any) => (
    <div data-testid="monaco-diff" data-language={language}>
      <div data-testid="original">{original}</div>
      <div data-testid="modified">{modified}</div>
    </div>
  ),
}));
```
  </action>
  <verify>pnpm --filter @gsd/web test -- "(DiffPanel|CommitTimeline).test" --run 2>&1 | grep -E "(FAIL|Error|Cannot find)"</verify>
  <done>Tests exist and fail because components don't exist</done>
</task>

<task type="auto">
  <name>Task 2: Implement DiffPanel and DiffEditor components (GREEN)</name>
  <files>apps/web/src/components/features/execute/DiffPanel.tsx, apps/web/src/components/features/execute/DiffEditor.tsx, apps/web/src/components/features/execute/FileTree.tsx</files>
  <action>
Install @monaco-editor/react:
```bash
pnpm --filter @gsd/web add @monaco-editor/react
```

**DiffEditor.tsx:**
- Props: original (string), modified (string), filePath (string)
- Uses DiffEditor from @monaco-editor/react
- Language detection from file extension (ts, tsx, js, jsx, json, md, css, etc.)
- useState for sideBySide (default false per CONTEXT.md)
- Theme synced with dark mode via next-themes useTheme

**FileTree.tsx:**
- Props: files (Array<{ path: string; status: 'added' | 'modified' | 'deleted' }>), onSelect (path) => void
- Groups files by directory
- Expandable directory nodes
- File icons based on status (green +, yellow ~, red -)
- Clicking file calls onSelect

**DiffPanel.tsx:**
- Reads selectedFile from executionStore
- Renders header with file path and unified/side-by-side toggle
- Renders FileTree for file navigation
- Renders DiffEditor with original/modified content
- Empty state when no file selected

Per CONTEXT.md:
- "Toggle between unified and side-by-side (Monaco) diff views - default to unified"
- "Right sidebar for diff panel - updates based on selected file from tool cards"
- "Files grouped by directory tree in diff panel"
  </action>
  <verify>pnpm --filter @gsd/web test -- DiffPanel.test --run</verify>
  <done>DiffPanel renders with Monaco editor and file tree</done>
</task>

<task type="auto">
  <name>Task 3: Implement CommitTimeline component (GREEN)</name>
  <files>apps/web/src/components/features/execute/CommitTimeline.tsx</files>
  <action>
**CommitTimeline.tsx:**
- Props: commits (from executionStore)
- Uses @radix-ui/react-accordion or simple useState for collapse
- Header: "{commits.length} commits made" with [View] toggle
- Hidden by default per CONTEXT.md: "Collapsible section for commits: 'X commits made [View]', hidden by default"

Expanded view:
- List of commits with:
  - SHA (first 7 chars, monospace font)
  - Message (truncated to ~50 chars with ellipsis)
  - Relative timestamp using helper function or date-fns formatDistanceToNow

Styling:
- Subtle border/background
- Git-style vertical timeline line on left
- Commit dots on timeline

Integration:
- Reads commits from executionStore
- addCommit action called when backend emits git events (future: may need new event type)
  </action>
  <verify>pnpm --filter @gsd/web test -- CommitTimeline.test --run</verify>
  <done>CommitTimeline renders with collapsible commit list</done>
</task>

</tasks>

<verification>
- All tests pass: `pnpm --filter @gsd/web test -- "(DiffPanel|CommitTimeline)" --run`
- TypeScript compiles: `pnpm --filter @gsd/web typecheck`
- Monaco editor loads (visual verification in browser)
</verification>

<success_criteria>
- DiffPanel renders Monaco DiffEditor with syntax highlighting
- Toggle switches between unified and side-by-side views
- FileTree groups files by directory with expand/collapse
- CommitTimeline shows commit count, expands to show list
- Theme syncs with app dark/light mode
</success_criteria>

<output>
After completion, create `.planning/phases/17-execute-phase-ui/17-05-SUMMARY.md`
</output>
