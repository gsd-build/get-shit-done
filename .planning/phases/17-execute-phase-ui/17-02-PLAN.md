---
phase: 17-execute-phase-ui
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/features/execute/PipelineView.tsx
  - apps/web/src/components/features/execute/PipelineView.test.tsx
  - apps/web/src/components/features/execute/WaveColumn.tsx
  - apps/web/src/components/features/execute/PlanCard.tsx
  - apps/web/src/components/features/execute/PlanCard.test.tsx
  - apps/web/src/components/features/execute/LogStream.tsx
  - apps/web/src/components/features/execute/LogStream.test.tsx
autonomous: true
requirements: [EXEC-01]

must_haves:
  truths:
    - "User sees waves as horizontal columns flowing left-to-right"
    - "Each wave column contains plan cards stacked vertically"
    - "Active plans auto-expand to show logs, completed plans collapse"
    - "Plan cards show name, status indicator, elapsed time"
    - "Log stream auto-scrolls but pauses when user scrolls up"
  artifacts:
    - path: "apps/web/src/components/features/execute/PipelineView.tsx"
      provides: "Horizontal wave pipeline layout"
      exports: ["PipelineView"]
    - path: "apps/web/src/components/features/execute/PlanCard.tsx"
      provides: "Expandable plan card with status and timer"
      exports: ["PlanCard"]
    - path: "apps/web/src/components/features/execute/LogStream.tsx"
      provides: "Auto-scrolling log viewer"
      exports: ["LogStream"]
  key_links:
    - from: "PipelineView"
      to: "WaveColumn"
      via: "maps waves to columns"
      pattern: "waves\\.map.*WaveColumn"
    - from: "PlanCard"
      to: "LogStream"
      via: "renders logs when expanded"
      pattern: "isExpanded.*LogStream"
---

<objective>
Build the wave-based pipeline visualization with plan cards and log streaming.

Purpose: Display execution progress in a CI-style horizontal pipeline per CONTEXT.md locked decision.
Output: PipelineView, WaveColumn, PlanCard, and LogStream components with tests.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-execute-phase-ui/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for pipeline components (RED)</name>
  <files>apps/web/src/components/features/execute/PipelineView.test.tsx, apps/web/src/components/features/execute/PlanCard.test.tsx, apps/web/src/components/features/execute/LogStream.test.tsx</files>
  <action>
Create test files defining expected component behavior:

**PipelineView.test.tsx:**
1. Renders wave columns for each wave in data
2. Waves render as horizontal grid columns (CSS grid-flow-col)
3. Empty waves array renders empty state
4. Aria labels for accessibility

**PlanCard.test.tsx:**
1. Renders plan name and status indicator
2. Shows elapsed time (live timer for running, "completed in Xs" for done)
3. Auto-expands when status is 'running'
4. Auto-collapses when status is 'complete'
5. Manual expand/collapse toggle works
6. Expanded state shows LogStream component
7. Status indicator colors: running=blue, complete=green, error=red, pending=gray

**LogStream.test.tsx:**
1. Renders log content in pre element
2. Auto-scrolls to bottom when logs update (if autoScroll true)
3. Pauses auto-scroll when user scrolls up
4. Shows "Resume auto-scroll" button when paused and streaming
5. Clicking resume button re-enables auto-scroll

Use @testing-library/react render, screen, fireEvent, act.
Mock scrollHeight, scrollTop, clientHeight for scroll tests.
  </action>
  <verify>pnpm --filter @gsd/web test -- "(PipelineView|PlanCard|LogStream).test" --run 2>&1 | grep -E "(FAIL|Error|Cannot find)"</verify>
  <done>All component tests exist and fail because components don't exist yet</done>
</task>

<task type="auto">
  <name>Task 2: Implement pipeline components to pass tests (GREEN)</name>
  <files>apps/web/src/components/features/execute/PipelineView.tsx, apps/web/src/components/features/execute/WaveColumn.tsx, apps/web/src/components/features/execute/PlanCard.tsx, apps/web/src/components/features/execute/LogStream.tsx</files>
  <action>
Implement components to pass all tests:

**PipelineView.tsx:**
- Uses CSS Grid with auto-cols-[280px] grid-flow-col for horizontal layout
- Maps waves to WaveColumn components
- Horizontal overflow-x-auto for scrolling when many waves
- aria-label="Execution waves" for accessibility

**WaveColumn.tsx:**
- Displays wave number header
- Stacks PlanCard components vertically
- Status indicator for wave (pending/running/complete)

**PlanCard.tsx:**
- Expandable card with click handler
- Shows: plan name, status badge, elapsed time
- Uses useState for expanded state
- Auto-expands via useEffect when status becomes 'running'
- Auto-collapses via useEffect when status becomes 'complete'
- Renders LogStream when expanded
- Live timer using setInterval for running plans
- Tailwind classes for styling

**LogStream.tsx:**
- ref on container div for scroll manipulation
- onScroll handler detects user scroll up
- useEffect scrolls to bottom when autoScroll && logs change
- "Resume auto-scroll" button appears when !autoScroll && isStreaming
- pre element with whitespace-pre-wrap for log content
- font-mono text-sm styling

Per CONTEXT.md: Use 'use client' directive. Follow Tailwind v4 patterns.
  </action>
  <verify>pnpm --filter @gsd/web test -- "(PipelineView|PlanCard|LogStream).test" --run</verify>
  <done>All pipeline component tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Add elapsed time timer and status indicators</name>
  <files>apps/web/src/components/features/execute/PlanCard.tsx, apps/web/src/components/ui/StatusBadge.tsx</files>
  <action>
1. Create StatusBadge.tsx reusable component:
   - Props: status ('pending' | 'running' | 'complete' | 'error')
   - Color mapping per CONTEXT.md patterns
   - Optional pulsing animation for 'running'

2. Enhance PlanCard.tsx elapsed time:
   - startTime stored in plan data
   - Live timer updates every 100ms while running
   - Format: "0:00" for running, "completed in 2.3s" for complete
   - Clear interval on unmount and when status changes

3. Add integration test:
   - Verify timer increments while running
   - Verify timer stops when completed
  </action>
  <verify>pnpm --filter @gsd/web test -- PlanCard.test --run</verify>
  <done>Elapsed time timer works correctly, status badges render with proper colors</done>
</task>

</tasks>

<verification>
- All component tests pass: `pnpm --filter @gsd/web test -- "(PipelineView|PlanCard|LogStream)" --run`
- TypeScript compiles: `pnpm --filter @gsd/web typecheck`
- Visual check: components render horizontal pipeline layout
</verification>

<success_criteria>
- PipelineView renders waves as horizontal columns (CSS Grid)
- Plan cards show name, status badge, elapsed time
- Active plans auto-expand, completed plans auto-collapse
- LogStream auto-scrolls with user scroll-up pause detection
- All tests pass with 80%+ coverage
</success_criteria>

<output>
After completion, create `.planning/phases/17-execute-phase-ui/17-02-SUMMARY.md`
</output>
