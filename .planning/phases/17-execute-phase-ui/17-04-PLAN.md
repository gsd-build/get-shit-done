---
phase: 17-execute-phase-ui
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/features/execute/CheckpointModal.tsx
  - apps/web/src/components/features/execute/CheckpointModal.test.tsx
  - apps/web/src/components/features/execute/CountdownTimer.tsx
  - apps/web/src/hooks/useCheckpointResponse.ts
autonomous: true
requirements: [EXEC-03]

must_haves:
  truths:
    - "Checkpoint modal blocks interaction until answered"
    - "Modal shows timeout countdown with color transitions"
    - "User can respond via options or free-form input"
    - "Response is sent to backend via Socket.IO"
  artifacts:
    - path: "apps/web/src/components/features/execute/CheckpointModal.tsx"
      provides: "Blocking checkpoint dialog"
      exports: ["CheckpointModal"]
    - path: "apps/web/src/components/features/execute/CountdownTimer.tsx"
      provides: "Visual countdown with color changes"
      exports: ["CountdownTimer"]
  key_links:
    - from: "CheckpointModal"
      to: "socket"
      via: "checkpoint:response event"
      pattern: "socket\\.emit.*CHECKPOINT_RESPONSE"
---

<objective>
Build checkpoint dialog modal with countdown timer and response handling.

Purpose: Enable users to respond to checkpoint requests with timeout awareness per CONTEXT.md locked decisions.
Output: CheckpointModal with CountdownTimer and response submission.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-execute-phase-ui/17-RESEARCH.md

<interfaces>
From packages/events/src/types.ts:
```typescript
export interface CheckpointRequestEvent {
  checkpointId: string;
  type: 'human-verify' | 'decision' | 'human-action';
  prompt: string;
  options?: CheckpointOption[];
  timeoutMs?: number;
}

export interface CheckpointOption {
  id: string;
  label: string;
}

export interface CheckpointResponseEvent {
  checkpointId: string;
  response: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for checkpoint components (RED)</name>
  <files>apps/web/src/components/features/execute/CheckpointModal.test.tsx</files>
  <action>
Create test file defining expected CheckpointModal behavior:

1. **Visibility tests:**
   - Modal renders when checkpoint prop is provided
   - Modal not rendered when checkpoint is null
   - Modal has proper overlay blocking background

2. **Content tests:**
   - Shows checkpoint prompt text
   - Shows "Checkpoint Required" title
   - For checkpoints with options: renders option buttons
   - For checkpoints without options: renders text input

3. **Countdown tests:**
   - Shows countdown timer when timeoutMs provided
   - Timer color is green when > 30s remaining
   - Timer color is yellow when <= 30s remaining
   - Timer color is red/pulsing when <= 10s remaining
   - No timer shown when timeoutMs not provided

4. **Response tests:**
   - Clicking option button calls onRespond with option.id
   - Pressing Enter in text input calls onRespond with input value
   - After response, modal calls onRespond (doesn't close itself - parent handles)

5. **Accessibility tests:**
   - Modal has role="dialog"
   - Modal has aria-modal="true"
   - Focus trapped within modal
   - First interactive element receives focus

Use @testing-library/react and mock props.
  </action>
  <verify>pnpm --filter @gsd/web test -- CheckpointModal.test --run 2>&1 | grep -E "(FAIL|Error|Cannot find)"</verify>
  <done>CheckpointModal tests exist and fail because component doesn't exist</done>
</task>

<task type="auto">
  <name>Task 2: Implement CheckpointModal and CountdownTimer (GREEN)</name>
  <files>apps/web/src/components/features/execute/CheckpointModal.tsx, apps/web/src/components/features/execute/CountdownTimer.tsx</files>
  <action>
**CountdownTimer.tsx:**
- Props: durationMs (number), onTimeout (callback)
- Uses react-countdown-circle-timer (already in 17-RESEARCH.md recommendation)
- Color transitions: green -> yellow (30s) -> red (10s)
- Pulsing animation when <= 10s
- Calls onTimeout when reaches 0

If react-countdown-circle-timer not available, implement custom:
- useState for remaining time
- useEffect with setInterval (100ms precision)
- SVG circle with stroke-dashoffset for progress
- CSS animation for pulse effect

**CheckpointModal.tsx:**
- Props: checkpoint (CheckpointRequestEvent | null), onRespond (response: string) => void
- Uses @radix-ui/react-dialog for modal primitives
- Portal renders to document body
- Overlay with bg-black/50
- Centered content panel

Layout per CONTEXT.md:
- Title: "Checkpoint Required"
- Countdown timer in flex row with prompt
- Prompt text
- Options as button group OR text input

Response handling:
- Option click: onRespond(option.id)
- Text input: onKeyDown Enter -> onRespond(value)
- Disable buttons after click to prevent double-submit
  </action>
  <verify>pnpm --filter @gsd/web test -- CheckpointModal.test --run</verify>
  <done>CheckpointModal tests pass with proper countdown and response handling</done>
</task>

<task type="auto">
  <name>Task 3: Add useCheckpointResponse hook for Socket.IO integration</name>
  <files>apps/web/src/hooks/useCheckpointResponse.ts, apps/web/src/hooks/useCheckpointResponse.test.ts</files>
  <action>
**useCheckpointResponse.ts:**
- Takes socket (TypedSocket | null)
- Returns respondToCheckpoint(checkpointId: string, response: string) function
- Function emits EVENTS.CHECKPOINT_RESPONSE via socket
- Clears pending checkpoint from executionStore after emit

**useCheckpointResponse.test.ts:**
- Mock socket with vi.fn()
- Test: emits checkpoint:response event with correct payload
- Test: clears pendingCheckpoint from store after response
- Test: no-op when socket is null

Integration with CheckpointModal:
- Page component will use this hook
- Pass respondToCheckpoint as onRespond to CheckpointModal
  </action>
  <verify>pnpm --filter @gsd/web test -- useCheckpointResponse.test --run</verify>
  <done>Checkpoint response hook works and integrates with store</done>
</task>

</tasks>

<verification>
- All tests pass: `pnpm --filter @gsd/web test -- Checkpoint --run`
- TypeScript compiles: `pnpm --filter @gsd/web typecheck`
- Modal renders correctly with countdown
</verification>

<success_criteria>
- Modal blocks interaction until checkpoint is answered
- Countdown timer shows with color transitions (green > yellow > red)
- Options render as clickable buttons
- Free-form input accepts Enter key submission
- Response sent via Socket.IO CHECKPOINT_RESPONSE event
- Focus is trapped within modal
</success_criteria>

<output>
After completion, create `.planning/phases/17-execute-phase-ui/17-04-SUMMARY.md`
</output>
