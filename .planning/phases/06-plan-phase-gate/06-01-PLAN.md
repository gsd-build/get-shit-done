---
phase: 06-plan-phase-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/sme-gate-plan-phase.test.cjs
  - get-shit-done/workflows/plan-phase.md
  - commands/gsd/plan-phase.md
  - get-shit-done/references/gates.md
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
  - GATE-05
  - GATE-06
  - GATE-07
  - GATE-08
  - CONFIG-04

must_haves:
  truths:
    - "When use_sme_agents is false (default), the SME audit gate is completely skipped — no auditor is spawned, no warnings appear"
    - "When use_sme_agents is true and SME documents exist for the phase's processes, the gsd-sme-auditor is spawned with SME context blocks injected before the PLAN.md path"
    - "When use_sme_agents is true but no SME documents exist, a non-blocking warning with /gsd-create-sme instructions is displayed and the gate proceeds"
    - "In soft mode, SME_CONCERNS are displayed as warnings and planning continues without halting"
    - "In strict mode, SME_CONCERNS BLOCKERs halt planning and present the user with acknowledge-risk or revise-plan options"
    - "The --acknowledge-sme-risk flag overrides strict mode and allows planning to proceed with documented risk"
    - "The SME audit gate runs after the plan bounce step and before the requirements coverage gate"
  artifacts:
    - path: "tests/sme-gate-plan-phase.test.cjs"
      provides: "Structural validation for all 9 requirements (GATE-01..08 + CONFIG-04)"
      min_lines: 120
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "Step 12.6 SME Audit Gate workflow step"
      contains: "## 12.6. SME Audit Gate"
    - path: "commands/gsd/plan-phase.md"
      provides: "--acknowledge-sme-risk in argument-hint"
      contains: "--acknowledge-sme-risk"
    - path: "get-shit-done/references/gates.md"
      provides: "Gate Matrix row for SME audit gate"
      contains: "SME"
  key_links:
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "gsd-sdk query sme.detect-processes"
      via: "bash command in step 12.6"
      pattern: "sme\\.detect-processes"
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "gsd-sdk query sme.context-block"
      via: "bash command in step 12.6"
      pattern: "sme\\.context-block"
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "agents/gsd-sme-auditor.md"
      via: "Task() invocation in step 12.6"
      pattern: "gsd-sme-auditor"
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "commands/gsd/plan-phase.md"
      via: "--acknowledge-sme-risk flag parsed in step 2, declared in argument-hint"
      pattern: "acknowledge-sme-risk"
---

<objective>
TDD: Write structural tests (RED) then implement the plan-phase SME audit gate as step 12.6 (GREEN)

Purpose: This is the core product value of the SME Agent Framework. The gate integrates the auditor agent into the plan-phase workflow so domain-specific risks are caught before code gets written. It detects relevant processes, spawns the auditor with SME context, and routes on the auditor's return markers with per-process soft/strict blocking.

Output: Working step 12.6 in plan-phase.md, updated argument-hint, gates.md row, and structural tests proving all 9 requirements are met.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plan-phase-gate/06-RESEARCH.md
@.planning/phases/06-plan-phase-gate/06-PATTERNS.md

@.planning/phases/05-sme-auditor-agent/05-01-SUMMARY.md
@.planning/phases/02-sdk-query-handlers/02-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From agents/gsd-sme-auditor.md (Phase 5):
- Agent name: gsd-sme-auditor
- Tools: Read, Bash, Glob, Grep (read-only, no Write/Edit)
- Return markers: `## SME_APPROVED` or `## SME_CONCERNS`
- Accepts multiple `<sme_context>` blocks in prompt
- block_mode is gate logic, NOT auditor logic — auditor always reports all findings

From sdk/src/query/sme.ts (Phase 2, registered in index.ts):
- `gsd-sdk query sme.list` -> { enabled: boolean, smes: Array<{process_name, block_mode, file, ...}> }
- `gsd-sdk query sme.detect-processes --file-paths ... --goal "..."` -> { enabled: boolean, matches: Array<{process_name, block_mode, file}> }
- `gsd-sdk query sme.context-block {process_name}` -> { block: "<sme_context process='...' block_mode='...'>...</sme_context>" }

From get-shit-done/references/agent-contracts.md:
- Registry row: `| gsd-sme-auditor | SME domain audit | \`## SME_APPROVED\`, \`## SME_CONCERNS\` |`
- Return marker regex: `/^## SME_APPROVED/m`, `/^## SME_CONCERNS/m`

From get-shit-done/workflows/plan-phase.md step 2 (line 57):
```
Extract from $ARGUMENTS: phase number (integer or decimal like `2.1`), flags (`--research`, `--skip-research`, `--gaps`, `--skip-verify`, `--skip-ui`, `--prd <filepath>`, `--reviews`, `--text`, `--bounce`, `--skip-bounce`, `--chunked`).
```

From get-shit-done/workflows/plan-phase.md step 12.5 (line 1205):
```
## 12.5. Plan Bounce (Optional External Refinement)
```
Step 13 begins at line 1276:
```
## 13. Requirements Coverage Gate
```

From commands/gsd/plan-phase.md (line 4):
```
argument-hint: "[phase] [--auto] [--research] [--skip-research] [--gaps] [--skip-verify] [--prd <file>] [--reviews] [--text] [--tdd]"
```

From get-shit-done/references/gates.md Gate Matrix (lines 48-57):
```
| Workflow | Phase | Gate Type | Artifacts Checked | Failure Behavior |
|----------|-------|-----------|-------------------|------------------|
| plan-phase | Step 12 | Revision | PLAN.md quality | Loop to planner (max 3) |
| plan-phase | Post-revision | Escalation | Unresolved issues | Surface to developer |
```

From tests/plan-bounce.test.cjs (existing tests assert on `## 12.5` — DO NOT renumber):
```javascript
test('plan-phase.md contains step 12.5', () => {
    content.includes('## 12.5'),
```

From init.plan-phase response:
- checker_model: "sonnet" (reuse for auditor invocation)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write structural tests for SME audit gate</name>
  <files>tests/sme-gate-plan-phase.test.cjs</files>
  <read_first>
    - tests/plan-phase-ui-redirect.test.cjs (analog: CJS structural test for plan-phase.md)
    - tests/gates-taxonomy.test.cjs (analog: CJS structural test for gates.md)
    - tests/plan-bounce.test.cjs (analog: CJS structural test for step 12.5 — see pattern for positional assertions)
    - .planning/phases/06-plan-phase-gate/06-PATTERNS.md (complete pattern map with code excerpts)
    - .planning/phases/06-plan-phase-gate/06-RESEARCH.md (CJS Test Skeleton section)
  </read_first>
  <behavior>
    GATE-01 tests:
    - Test 1: plan-phase.md contains an "SME Audit Gate" heading
    - Test 2: SME Audit Gate appears AFTER "Plan Bounce" (step 12.5) in file position
    - Test 3: SME Audit Gate appears BEFORE "Requirements Coverage Gate" (step 13) in file position

    GATE-02 tests:
    - Test 4: plan-phase.md contains the string "sme.detect-processes"

    GATE-03 tests:
    - Test 5: plan-phase.md contains "gsd-sme-auditor" (auditor spawn reference)
    - Test 6: plan-phase.md contains "sme.context-block" (context block fetch)

    GATE-04 tests:
    - Test 7: plan-phase.md contains "soft" AND "proceed" in the SME gate section (soft mode path)

    GATE-05 tests:
    - Test 8: plan-phase.md contains "strict" AND "AskUserQuestion" in the SME gate section (strict mode halt path)

    GATE-06 tests:
    - Test 9: commands/gsd/plan-phase.md argument-hint contains "--acknowledge-sme-risk"
    - Test 10: plan-phase.md contains "--acknowledge-sme-risk" (parsed in step 2)
    - Test 11: plan-phase.md contains "ACKNOWLEDGE_SME_RISK" (variable set from flag)

    GATE-07 tests:
    - Test 12: plan-phase.md contains "/gsd-create-sme" in the SME gate section (instruction for missing SME)

    GATE-08 tests:
    - Test 13: In the auditor prompt template, "SME_CONTEXT_BLOCKS" appears before "PLAN.md path:" in file position

    CONFIG-04 tests:
    - Test 14: plan-phase.md contains "sme.list" in the SME gate section (check for existing documents)
    - Test 15: plan-phase.md contains "No SME documents" or "no SME documents" warning text

    Gates.md tests:
    - Test 16: gates.md Gate Matrix contains a row with "SME" and "plan-phase"
  </behavior>
  <action>
Create `tests/sme-gate-plan-phase.test.cjs` following the exact CJS structural test pattern from 06-PATTERNS.md.

File structure:
1. JSDoc banner listing all requirements covered (GATE-01..08, CONFIG-04)
2. `'use strict';`
3. Require `node:test`, `node:assert/strict`, `fs`, `path`
4. Constants:
   - `const ROOT = path.join(__dirname, '..');`
   - `const PLAN_PHASE = path.join(ROOT, 'get-shit-done', 'workflows', 'plan-phase.md');`
   - `const PLAN_PHASE_CMD = path.join(ROOT, 'commands', 'gsd', 'plan-phase.md');`
   - `const GATES_REF = path.join(ROOT, 'get-shit-done', 'references', 'gates.md');`
5. One `describe()` block per requirement group, each containing the tests from the behavior section above
6. Inside each `test()`: read file with `fs.readFileSync(filePath, 'utf-8')` (do NOT hoist reads to describe scope)
7. Use `assert.ok(content.includes('...'), 'descriptive failure message')` for presence checks
8. Use `assert.ok(idxA < idxB, '...')` for positional ordering checks (GATE-01 position tests, GATE-08 prompt ordering)

For GATE-04/GATE-05 tests that check content "in the SME gate section": extract the section between "SME Audit Gate" and "## 13" using `content.substring(content.indexOf('SME Audit Gate'), content.indexOf('## 13'))` and assert on that substring. This prevents false positives from other sections.

All 16 tests MUST FAIL on first run (RED) because the target content does not yet exist in plan-phase.md, plan-phase.md command, or gates.md. Commit with message: `test(06-01): add structural tests for SME audit gate in plan-phase (RED)`
  </action>
  <verify>
    <automated>cd /home/mkline/Repos/get-shit-done && node --test tests/sme-gate-plan-phase.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/sme-gate-plan-phase.test.cjs exists and is >= 120 lines
    - File starts with `'use strict';` (after JSDoc comment)
    - File contains `require('node:test')`
    - File contains `require('node:assert/strict')`
    - File contains exactly 16 `test(` calls (one per behavior item)
    - File contains `describe('GATE-01` through `describe('GATE-08`
    - File contains `describe('CONFIG-04`
    - File contains `describe('Gates.md` or similar for the gates matrix test
    - Running `node --test tests/sme-gate-plan-phase.test.cjs` produces 16 failing tests (RED gate)
    - Commit exists with message starting `test(06-01):`
  </acceptance_criteria>
  <done>16 structural tests exist covering all 9 requirements, all failing RED because implementation does not exist yet. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement SME audit gate in plan-phase workflow</name>
  <files>get-shit-done/workflows/plan-phase.md, commands/gsd/plan-phase.md, get-shit-done/references/gates.md</files>
  <read_first>
    - get-shit-done/workflows/plan-phase.md (FULL file — need step 2 lines 55-68 for flag parsing, step 5.55 lines 435-441 for config gate pattern, step 10 lines 1071-1081 for Task() pattern, step 11 lines 1082-1086 for return marker routing, step 12.5 lines 1205-1274 for insertion point context, step 13 line 1276 for end boundary)
    - commands/gsd/plan-phase.md (frontmatter — need argument-hint on line 4)
    - get-shit-done/references/gates.md (Gate Matrix table — lines 46-57)
    - tests/sme-gate-plan-phase.test.cjs (the RED tests from Task 1 — understand exactly what strings must be present)
    - .planning/phases/06-plan-phase-gate/06-PATTERNS.md (complete code excerpts for all patterns)
    - .planning/phases/06-plan-phase-gate/06-RESEARCH.md (Gate Step Skeleton section with full code example)
  </read_first>
  <behavior>
    - Test 1 (GATE-01 heading): plan-phase.md contains "SME Audit Gate" -> passes when step 12.6 heading is added
    - Test 2 (GATE-01 position after bounce): indexOf("Plan Bounce") < indexOf("SME Audit Gate") -> passes when step 12.6 is inserted after step 12.5
    - Test 3 (GATE-01 position before req coverage): indexOf("SME Audit Gate") < indexOf("Requirements Coverage Gate") -> passes when step 12.6 is before step 13
    - Test 4 (GATE-02): "sme.detect-processes" present -> passes when detect call is in step 12.6
    - Test 5 (GATE-03 auditor): "gsd-sme-auditor" present -> passes when Task() spawns auditor
    - Test 6 (GATE-03 context): "sme.context-block" present -> passes when context fetch loop is in step 12.6
    - Test 7 (GATE-04): "soft" and "proceed" in gate section -> passes when soft mode path is written
    - Test 8 (GATE-05): "strict" and "AskUserQuestion" in gate section -> passes when strict mode halt is written
    - Test 9 (GATE-06 command): argument-hint contains "--acknowledge-sme-risk" -> passes when command file updated
    - Test 10 (GATE-06 parse): plan-phase.md contains "--acknowledge-sme-risk" -> passes when step 2 updated
    - Test 11 (GATE-06 var): "ACKNOWLEDGE_SME_RISK" present -> passes when variable set in step 2
    - Test 12 (GATE-07): "/gsd-create-sme" in gate section -> passes when no-SME warning includes instruction
    - Test 13 (GATE-08): SME_CONTEXT_BLOCKS before "PLAN.md path:" -> passes when prompt construction orders correctly
    - Test 14 (CONFIG-04 list): "sme.list" in gate section -> passes when sme.list check is in step 12.6
    - Test 15 (CONFIG-04 warning): "no SME documents" text present -> passes when warning message written
    - Test 16 (gates.md): Gate Matrix has row with "SME" and "plan-phase" -> passes when row added
  </behavior>
  <action>
Make four edits to three files. All changes are markdown edits (no TypeScript compilation needed).

**Edit 1: Update step 2 in plan-phase.md (flag parsing)**

Find line 57 containing:
```
Extract from $ARGUMENTS: phase number (integer or decimal like `2.1`), flags (`--research`, `--skip-research`, `--gaps`, `--skip-verify`, `--skip-ui`, `--prd <filepath>`, `--reviews`, `--text`, `--bounce`, `--skip-bounce`, `--chunked`).
```

Replace with:
```
Extract from $ARGUMENTS: phase number (integer or decimal like `2.1`), flags (`--research`, `--skip-research`, `--gaps`, `--skip-verify`, `--skip-ui`, `--prd <filepath>`, `--reviews`, `--text`, `--bounce`, `--skip-bounce`, `--chunked`, `--acknowledge-sme-risk`).
```

Add the following line after line 59 (after the TEXT_MODE paragraph):
```
Set `ACKNOWLEDGE_SME_RISK=true` if `--acknowledge-sme-risk` is present in $ARGUMENTS.
```

**Edit 2: Insert step 12.6 in plan-phase.md**

Insert the following block between the end of step 12.5 (line 1274, after "Remove all `*-PLAN.pre-bounce.md` backup files...") and step 13 heading (line 1276, `## 13. Requirements Coverage Gate`).

The complete step 12.6 content to insert (copy verbatim):

```markdown
## 12.6. SME Audit Gate

> Skip if `workflow.use_sme_agents` is not `true`. Absent = disabled (default is `false`).

```bash
SME_AGENTS=$(gsd-sdk query config-get workflow.use_sme_agents --raw 2>/dev/null || echo "false")
```

**If `SME_AGENTS` is not `true`:** Skip to step 13.

Display banner:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► SME AUDIT GATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Detect Processes

Collect file paths from PLAN.md frontmatter `files_modified` fields and the phase goal:

```bash
PLAN_FILES=$(grep -h "files_modified:" "${PHASE_DIR}"/*-PLAN.md 2>/dev/null | tr ' ' '\n' | grep "^\s*-" | sed 's/^\s*-\s*//' | tr '\n' ' ')
PHASE_GOAL=$(gsd-sdk query roadmap.get-phase "${PHASE}" --pick goal 2>/dev/null || echo "")

SME_DETECT=$(gsd-sdk query sme.detect-processes --file-paths ${PLAN_FILES} --goal "${PHASE_GOAL}" 2>/dev/null || echo '{"data":{"enabled":false,"matches":[]}}')
```

Parse `SME_DETECT`: extract `enabled` and `matches[]`.

**If `enabled` is false:** Skip to step 13.

**If `matches` is empty:**

Check if any SME documents exist at all:

```bash
SME_LIST=$(gsd-sdk query sme.list 2>/dev/null || echo '{"data":{"enabled":true,"smes":[]}}')
```

Parse `SME_LIST`: extract `smes[]`.

If `smes[]` is empty (CONFIG-04 — no SME documents exist yet):
```
◆ SME agents enabled but no SME documents exist yet.
  Create one with: /gsd-create-sme [process-name]
  Skipping SME audit for this phase.
```

If `smes[]` is non-empty but `matches` is empty (GATE-07 — processes detected but no matching SME):
```
◆ No SME found for processes detected in this phase.
  Create one with: /gsd-create-sme [process-name]
  Skipping SME audit for this phase.
```

Proceed to step 13. Never block when no SME exists.

### Fetch Context Blocks

For each match in `matches[]`, fetch its SME context block:

```bash
SME_CONTEXT_BLOCKS=""
for match in matches; do
  PROCESS_NAME=$(extract process_name from match)
  CTX=$(gsd-sdk query sme.context-block "${PROCESS_NAME}" 2>/dev/null || echo "")
  SME_CONTEXT_BLOCKS="${SME_CONTEXT_BLOCKS}${CTX_BLOCK}"
done
```

### Determine Effective Block Mode

When multiple SMEs are matched, use strict-wins resolution:

```
EFFECTIVE_BLOCK_MODE=soft
for match in matches; do
  if match.block_mode == "strict"; then
    EFFECTIVE_BLOCK_MODE=strict
  fi
done
```

### Spawn Auditor

Construct auditor prompt with SME context blocks injected FIRST (GATE-08 — prevents context window saturation from hiding BLOCKERs):

```
Task(
  prompt="""${SME_CONTEXT_BLOCKS}

PLAN.md path: ${PHASE_DIR}/${PADDED_PHASE}-PLAN.md

Phase: ${phase_name}
""",
  subagent_type="gsd-sme-auditor",
  model="${checker_model}",
  description="SME audit Phase ${PHASE}"
)
```

> **ORCHESTRATOR RULE — CODEX RUNTIME**: After calling Task() above, stop working on this task immediately. Do not read more files, edit code, or run tests related to this task while the subagent is active. Wait for the subagent to return its result. This prevents duplicate work, conflicting edits, and wasted context. Only resume when the subagent result is available.

### Route on Return Marker

**`## SME_APPROVED`:** Display approval message. Proceed to step 13.

**`## SME_CONCERNS`:**

**If `ACKNOWLEDGE_SME_RISK` is true:** Log risk acceptance to output. Display: "SME concerns acknowledged via --acknowledge-sme-risk flag. Proceeding." Proceed to step 13.

**If `EFFECTIVE_BLOCK_MODE` is `soft`:** Display SME concerns as warnings. Proceed to step 13.

**If `EFFECTIVE_BLOCK_MODE` is `strict`:**

Display SME concerns.

If `TEXT_MODE` is true, present as a plain-text numbered list:
```
SME Audit: BLOCKERs Found
The SME auditor found unaddressed domain risks in the plan.
Options:
1. Acknowledge risk and proceed (risk documented in output)
2. Revise plan (return to step 8 for replanning)
Type your choice (1 or 2):
```

Otherwise use:
```
AskUserQuestion(
  header: "SME Audit: BLOCKERs Found",
  question: "The SME auditor found unaddressed domain risks in the plan. How would you like to proceed?",
  options:
    - "Acknowledge risk and proceed" — risk documented in output, proceed to step 13
    - "Revise plan" — return to step 8 for replanning
)
```

If user selects "Acknowledge risk": Log acknowledgment. Proceed to step 13.
If user selects "Revise plan": Return to step 8.

**No recognized marker / empty return:** Log warning. Offer: 1) Retry audit, 2) Skip audit and proceed, 3) Stop.
```

**Edit 3: Update argument-hint in commands/gsd/plan-phase.md**

Find line 4 containing:
```
argument-hint: "[phase] [--auto] [--research] [--skip-research] [--gaps] [--skip-verify] [--prd <file>] [--reviews] [--text] [--tdd]"
```

Replace with:
```
argument-hint: "[phase] [--auto] [--research] [--skip-research] [--gaps] [--skip-verify] [--prd <file>] [--reviews] [--text] [--tdd] [--acknowledge-sme-risk]"
```

**Edit 4: Add Gate Matrix row in gates.md**

Find the Gate Matrix table (line 48-57). Add a new row after the existing `plan-phase | Post-revision | Escalation` row:
```
| plan-phase | Step 12.6 | Escalation | SME domain risks in PLAN.md | Soft: warn + proceed; Strict: halt until acknowledged |
```

After all four edits, run tests. All 16 tests must pass GREEN.

Commit with message: `feat(06-01): add SME audit gate as step 12.6 in plan-phase workflow`
  </action>
  <verify>
    <automated>cd /home/mkline/Repos/get-shit-done && node --test tests/sme-gate-plan-phase.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/workflows/plan-phase.md contains `## 12.6. SME Audit Gate`
    - get-shit-done/workflows/plan-phase.md contains `sme.detect-processes`
    - get-shit-done/workflows/plan-phase.md contains `sme.context-block`
    - get-shit-done/workflows/plan-phase.md contains `gsd-sme-auditor`
    - get-shit-done/workflows/plan-phase.md contains `SME_CONTEXT_BLOCKS` before `PLAN.md path:`
    - get-shit-done/workflows/plan-phase.md contains `AskUserQuestion` in the section between "SME Audit Gate" and "## 13"
    - get-shit-done/workflows/plan-phase.md contains `ACKNOWLEDGE_SME_RISK`
    - get-shit-done/workflows/plan-phase.md contains `--acknowledge-sme-risk`
    - get-shit-done/workflows/plan-phase.md contains `sme.list`
    - get-shit-done/workflows/plan-phase.md contains `no SME documents` (case-insensitive)
    - get-shit-done/workflows/plan-phase.md contains `/gsd-create-sme`
    - get-shit-done/workflows/plan-phase.md step ordering: indexOf("Plan Bounce") < indexOf("SME Audit Gate") < indexOf("Requirements Coverage Gate")
    - commands/gsd/plan-phase.md argument-hint contains `--acknowledge-sme-risk`
    - get-shit-done/references/gates.md Gate Matrix contains row with "SME" and "plan-phase" and "12.6"
    - Running `node --test tests/sme-gate-plan-phase.test.cjs` produces 16 passing tests (GREEN gate)
    - Running `node --test tests/plan-bounce.test.cjs` still passes (step 12.5 not modified)
    - Commit exists with message starting `feat(06-01):`
  </acceptance_criteria>
  <done>All 16 structural tests pass GREEN. Step 12.6 SME Audit Gate exists in plan-phase.md with config guard, process detection, context block fetch, auditor spawn, soft/strict routing, --acknowledge-sme-risk override, and no-SME warning paths. Command argument-hint updated. Gates.md matrix updated. Existing plan-bounce tests unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer config -> workflow | `workflow.use_sme_agents` and `sme.blocking` read from config file |
| CLI flag -> workflow | `--acknowledge-sme-risk` flag parsed from user arguments |
| SME document content -> auditor prompt | SME findings injected into auditor prompt via `sme.context-block` |
| Auditor return -> gate routing | Return markers (`## SME_APPROVED` / `## SME_CONCERNS`) determine gate behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | SME document content in auditor prompt | accept | SME documents are developer-authored project files at project trust level — established in T-02-02 (Phase 2 security decision). No additional sanitization needed. |
| T-06-02 | Tampering | `--acknowledge-sme-risk` flag bypass | accept | Flag is a developer-level override for their own project. Risk acceptance is logged to terminal output so it is visible in session history. Acceptable for developer tool. |
| T-06-03 | Denial of Service | Auditor hangs or returns no marker | mitigate | Gate includes "No recognized marker / empty return" fallback path offering retry, skip, or stop options — prevents workflow from hanging indefinitely. |
| T-06-04 | Spoofing | Forged auditor return markers | accept | Auditor is spawned by the workflow orchestrator in a trusted context. The return marker contract is defined in agent-contracts.md. No external input can inject markers. |
</threat_model>

<verification>
```bash
# All 16 structural tests pass
node --test tests/sme-gate-plan-phase.test.cjs

# Existing plan-bounce tests still pass (step 12.5 not modified)
node --test tests/plan-bounce.test.cjs

# Full CJS test suite passes
node --test tests/*.test.cjs

# Step 12.6 exists in correct position
grep -n "## 12\." get-shit-done/workflows/plan-phase.md

# argument-hint includes new flag
grep "acknowledge-sme-risk" commands/gsd/plan-phase.md

# Gates matrix includes SME row
grep "SME" get-shit-done/references/gates.md
```
</verification>

<success_criteria>
1. 16 structural tests pass GREEN covering GATE-01..08 and CONFIG-04
2. Step 12.6 SME Audit Gate exists in plan-phase.md between Plan Bounce (12.5) and Requirements Coverage (13)
3. `--acknowledge-sme-risk` flag is declared in command argument-hint and parsed in step 2
4. Gate Matrix in gates.md has a row for the SME audit gate
5. Existing `plan-bounce.test.cjs` tests still pass (step 12.5 untouched)
6. TDD commit sequence: RED commit then GREEN commit
</success_criteria>

<output>
After completion, create `.planning/phases/06-plan-phase-gate/06-01-SUMMARY.md`
</output>
