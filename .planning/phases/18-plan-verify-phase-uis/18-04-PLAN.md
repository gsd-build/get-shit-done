---
phase: 18-plan-verify-phase-uis
plan: 04
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - apps/web/src/components/features/plan/PlanKanban.tsx
  - apps/web/src/components/features/plan/PlanKanban.test.tsx
  - apps/web/src/components/features/plan/WaveColumn.tsx
  - apps/web/src/components/features/plan/WaveColumn.test.tsx
  - apps/web/src/components/features/plan/TaskCard.tsx
  - apps/web/src/components/features/plan/TaskCard.test.tsx
  - apps/web/src/components/features/plan/DependencyLines.tsx
  - apps/web/src/components/features/plan/index.ts
autonomous: true
requirements: [PLAN-02, PLAN-05]

must_haves:
  truths:
    - "User sees tasks grouped in columns by wave (Wave 1, Wave 2, ...)"
    - "User sees connecting lines between cards showing dependencies"
    - "User can click a card to edit it inline"
    - "User can edit title and description only (wave/dependencies stay fixed)"
  artifacts:
    - path: "apps/web/src/components/features/plan/PlanKanban.tsx"
      provides: "Kanban board container with columns and SVG overlay"
      min_lines: 60
    - path: "apps/web/src/components/features/plan/WaveColumn.tsx"
      provides: "Single wave column containing task cards"
      min_lines: 30
    - path: "apps/web/src/components/features/plan/TaskCard.tsx"
      provides: "Task card with inline editing capability"
      min_lines: 80
    - path: "apps/web/src/components/features/plan/DependencyLines.tsx"
      provides: "SVG arrows showing task dependencies"
      min_lines: 40
  key_links:
    - from: "apps/web/src/components/features/plan/PlanKanban.tsx"
      to: "apps/web/src/components/features/plan/WaveColumn.tsx"
      via: "component composition"
      pattern: "WaveColumn"
    - from: "apps/web/src/components/features/plan/PlanKanban.tsx"
      to: "apps/web/src/components/features/plan/DependencyLines.tsx"
      via: "SVG overlay"
      pattern: "DependencyLines"
    - from: "apps/web/src/components/features/plan/TaskCard.tsx"
      to: "react-contenteditable"
      via: "inline editing"
      pattern: "ContentEditable"
---

<objective>
Create Kanban-style plan preview with wave columns and inline editing

Purpose: Visualize generated plans in a Kanban board layout where columns represent waves and cards represent tasks. Users can preview the plan structure, see dependencies as connecting lines, and edit task titles/descriptions inline. Per CONTEXT.md locked decisions.
Output: PlanKanban container, WaveColumn, TaskCard with inline editing, and DependencyLines SVG overlay.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-plan-verify-phase-uis/18-01-PLAN.md

<interfaces>
<!-- From Plan 18-01 -->
From apps/web/src/types/plan.ts:
```typescript
export interface PlanTask {
  id: string;
  name: string;
  description: string;
  wave: number;
  dependsOn: string[];
  files?: string[];
  type: 'auto' | 'checkpoint:human-verify' | 'checkpoint:decision' | 'checkpoint:human-action';
}

export interface Plan {
  id: string;
  phaseId: string;
  tasks: PlanTask[];
  createdAt: string;
}
```

From apps/web/src/stores/planStore.ts:
```typescript
export const usePlanStore = create<PlanState>((set) => ({ ... }));
export const selectPlan = (state: PlanState) => state.plan;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for TaskCard and WaveColumn</name>
  <files>
    apps/web/src/components/features/plan/TaskCard.test.tsx
    apps/web/src/components/features/plan/WaveColumn.test.tsx
  </files>
  <action>
Create TaskCard.test.tsx with failing tests:
- Test renders task name and description
- Test renders task type badge (auto, checkpoint, etc.)
- Test renders file count if files provided
- Test clicking card enables inline editing mode
- Test editing title updates via onTaskEdit callback
- Test editing description updates via onTaskEdit callback
- Test pressing Escape cancels edit mode
- Test pressing Enter/blur saves changes
- Test card has data-testid with task id for position tracking

Create WaveColumn.test.tsx with failing tests:
- Test renders wave number header (e.g., "Wave 1")
- Test renders all tasks in the wave
- Test renders task count badge
- Test passes onTaskEdit to each TaskCard
- Test column has min-height for drop targets (future drag-drop)

Create stub component files that will fail these tests.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run TaskCard.test.tsx WaveColumn.test.tsx 2>&1 | grep -E "(FAIL|PASS|Error)" | head -20</automated>
  </verify>
  <done>Tests exist and fail, component stubs created</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement TaskCard with inline editing</name>
  <files>
    apps/web/src/components/features/plan/TaskCard.tsx
  </files>
  <action>
Implement TaskCard.tsx per CONTEXT.md locked decisions:
- Accept task: PlanTask, onTaskEdit callback, optional isSelected
- Use useState for isEditing, editedTitle, editedDescription
- Normal mode:
  - Card with border, rounded corners, p-3 padding
  - Task name as heading (font-medium)
  - Description below (text-sm text-muted-foreground)
  - Type badge (auto = neutral, checkpoint:* = warning)
  - File count if files array exists: "{N} files" badge
  - Click handler sets isEditing=true
- Edit mode (per CONTEXT.md: click card to edit inline):
  - Use react-contenteditable for title (single line, no line breaks)
  - Use textarea for description (multi-line)
  - Save button and Cancel button at bottom
  - onBlur on last field OR Save button: call onTaskEdit with {title, description}
  - Cancel button or Escape key: revert to original values, set isEditing=false
  - IMPORTANT: Do NOT allow editing wave or dependsOn (per CONTEXT.md)
- Add id attribute: id={`task-${task.id}`} for position tracking
- Add data-testid={`task-card-${task.id}`}
- Use clsx for conditional edit mode styling (shadow, ring)
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run TaskCard.test.tsx 2>&1 | grep -E "(PASS|FAIL)" | head -10</automated>
  </verify>
  <done>TaskCard tests pass, inline editing works correctly</done>
</task>

<task type="auto">
  <name>Task 3: GREEN - Implement WaveColumn, DependencyLines, and PlanKanban</name>
  <files>
    apps/web/src/components/features/plan/WaveColumn.tsx
    apps/web/src/components/features/plan/DependencyLines.tsx
    apps/web/src/components/features/plan/PlanKanban.tsx
    apps/web/src/components/features/plan/PlanKanban.test.tsx
    apps/web/src/components/features/plan/index.ts
  </files>
  <action>
Implement WaveColumn.tsx:
- Accept waveNumber: number, tasks: PlanTask[], onTaskEdit callback
- Header with "Wave {N}" and task count badge
- Vertical stack of TaskCard components
- Min-height for visual consistency
- Background: bg-muted/30 rounded-lg p-3

Implement DependencyLines.tsx:
- Accept tasks: PlanTask[], cardPositions: Map&lt;string, DOMRect&gt;, containerRef
- Create SVG with absolute positioning over container
- For each task with dependsOn, draw lines from dependent card to this card
- Use simple straight lines (evaluate curved lines later per RESEARCH.md)
- Line style: stroke="#888" stroke-width="1.5" stroke-dasharray="4"
- Arrow markers at end of lines
- Handle case where positions not yet calculated (render nothing)

Implement PlanKanban.tsx:
- Accept tasks: PlanTask[], onTaskEdit callback
- Group tasks by wave number into record
- Use containerRef for position tracking
- Track cardPositions with useState Map
- useEffect to update positions on mount, resize, and task changes
- Render DependencyLines as SVG overlay (absolute positioned)
- Render horizontal scroll container with grid of WaveColumn components
- Column layout: auto-cols-[300px] grid-flow-col gap-4 overflow-x-auto p-4

Create PlanKanban.test.tsx:
- Test groups tasks into correct wave columns
- Test renders DependencyLines overlay
- Test passes onTaskEdit to columns

Update index.ts to export new components:
```typescript
export { PlanKanban } from './PlanKanban';
export { WaveColumn } from './WaveColumn';
export { TaskCard } from './TaskCard';
export { DependencyLines } from './DependencyLines';
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run TaskCard.test.tsx WaveColumn.test.tsx PlanKanban.test.tsx && pnpm run build</automated>
  </verify>
  <done>All tests pass, Kanban layout works, build succeeds</done>
</task>

</tasks>

<verification>
- [ ] TaskCard renders task info and type badge
- [ ] TaskCard inline editing updates title/description only
- [ ] WaveColumn groups tasks with header showing wave number
- [ ] DependencyLines draws connecting lines between dependent tasks
- [ ] PlanKanban renders horizontal scroll of wave columns
- [ ] All tests pass with `pnpm test --run`
- [ ] Build succeeds with `pnpm run build`
</verification>

<success_criteria>
- Kanban board displays tasks grouped by wave in columns
- Dependency lines show which tasks depend on others
- Click card to edit title/description inline (wave/deps stay fixed)
- Save edits on blur/Enter, cancel on Escape
- Components exported via barrel index
</success_criteria>

<output>
After completion, create `.planning/phases/18-plan-verify-phase-uis/18-04-SUMMARY.md`
</output>
