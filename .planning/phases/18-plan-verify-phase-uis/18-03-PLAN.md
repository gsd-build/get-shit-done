---
phase: 18-plan-verify-phase-uis
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - apps/web/src/components/features/plan/ResearchSwimlanes.tsx
  - apps/web/src/components/features/plan/ResearchSwimlanes.test.tsx
  - apps/web/src/components/features/plan/AgentLane.tsx
  - apps/web/src/components/features/plan/AgentLane.test.tsx
  - apps/web/src/components/features/plan/AgentSummary.tsx
  - apps/web/src/components/features/plan/index.ts
autonomous: true
requirements: [PLAN-01]

must_haves:
  truths:
    - "User sees horizontal lanes for each parallel researcher agent"
    - "User sees running/complete/error status with icons"
    - "User sees elapsed time updating while agent runs"
    - "User sees current action text (e.g., 'Reading src/api/...')"
    - "User can click to expand/collapse summary when agent completes"
    - "Failed agents have red lane with inline error message"
  artifacts:
    - path: "apps/web/src/components/features/plan/ResearchSwimlanes.tsx"
      provides: "Container for agent lanes using CSS Grid"
      min_lines: 30
    - path: "apps/web/src/components/features/plan/AgentLane.tsx"
      provides: "Single horizontal agent lane with status, action, timer"
      min_lines: 60
    - path: "apps/web/src/components/features/plan/AgentSummary.tsx"
      provides: "Expandable summary section for completed agents"
      min_lines: 20
    - path: "apps/web/src/components/features/plan/index.ts"
      provides: "Barrel export for plan components"
  key_links:
    - from: "apps/web/src/components/features/plan/ResearchSwimlanes.tsx"
      to: "apps/web/src/components/features/plan/AgentLane.tsx"
      via: "component composition"
      pattern: "AgentLane"
    - from: "apps/web/src/components/features/plan/AgentLane.tsx"
      to: "apps/web/src/types/plan.ts"
      via: "type import"
      pattern: "import.*ResearchAgent"
---

<objective>
Create research agent swimlane visualization components

Purpose: Visualize parallel researcher agents in a GitHub Actions-like swimlane layout. Each agent gets a horizontal lane showing status, elapsed time, and current action. Per CONTEXT.md locked decisions.
Output: ResearchSwimlanes container, AgentLane row component, and AgentSummary expandable section.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-plan-verify-phase-uis/18-01-PLAN.md

<interfaces>
<!-- From Plan 18-01 -->
From apps/web/src/types/plan.ts:
```typescript
export type AgentStatus = 'pending' | 'running' | 'complete' | 'error';

export interface ResearchAgent {
  id: string;
  name: string;
  status: AgentStatus;
  currentAction?: string;
  elapsedMs: number;
  summary?: string;
  error?: string;
}
```

From apps/web/src/stores/planStore.ts:
```typescript
export const usePlanStore = create<PlanState>((set) => ({ ... }));
export const selectAgents = (state: PlanState) => state.agents;
```

From apps/web/src/components/ui/index.ts (Phase 15):
```typescript
export { ProgressBar } from './ProgressBar';
export { Badge } from './Badge';
export { FilterChip } from './FilterChip';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for AgentLane and ResearchSwimlanes</name>
  <files>
    apps/web/src/components/features/plan/AgentLane.test.tsx
    apps/web/src/components/features/plan/ResearchSwimlanes.test.tsx
  </files>
  <action>
Create AgentLane.test.tsx with failing tests:
- Test renders agent name
- Test renders pending status with border-only circle icon
- Test renders running status with spinning loader icon and primary border
- Test renders complete status with green check icon and green border
- Test renders error status with red X icon, red border, and error message inline
- Test shows current action text when running
- Test shows formatted elapsed time (e.g., "45s", "2m 15s")
- Test expand button appears only when complete with summary
- Test clicking expand button reveals summary text
- Test clicking again collapses summary

Create ResearchSwimlanes.test.tsx with failing tests:
- Test renders empty state when no agents
- Test renders one AgentLane per agent in agents array
- Test uses CSS grid layout with gap between lanes
- Test has role="list" and aria-label for accessibility

Create stub component files that will fail these tests.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run AgentLane.test.tsx ResearchSwimlanes.test.tsx 2>&1 | grep -E "(FAIL|PASS|Error)" | head -20</automated>
  </verify>
  <done>Tests exist and fail, component stubs created</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement AgentLane with status display and timer</name>
  <files>
    apps/web/src/components/features/plan/AgentLane.tsx
    apps/web/src/components/features/plan/AgentSummary.tsx
  </files>
  <action>
Implement AgentLane.tsx per CONTEXT.md locked decisions:
- Accept agent: ResearchAgent and optional onClick callback
- Use clsx for conditional styling
- Layout with flex row: status icon, name, current action (truncated), elapsed time, expand button
- Status icons using lucide-react:
  - pending: div with border-2 rounded-full (empty circle)
  - running: Loader2 with animate-spin and text-primary
  - complete: CheckCircle with text-green-500
  - error: XCircle with text-red-500
- Border colors per status:
  - pending: border-border (default)
  - running: border-primary bg-primary/5
  - complete: border-green-500/50 bg-green-50/50 dark:bg-green-900/10
  - error: border-red-500 bg-red-50 dark:bg-red-900/20 (red lane per CONTEXT.md)
- Show currentAction when running (truncated with max-w-xs)
- Show error message inline when status='error' (red text per CONTEXT.md)
- Elapsed time formatted: seconds if < 60, otherwise Xm Ys
- Expand button (ChevronDown/ChevronRight) only when complete AND summary exists
- Internal expanded state managed with useState

Implement AgentSummary.tsx:
- Accept summary: string
- Render within border-t mt-3 pt-3
- Use whitespace-pre-wrap for summary text
- Allow passing className for customization
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run AgentLane.test.tsx 2>&1 | grep -E "(PASS|FAIL)" | head -10</automated>
  </verify>
  <done>AgentLane tests pass, component renders all states correctly</done>
</task>

<task type="auto">
  <name>Task 3: GREEN - Implement ResearchSwimlanes container and barrel export</name>
  <files>
    apps/web/src/components/features/plan/ResearchSwimlanes.tsx
    apps/web/src/components/features/plan/index.ts
  </files>
  <action>
Implement ResearchSwimlanes.tsx:
- Accept agents: ResearchAgent[] and optional onAgentClick callback
- Use CSS Grid: grid grid-rows-[auto] gap-2 p-4
- Add role="list" and aria-label="Research agents" for accessibility
- Map over agents rendering AgentLane for each
- Pass onAgentClick to each lane if provided
- Handle empty array gracefully (just render empty container)

Create index.ts barrel export:
```typescript
export { ResearchSwimlanes } from './ResearchSwimlanes';
export { AgentLane } from './AgentLane';
export { AgentSummary } from './AgentSummary';
```

Verify all tests pass and build succeeds.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run AgentLane.test.tsx ResearchSwimlanes.test.tsx && pnpm run build</automated>
  </verify>
  <done>All tests pass, components exported, build succeeds</done>
</task>

</tasks>

<verification>
- [ ] AgentLane renders all four status states correctly
- [ ] AgentLane shows elapsed time in correct format
- [ ] AgentLane expand/collapse works for completed agents with summary
- [ ] Error agents have red lane with inline error message
- [ ] ResearchSwimlanes renders grid of AgentLane components
- [ ] Accessibility attributes present (role, aria-label)
- [ ] All tests pass with `pnpm test --run`
- [ ] Build succeeds with `pnpm run build`
</verification>

<success_criteria>
- GitHub Actions-like swimlane layout displays parallel agents
- Each lane shows status icon, name, current action, elapsed time
- Completed agents have expandable summary (collapsed by default)
- Error agents have red styling with inline error message
- Components are accessible with proper ARIA attributes
</success_criteria>

<output>
After completion, create `.planning/phases/18-plan-verify-phase-uis/18-03-SUMMARY.md`
</output>
