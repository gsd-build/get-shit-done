---
phase: 18-plan-verify-phase-uis
plan: 07
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - apps/web/src/components/features/verify/ManualChecklist.tsx
  - apps/web/src/components/features/verify/ManualChecklist.test.tsx
  - apps/web/src/components/features/verify/ManualTestItem.tsx
  - apps/web/src/components/features/verify/ApprovalBar.tsx
  - apps/web/src/components/features/verify/ApprovalBar.test.tsx
  - apps/web/src/components/features/verify/GapSelectionModal.tsx
  - apps/web/src/components/features/verify/GapSelectionModal.test.tsx
  - apps/web/src/components/features/verify/index.ts
autonomous: true
requirements: [VERIF-05, VERIF-06]

must_haves:
  truths:
    - "User sees checkboxes with optional notes for manual test checklist"
    - "User can mark each manual test as pass/fail"
    - "User can add optional note to any manual test"
    - "User can approve or reject with two-step confirmation"
    - "Rejection opens modal for gap selection"
  artifacts:
    - path: "apps/web/src/components/features/verify/ManualChecklist.tsx"
      provides: "Manual test checklist container"
      min_lines: 30
    - path: "apps/web/src/components/features/verify/ManualTestItem.tsx"
      provides: "Single manual test with checkbox and note"
      min_lines: 50
    - path: "apps/web/src/components/features/verify/ApprovalBar.tsx"
      provides: "Approve/Reject action bar with confirmation"
      min_lines: 60
    - path: "apps/web/src/components/features/verify/GapSelectionModal.tsx"
      provides: "Modal for selecting gaps when rejecting"
      min_lines: 50
  key_links:
    - from: "apps/web/src/components/features/verify/ManualTestItem.tsx"
      to: "@radix-ui/react-checkbox"
      via: "Radix primitive"
      pattern: "Checkbox"
    - from: "apps/web/src/components/features/verify/ApprovalBar.tsx"
      to: "@radix-ui/react-dialog"
      via: "Radix primitive"
      pattern: "Dialog"
---

<objective>
Create manual test checklist and approval workflow with two-step confirmation

Purpose: Enable users to mark manual test items as pass/fail with optional notes, and provide approve/reject actions with confirmation modals. Per CONTEXT.md locked decisions.
Output: ManualChecklist, ManualTestItem with Radix Checkbox, ApprovalBar with confirmation, GapSelectionModal.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-plan-verify-phase-uis/18-02-PLAN.md

<interfaces>
<!-- From Plan 18-02 -->
From apps/web/src/types/verification.ts:
```typescript
export interface ManualTest {
  id: string;
  description: string;
  passed: boolean | null;
  note?: string;
}

export interface Gap {
  id: string;
  requirementId: string;
  description: string;
  severity: Severity;
}
```

From apps/web/src/stores/verificationStore.ts:
```typescript
export const selectManualTests = (state: VerificationState) => state.manualTests;
export const selectManualTestsComplete = (state: VerificationState) => ...;
export const selectHasBlockingGaps = (state: VerificationState) => ...;
```

From @radix-ui/react-checkbox:
```typescript
import * as Checkbox from '@radix-ui/react-checkbox';
// Checkbox.Root, Checkbox.Indicator
```

From @radix-ui/react-dialog:
```typescript
import * as Dialog from '@radix-ui/react-dialog';
// Dialog.Root, Dialog.Portal, Dialog.Overlay, Dialog.Content, Dialog.Title, Dialog.Close
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for ManualTestItem and ManualChecklist</name>
  <files>
    apps/web/src/components/features/verify/ManualTestItem.test.tsx
    apps/web/src/components/features/verify/ManualChecklist.test.tsx
  </files>
  <action>
Create ManualTestItem.test.tsx with failing tests:
- Test renders test description
- Test renders unchecked checkbox when passed=null
- Test renders checked checkbox when passed=true
- Test clicking checkbox calls onUpdate with passed=true
- Test "Add note" button appears when no note
- Test clicking "Add note" shows textarea
- Test typing in textarea updates note
- Test blur on textarea calls onUpdate with note value

Create ManualChecklist.test.tsx with failing tests:
- Test renders heading "Manual Test Checklist"
- Test renders all manual tests
- Test passes onTestUpdate to each item
- Test shows empty state when no tests
- Test shows completion status (X of Y complete)

Create stub component files that will fail these tests.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run ManualTestItem.test.tsx ManualChecklist.test.tsx 2>&1 | grep -E "(FAIL|PASS|Error)" | head -20</automated>
  </verify>
  <done>Tests exist and fail, component stubs created</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement ManualTestItem and ManualChecklist</name>
  <files>
    apps/web/src/components/features/verify/ManualTestItem.tsx
    apps/web/src/components/features/verify/ManualChecklist.tsx
  </files>
  <action>
Implement ManualTestItem.tsx per CONTEXT.md (checkboxes with optional notes):
- Accept test: ManualTest, onUpdate: (passed: boolean, note?: string) => void
- Use @radix-ui/react-checkbox for accessible checkbox
- Checkbox styling:
  - w-5 h-5 border border-border rounded flex items-center justify-center
  - data-[state=checked]:bg-primary data-[state=checked]:border-primary
  - CheckIcon inside Checkbox.Indicator
- Layout: flex items-start gap-3
- Description text (text-sm text-foreground)
- Note section:
  - If note exists OR showNote state true: render textarea
  - Otherwise: render "+ Add note" button (text-xs text-muted-foreground)
  - textarea: mt-2 w-full px-2 py-1 text-sm border rounded resize-none rows=2
  - onBlur: call onUpdate with current passed value and note
- useState for local note value and showNote boolean
- Wrap in border border-border rounded-lg p-3

Implement ManualChecklist.tsx:
- Accept tests: ManualTest[], onTestUpdate: (testId, passed, note?) => void
- Render heading: "Manual Test Checklist" (font-semibold text-foreground)
- Render completion: "{completed} of {total} complete" (text-sm text-muted-foreground)
- Map over tests, render ManualTestItem for each
- Space-y-3 for vertical spacing
- Empty state: "No manual tests required" when tests array empty
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run ManualTestItem.test.tsx ManualChecklist.test.tsx 2>&1 | grep -E "(PASS|FAIL)" | head -10</automated>
  </verify>
  <done>ManualTestItem and ManualChecklist tests pass</done>
</task>

<task type="auto">
  <name>Task 3: GREEN - Implement ApprovalBar and GapSelectionModal</name>
  <files>
    apps/web/src/components/features/verify/ApprovalBar.tsx
    apps/web/src/components/features/verify/GapSelectionModal.tsx
    apps/web/src/components/features/verify/ApprovalBar.test.tsx
    apps/web/src/components/features/verify/GapSelectionModal.test.tsx
    apps/web/src/components/features/verify/index.ts
  </files>
  <action>
Implement GapSelectionModal.tsx:
- Accept open, gaps, selectedGaps, onSelectionChange, onConfirm, onCancel
- Use @radix-ui/react-dialog for modal
- Dialog overlay: fixed inset-0 bg-black/50
- Dialog content: fixed centered, bg-card p-6 rounded-lg shadow-lg max-w-lg w-full
- Title: "Select Gaps to Address"
- List of gaps with checkboxes (use @radix-ui/react-checkbox)
- Each gap shows: SeverityBadge + description
- Select all / Deselect all buttons
- Footer: Cancel button + "Create Fix Plans" button (disabled if none selected)
- Cancel calls onCancel, Confirm calls onConfirm

Implement ApprovalBar.tsx per CONTEXT.md (two-step confirmation):
- Accept gaps, onApprove, onReject: (selectedGapIds: string[]) => void
- Fixed position: bottom-0 left-0 right-0, bg-card border-t p-4
- Two buttons: "Reject with Gaps" (error color) and "Approve" (primary)
- useState for showApproveConfirm, showRejectModal, selectedGaps
- Approve button disabled if hasBlockingGaps
- Click Reject: opens GapSelectionModal
- Click Approve: opens confirmation Dialog
- Approve confirmation modal:
  - "Are you sure you want to approve this phase?"
  - "This will mark all requirements as completed."
  - Cancel + Confirm Approval buttons
- Reject flow: GapSelectionModal -> onConfirm calls onReject with selectedGapIds

Create ApprovalBar.test.tsx:
- Test renders Approve and Reject buttons
- Test Approve disabled when blocking gaps exist
- Test clicking Reject opens gap selection modal
- Test clicking Approve opens confirmation modal
- Test confirming approval calls onApprove
- Test selecting gaps and confirming calls onReject with gap IDs

Create GapSelectionModal.test.tsx:
- Test renders list of gaps with checkboxes
- Test clicking gap checkbox toggles selection
- Test Select All selects all gaps
- Test Confirm button disabled when none selected
- Test Confirm calls onConfirm with selected IDs

Update index.ts with new exports:
```typescript
export { ManualChecklist } from './ManualChecklist';
export { ManualTestItem } from './ManualTestItem';
export { ApprovalBar } from './ApprovalBar';
export { GapSelectionModal } from './GapSelectionModal';
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run ManualTestItem.test.tsx ManualChecklist.test.tsx ApprovalBar.test.tsx GapSelectionModal.test.tsx && pnpm run build</automated>
  </verify>
  <done>All tests pass, approval workflow works, build succeeds</done>
</task>

</tasks>

<verification>
- [ ] ManualTestItem renders checkbox with optional note field
- [ ] ManualChecklist shows all tests with completion count
- [ ] ApprovalBar renders Approve/Reject buttons
- [ ] Approve disabled when blocking gaps exist
- [ ] Two-step confirmation for both approve and reject
- [ ] GapSelectionModal allows multi-select of gaps
- [ ] All tests pass with `pnpm test --run`
- [ ] Build succeeds with `pnpm run build`
</verification>

<success_criteria>
- Checkboxes with optional notes for manual test checklist (per CONTEXT.md)
- Two-step confirmation for approval/rejection (per CONTEXT.md)
- Gap selection modal for rejection workflow
- Approve blocked when blocking gaps exist
- Components exported via barrel index
</success_criteria>

<output>
After completion, create `.planning/phases/18-plan-verify-phase-uis/18-07-SUMMARY.md`
</output>
