---
phase: 18-plan-verify-phase-uis
plan: 06
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - apps/web/src/components/features/verify/SeverityBadge.tsx
  - apps/web/src/components/features/verify/SeverityBadge.test.tsx
  - apps/web/src/components/features/verify/GapList.tsx
  - apps/web/src/components/features/verify/GapList.test.tsx
  - apps/web/src/components/features/verify/CoverageHeatmap.tsx
  - apps/web/src/components/features/verify/CoverageHeatmap.test.tsx
  - apps/web/src/components/features/verify/index.ts
autonomous: true
requirements: [VERIF-02, PLAN-03, PLAN-04]

must_haves:
  truths:
    - "User sees gaps with color-coded severity badges (red=Blocking, orange=Major, yellow=Minor)"
    - "User sees verification feedback with specific issues highlighted per plan"
    - "User sees heatmap grid for requirement coverage matrix"
  artifacts:
    - path: "apps/web/src/components/features/verify/SeverityBadge.tsx"
      provides: "Color-coded badge for gap severity"
      min_lines: 25
    - path: "apps/web/src/components/features/verify/GapList.tsx"
      provides: "List of gaps with severity badges"
      min_lines: 40
    - path: "apps/web/src/components/features/verify/CoverageHeatmap.tsx"
      provides: "Requirement-phase coverage matrix"
      min_lines: 50
  key_links:
    - from: "apps/web/src/components/features/verify/GapList.tsx"
      to: "apps/web/src/components/features/verify/SeverityBadge.tsx"
      via: "component composition"
      pattern: "SeverityBadge"
    - from: "apps/web/src/components/features/verify/CoverageHeatmap.tsx"
      to: "react-grid-heatmap"
      via: "library import"
      pattern: "import HeatMap"
---

<objective>
Create gap highlighting with severity badges and coverage heatmap matrix

Purpose: Display gaps with color-coded severity levels (Blocking/Major/Minor) and a heatmap showing requirement-to-phase coverage. Per CONTEXT.md locked decisions.
Output: SeverityBadge component, GapList with filtering, CoverageHeatmap using react-grid-heatmap.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-plan-verify-phase-uis/18-02-PLAN.md

<interfaces>
<!-- From Plan 18-02 -->
From apps/web/src/types/verification.ts:
```typescript
export type Severity = 'blocking' | 'major' | 'minor';

export interface Gap {
  id: string;
  requirementId: string;
  description: string;
  severity: Severity;
}
```

From apps/web/src/stores/verificationStore.ts:
```typescript
export const selectGaps = (state: VerificationState) => state.gaps;
export const selectBlockingGaps = (state: VerificationState) => ...;
export const selectHasBlockingGaps = (state: VerificationState) => ...;
```

From react-grid-heatmap (installed in 18-01):
```typescript
import HeatMap from 'react-grid-heatmap';

<HeatMap
  xLabels={phases}
  yLabels={requirements}
  data={matrix}
  cellStyle={(_, __, value) => ({ background: colorForValue(value) })}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for SeverityBadge and GapList</name>
  <files>
    apps/web/src/components/features/verify/SeverityBadge.test.tsx
    apps/web/src/components/features/verify/GapList.test.tsx
  </files>
  <action>
Create SeverityBadge.test.tsx with failing tests:
- Test renders "Blocking" text with red background (bg-red-500)
- Test renders "Major" text with orange background (bg-orange-500)
- Test renders "Minor" text with yellow background (bg-yellow-400)
- Test has correct text color (white for red/orange, black for yellow)
- Test accepts optional className prop

Create GapList.test.tsx with failing tests:
- Test renders all gaps
- Test renders SeverityBadge for each gap
- Test renders gap description
- Test renders requirement ID for each gap
- Test can filter by severity (show only blocking, only major, etc.)
- Test shows "No gaps found" when array is empty
- Test sorts by severity (blocking first, then major, then minor)

Create stub component files that will fail these tests.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run SeverityBadge.test.tsx GapList.test.tsx 2>&1 | grep -E "(FAIL|PASS|Error)" | head -20</automated>
  </verify>
  <done>Tests exist and fail, component stubs created</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement SeverityBadge and GapList</name>
  <files>
    apps/web/src/components/features/verify/SeverityBadge.tsx
    apps/web/src/components/features/verify/GapList.tsx
  </files>
  <action>
Implement SeverityBadge.tsx per CONTEXT.md (color-coded severity badges):
- Accept severity: Severity, optional className
- Use clsx for conditional styling
- Severity config object:
  - blocking: { bg: 'bg-red-500', text: 'text-white', label: 'Blocking' }
  - major: { bg: 'bg-orange-500', text: 'text-white', label: 'Major' }
  - minor: { bg: 'bg-yellow-400', text: 'text-black', label: 'Minor' }
- Render span with: inline-flex items-center px-2 py-0.5 rounded text-xs font-medium
- Return label text inside span

Implement GapList.tsx:
- Accept gaps: Gap[], optional filterSeverity?: Severity | 'all'
- Sort gaps: blocking first, major second, minor third
- Filter if filterSeverity specified and not 'all'
- Render empty state "No gaps found" if filtered list empty
- Each gap item:
  - SeverityBadge
  - Requirement ID (text-muted-foreground)
  - Description (text-sm)
  - Border-left with severity color for visual grouping
- Use ul/li for accessibility
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run SeverityBadge.test.tsx GapList.test.tsx 2>&1 | grep -E "(PASS|FAIL)" | head -10</automated>
  </verify>
  <done>SeverityBadge and GapList tests pass</done>
</task>

<task type="auto">
  <name>Task 3: GREEN - Implement CoverageHeatmap and update exports</name>
  <files>
    apps/web/src/components/features/verify/CoverageHeatmap.tsx
    apps/web/src/components/features/verify/CoverageHeatmap.test.tsx
    apps/web/src/components/features/verify/index.ts
  </files>
  <action>
Implement CoverageHeatmap.tsx per CONTEXT.md (heatmap grid for coverage matrix):
- Accept requirements: string[], phases: string[], coverageData: Coverage[]
- Coverage type: { requirementId: string; phaseId: string; coverage: 0 | 1 | 2 }
  - 0 = uncovered (red)
  - 1 = partial (yellow)
  - 2 = covered (green)
- Build matrix: requirements as rows, phases as columns
- Use react-grid-heatmap HeatMap component:
  - xLabels: phases
  - yLabels: requirements
  - data: 2D array of coverage values
  - cellStyle with color function:
    - value === 0: 'rgb(239, 68, 68)' (red-500)
    - value === 1: 'rgb(250, 204, 21)' (yellow-400)
    - value === 2: 'rgb(34, 197, 94)' (green-500)
  - cellHeight="32px"
  - xLabelsPos="top"
  - yLabelsPos="left"
- Add legend below heatmap showing color meaning
- Wrap in overflow-auto container for large matrices

Create CoverageHeatmap.test.tsx:
- Test renders with correct number of cells
- Test uncovered cells have red background
- Test covered cells have green background
- Test renders legend with color explanations
- Test handles empty data gracefully

Update index.ts to include new exports:
```typescript
export { SeverityBadge } from './SeverityBadge';
export { GapList } from './GapList';
export { CoverageHeatmap } from './CoverageHeatmap';
```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run SeverityBadge.test.tsx GapList.test.tsx CoverageHeatmap.test.tsx && pnpm run build</automated>
  </verify>
  <done>All tests pass, heatmap renders correctly, build succeeds</done>
</task>

</tasks>

<verification>
- [ ] SeverityBadge renders three severity levels with correct colors
- [ ] GapList shows gaps sorted by severity with badges
- [ ] GapList can filter by severity level
- [ ] CoverageHeatmap renders requirement-phase matrix
- [ ] Heatmap uses red/yellow/green for uncovered/partial/covered
- [ ] All tests pass with `pnpm test --run`
- [ ] Build succeeds with `pnpm run build`
</verification>

<success_criteria>
- Color-coded severity badges: red (Blocking), orange (Major), yellow (Minor)
- Gap list sorted by severity with visual grouping
- Heatmap grid showing requirement coverage per phase
- Legend explains heatmap colors
- Components exported via barrel index
</success_criteria>

<output>
After completion, create `.planning/phases/18-plan-verify-phase-uis/18-06-SUMMARY.md`
</output>
