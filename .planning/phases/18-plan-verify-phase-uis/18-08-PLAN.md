---
phase: 18-plan-verify-phase-uis
plan: 08
type: execute
wave: 3
depends_on: [18-03, 18-04, 18-05, 18-06, 18-07]
files_modified:
  - apps/web/src/app/projects/[id]/plan/page.tsx
  - apps/web/src/app/projects/[id]/verify/page.tsx
  - apps/web/tests/e2e/plan-phase.spec.ts
  - apps/web/tests/e2e/verify-phase.spec.ts
  - apps/web/src/lib/api.ts
autonomous: true
requirements: [VERIF-07]

must_haves:
  truths:
    - "User can navigate to plan phase page for a project"
    - "User can navigate to verify phase page for a project"
    - "Rejection routes to gap planning automatically"
    - "E2E tests verify full user flows"
  artifacts:
    - path: "apps/web/src/app/projects/[id]/plan/page.tsx"
      provides: "Plan phase page integrating swimlanes and Kanban"
      min_lines: 60
    - path: "apps/web/src/app/projects/[id]/verify/page.tsx"
      provides: "Verify phase page integrating report, gaps, checklist, approval"
      min_lines: 80
    - path: "apps/web/tests/e2e/plan-phase.spec.ts"
      provides: "E2E tests for plan phase workflow"
      min_lines: 40
    - path: "apps/web/tests/e2e/verify-phase.spec.ts"
      provides: "E2E tests for verification workflow"
      min_lines: 50
  key_links:
    - from: "apps/web/src/app/projects/[id]/verify/page.tsx"
      to: "/api/phases/:id/plan-gaps"
      via: "API call on rejection"
      pattern: "fetch.*plan-gaps"
    - from: "apps/web/src/app/projects/[id]/plan/page.tsx"
      to: "apps/web/src/components/features/plan/*"
      via: "component composition"
      pattern: "ResearchSwimlanes|PlanKanban"
---

<objective>
Create plan and verify phase pages with E2E tests and gap-to-planning flow

Purpose: Integrate all phase 18 components into full pages. Plan page shows research swimlanes and Kanban preview. Verify page shows report, gaps, manual checklist, and approval bar. Rejection routes to gap planning. Per CONTEXT.md auto-route decision.
Output: Plan phase page, verify phase page, API client for gap planning, E2E tests for both workflows.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-plan-verify-phase-uis/18-03-PLAN.md
@.planning/phases/18-plan-verify-phase-uis/18-04-PLAN.md
@.planning/phases/18-plan-verify-phase-uis/18-05-PLAN.md
@.planning/phases/18-plan-verify-phase-uis/18-06-PLAN.md
@.planning/phases/18-plan-verify-phase-uis/18-07-PLAN.md

<interfaces>
<!-- From previous plans -->
From apps/web/src/components/features/plan/index.ts:
```typescript
export { ResearchSwimlanes } from './ResearchSwimlanes';
export { PlanKanban } from './PlanKanban';
export { TaskCard } from './TaskCard';
```

From apps/web/src/components/features/verify/index.ts:
```typescript
export { VerificationPanel } from './VerificationPanel';
export { GapList } from './GapList';
export { CoverageHeatmap } from './CoverageHeatmap';
export { ManualChecklist } from './ManualChecklist';
export { ApprovalBar } from './ApprovalBar';
```

From apps/web/src/hooks/*:
```typescript
export { useResearchStream } from './useResearchStream';
export { useVerification } from './useVerification';
export { useSocket } from './useSocket';
```

From apps/web/src/stores/*:
```typescript
export { usePlanStore, selectAgents, selectPlan } from './planStore';
export { useVerificationStore, selectResults, selectGaps } from './verificationStore';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plan phase page with swimlanes and Kanban</name>
  <files>
    apps/web/src/app/projects/[id]/plan/page.tsx
    apps/web/src/lib/api.ts
  </files>
  <action>
Add to api.ts:
- fetchPlan(phaseId: string): Promise&lt;Plan&gt; - GET /api/phases/:id/plan
- updatePlanTask(phaseId: string, taskId: string, updates: Partial&lt;PlanTask&gt;): Promise&lt;PlanTask&gt; - PATCH /api/phases/:id/plan/tasks/:taskId
- startResearch(phaseId: string): Promise&lt;void&gt; - POST /api/phases/:id/research

Create apps/web/src/app/projects/[id]/plan/page.tsx:
- 'use client' directive
- Accept params: { id: string } (project ID from URL)
- Use useSocket hook for Socket.IO connection
- Use useResearchStream hook for agent streaming
- Use usePlanStore for plan state
- Layout sections:
  1. Header with "Planning Phase" title and phase info
  2. "Research Progress" section with ResearchSwimlanes
  3. "Plan Preview" section with PlanKanban
- "Start Research" button if no agents running
- Show loading skeleton while fetching plan
- Handle plan task edit via updatePlanTask API call
- Navigate back to project detail with Link
- Use bracket notation for params['id'] per project tsconfig

Ensure TypeScript strict compliance:
- Use bracket notation for params access
- Spread pattern for optional callbacks
  </action>
  <verify>
    <automated>cd apps/web && pnpm run build && pnpm run typecheck</automated>
  </verify>
  <done>Plan page renders swimlanes and Kanban, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create verify phase page with report and approval workflow</name>
  <files>
    apps/web/src/app/projects/[id]/verify/page.tsx
    apps/web/src/lib/api.ts
  </files>
  <action>
Add to api.ts:
- startVerification(phaseId: string): Promise&lt;void&gt; - POST /api/phases/:id/verify
- submitApproval(phaseId: string): Promise&lt;void&gt; - POST /api/phases/:id/approve
- submitRejection(phaseId: string, gapIds: string[]): Promise&lt;{ planUrl: string }&gt; - POST /api/phases/:id/reject
- fetchCoverage(phaseId: string): Promise&lt;Coverage[]&gt; - GET /api/phases/:id/coverage

Create apps/web/src/app/projects/[id]/verify/page.tsx:
- 'use client' directive
- Accept params: { id: string }
- Use useSocket for Socket.IO connection
- Use useVerification hook for verification streaming
- Use useVerificationStore for state
- Layout sections:
  1. Header with "Verification" title
  2. VerificationPanel (report header + requirement list)
  3. Tabs or sections for:
     - "Gaps" tab with GapList
     - "Coverage" tab with CoverageHeatmap
     - "Manual Tests" tab with ManualChecklist
  4. ApprovalBar fixed at bottom
- "Run Verification" button to trigger tests
- Handle approve: call submitApproval, show success toast, navigate to project
- Handle reject per CONTEXT.md (auto-route to gap planning):
  - Call submitRejection with selected gap IDs
  - API returns { planUrl } pointing to plan phase with gaps
  - Redirect to planUrl using router.push
- Fetch coverage data on mount for heatmap
- Use Radix Tabs for section switching
  </action>
  <verify>
    <automated>cd apps/web && pnpm run build && pnpm run typecheck</automated>
  </verify>
  <done>Verify page renders all sections, approval/rejection works, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Create E2E tests for plan and verify pages</name>
  <files>
    apps/web/tests/e2e/plan-phase.spec.ts
    apps/web/tests/e2e/verify-phase.spec.ts
  </files>
  <action>
Create plan-phase.spec.ts with E2E tests:
- Test navigates to plan page from project detail
- Test shows "Planning Phase" header
- Test shows Research Progress section (even if empty)
- Test shows Plan Preview section with Kanban columns
- Test clicking task card enters edit mode
- Test can edit task title (check for textarea/contenteditable)
- Test Escape key exits edit mode

Create verify-phase.spec.ts with E2E tests:
- Test navigates to verify page from project detail
- Test shows "Verification" header
- Test shows ReportHeader with status
- Test shows RequirementList with expandable items
- Test clicking requirement expands to show tests
- Test shows GapList with severity badges (if gaps exist)
- Test shows ManualChecklist section
- Test ApprovalBar visible at bottom
- Test Approve button shows confirmation modal
- Test Reject button opens gap selection modal
- Test selecting gaps and confirming shows redirect (mock API)

Use Playwright patterns from apps/web/tests/e2e/dashboard.spec.ts:
- Resilient selectors (getByRole, getByText, getByTestId)
- Wait for network idle or specific elements
- Mock API responses for predictable tests
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec playwright test plan-phase.spec.ts verify-phase.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>E2E tests pass, full workflows verified</done>
</task>

</tasks>

<verification>
- [ ] Plan page shows research swimlanes and Kanban preview
- [ ] Plan page allows inline task editing
- [ ] Verify page shows report, gaps, coverage, manual tests
- [ ] Verify page approval shows confirmation modal
- [ ] Verify page rejection routes to gap planning (VERIF-07)
- [ ] E2E tests cover plan and verify workflows
- [ ] All tests pass with `pnpm test --run`
- [ ] E2E tests pass with `pnpm exec playwright test`
- [ ] Build succeeds with `pnpm run build`
</verification>

<success_criteria>
- Plan phase page integrates swimlanes and Kanban from earlier plans
- Verify phase page integrates report, gaps, checklist, approval
- Rejection auto-routes to gap planning (per CONTEXT.md)
- E2E tests verify complete user flows
- All requirements for Phase 18 are addressed
</success_criteria>

<output>
After completion, create `.planning/phases/18-plan-verify-phase-uis/18-08-SUMMARY.md`
</output>
