---
phase: 18-plan-verify-phase-uis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/stores/verificationStore.ts
  - apps/web/src/stores/verificationStore.test.ts
  - apps/web/src/types/verification.ts
  - apps/web/src/hooks/useVerification.ts
  - apps/web/src/hooks/useVerification.test.ts
autonomous: true
requirements: [VERIF-01, VERIF-02, VERIF-03]

must_haves:
  truths:
    - "User can see test results as they stream in"
    - "User can see gaps with blocking/major/minor severity"
    - "User can see overall pass/fail status"
  artifacts:
    - path: "apps/web/src/types/verification.ts"
      provides: "TestResult, Gap, ManualTest, VerificationState types"
      exports: ["TestResult", "Gap", "ManualTest", "Severity"]
    - path: "apps/web/src/stores/verificationStore.ts"
      provides: "Zustand store for verification state"
      exports: ["useVerificationStore", "selectResults", "selectGaps", "selectStatus"]
    - path: "apps/web/src/hooks/useVerification.ts"
      provides: "Hook subscribing to verification Socket.IO events"
      exports: ["useVerification"]
  key_links:
    - from: "apps/web/src/hooks/useVerification.ts"
      to: "@gsd/events"
      via: "Socket.IO subscription"
      pattern: "socket\\.on.*verification:"
    - from: "apps/web/src/hooks/useVerification.ts"
      to: "apps/web/src/stores/verificationStore.ts"
      via: "store actions"
      pattern: "useVerificationStore\\.getState\\(\\)"
---

<objective>
Create verification state management and test result streaming hook

Purpose: Establish the data layer for verification report visualization. Users need to see test results stream in real-time, gaps highlighted with severity levels, and overall verification status.
Output: Zustand store for verification state, TypeScript types for test results and gaps, and a hook that subscribes to Socket.IO events for verification streaming.
</objective>

<execution_context>
@/Users/mauricevandermerwe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mauricevandermerwe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From @gsd/events package - events we need to add for verification -->
New verification events to emit (not yet in @gsd/events - backend will implement):
```typescript
// Client -> Server
'verification:subscribe': (phaseId: string) => void;
'verification:unsubscribe': (phaseId: string) => void;
'verification:start': (phaseId: string) => void;

// Server -> Client
'verification:test_start': { testName: string; requirementId: string } => void;
'verification:test_result': { requirementId: string; testName: string; passed: boolean; message?: string; duration: number } => void;
'verification:complete': { passed: boolean; summary: string; gaps: Gap[] } => void;
```

From apps/web/src/stores/projectStore.ts pattern:
```typescript
import { create } from 'zustand';

export const useProjectStore = create<ProjectState>((set) => ({
  // state and actions
}));

export const selectProjects = (state: ProjectState) => state.projects;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED - Write failing tests for verification types, store, and hook</name>
  <files>
    apps/web/src/types/verification.ts
    apps/web/src/stores/verificationStore.test.ts
    apps/web/src/hooks/useVerification.test.ts
  </files>
  <action>
Create verification.ts with type definitions (stub implementations):
- Severity type: 'blocking' | 'major' | 'minor'
- TestResult interface: { requirementId, testName, passed, message?, duration }
- Gap interface: { id, requirementId, description, severity }
- ManualTest interface: { id, description, passed: boolean | null, note? }
- VerificationStatus type: 'idle' | 'running' | 'complete'

Create verificationStore.test.ts with failing tests:
- Test setRunning changes status to 'running' and clears results
- Test setRunningTest updates current test name
- Test addTestResult appends result to results array
- Test setComplete sets status='complete', overallPassed, summary
- Test setGaps replaces gaps array
- Test setManualTests sets manual test items
- Test updateManualTest updates a specific test's passed/note
- Test reset returns to initial state

Create useVerification.test.ts with failing tests:
- Test subscribes to verification events on mount
- Test startVerification calls POST /api/phases/:id/verify
- Test updates store when verification:test_result received
- Test updates store when verification:complete received
- Test unsubscribes on unmount

Run tests - they should fail (RED phase).
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run verificationStore.test.ts useVerification.test.ts 2>&1 | grep -E "(FAIL|PASS|Error)" | head -20</automated>
  </verify>
  <done>Tests exist and fail, types file created as stub</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Implement verificationStore and useVerification</name>
  <files>
    apps/web/src/types/verification.ts
    apps/web/src/stores/verificationStore.ts
    apps/web/src/hooks/useVerification.ts
  </files>
  <action>
Implement verification.ts with complete type definitions.

Implement verificationStore.ts:
- Create Zustand store with state: { status, runningTest, results, gaps, manualTests, overallPassed, summary }
- setRunning(): sets status='running', clears results, sets runningTest=null
- setRunningTest(testName): updates runningTest
- addTestResult(result): appends to results array
- setComplete(passed, summary): sets status='complete', overallPassed, summary, runningTest=null
- setGaps(gaps): replaces gaps array
- setManualTests(tests): replaces manualTests array
- updateManualTest(testId, passed, note?): updates specific test
- reset(): returns to initial state
- Export selectors: selectStatus, selectResults, selectGaps, selectManualTests, selectOverallPassed

Implement useVerification.ts:
- Accept socket (TypedSocket | null) and phaseId parameters
- On mount: emit 'verification:subscribe' with phaseId
- Subscribe to 'verification:test_start' -> setRunningTest
- Subscribe to 'verification:test_result' -> addTestResult
- Subscribe to 'verification:complete' -> setComplete + setGaps
- startVerification(): POST to /api/phases/:id/verify, call setRunning
- On unmount: emit 'verification:unsubscribe', remove listeners
- Return { status, results, gaps, manualTests, overallPassed, summary, startVerification } from store

Follow patterns from apps/web/src/hooks/useProjects.ts.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run verificationStore.test.ts useVerification.test.ts 2>&1 | grep -E "(PASS|FAIL)" | head -10</automated>
  </verify>
  <done>All tests pass (GREEN phase), store and hook fully implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add derived selectors and gap filtering</name>
  <files>
    apps/web/src/stores/verificationStore.ts
    apps/web/src/stores/verificationStore.test.ts
  </files>
  <action>
Enhance verificationStore.ts with derived selectors:
- selectPassedCount: count of results where passed=true
- selectFailedCount: count of results where passed=false
- selectBlockingGaps: filter gaps where severity='blocking'
- selectHasBlockingGaps: boolean, true if any blocking gaps exist
- selectManualTestsComplete: boolean, true if all manualTests have passed !== null
- selectAllRequirementsPassed: boolean, all automated tests passed AND all manual tests passed

Add tests for derived selectors:
- Test selectPassedCount returns correct count
- Test selectFailedCount returns correct count
- Test selectBlockingGaps filters correctly
- Test selectHasBlockingGaps returns true when blocking gaps exist
- Test selectManualTestsComplete returns false when any test is null
- Test selectAllRequirementsPassed combines automated and manual results

Ensure TypeScript strict mode compliance.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test --run verificationStore.test.ts && pnpm run build</automated>
  </verify>
  <done>Derived selectors work, all tests pass, build succeeds</done>
</task>

</tasks>

<verification>
- [ ] Types defined in apps/web/src/types/verification.ts
- [ ] Zustand store in apps/web/src/stores/verificationStore.ts with selectors
- [ ] useVerification hook subscribes to Socket.IO events
- [ ] Derived selectors for counts and filtering work correctly
- [ ] All tests pass with `pnpm test --run`
- [ ] Build succeeds with `pnpm run build`
</verification>

<success_criteria>
- verificationStore manages test results, gaps, and manual tests
- useVerification subscribes to Socket.IO events and updates store
- startVerification triggers backend verification execution
- Derived selectors compute pass/fail counts and blocking gap detection
- Tests cover all store actions, hook lifecycle, and derived selectors
</success_criteria>

<output>
After completion, create `.planning/phases/18-plan-verify-phase-uis/18-02-SUMMARY.md`
</output>
