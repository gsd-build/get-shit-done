---
phase: 01-schema-config
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements:
  - SCHEMA-05

must_haves:
  truths:
    - "`gsd-tools template sme` outputs the SME template content to stdout"
    - "The stdout output contains valid YAML frontmatter and all six H2 sections"
    - "The template subcommand error message lists 'sme' as an available option"
    - "`workflow.use_sme_agents` key exists with default false so enabling it does not unconditionally block"
  artifacts:
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "template sme subcommand routing"
      contains: "subcommand === 'sme'"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/templates/sme.md"
      via: "fs.readFileSync + process.stdout.write"
      pattern: "templates/sme.md"
---

<objective>
Wire the `gsd-tools template sme` command to output the SME template to stdout, completing the template delivery mechanism.

Purpose: This is the programmatic interface that the creator agent (Phase 3) and users will use to generate blank SME documents. Per D-07, the command outputs to stdout so callers can redirect to any target path (e.g., `gsd-tools template sme > .planning/smes/payments-SME.md`).

Output: Updated `get-shit-done/bin/gsd-tools.cjs` with `sme` branch in the template case block.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-config/01-CONTEXT.md
@.planning/phases/01-schema-config/01-RESEARCH.md
@.planning/phases/01-schema-config/01-PATTERNS.md

<!-- Depends on Plan 01 output -->
@get-shit-done/templates/sme.md

<interfaces>
<!-- Key code structure the executor needs. Extracted from gsd-tools.cjs. -->

From get-shit-done/bin/gsd-tools.cjs (lines 548-571):
```javascript
case 'template': {
  const subcommand = args[1];
  if (subcommand === 'select') {
    template.cmdTemplateSelect(cwd, args[2], raw);
  } else if (subcommand === 'fill') {
    const templateType = args[2];
    // ... fill handling ...
    template.cmdTemplateFill(cwd, templateType, { ... }, raw);
  } else {
    error('Unknown template subcommand. Available: select, fill');
  }
  break;
}
```

The `error` function is already available in scope (defined at top of file).
`path` and `fs` are already imported at top of file.
`__dirname` resolves to `get-shit-done/bin/` at runtime.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire gsd-tools template sme subcommand</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    get-shit-done/bin/gsd-tools.cjs
    get-shit-done/templates/sme.md
  </read_first>
  <action>
In `get-shit-done/bin/gsd-tools.cjs`, locate the `case 'template':` block (around line 548).

Insert a new `else if` branch for `'sme'` BEFORE the final `else` that throws the unknown-subcommand error. The new branch goes after the `} else if (subcommand === 'fill') { ... }` block:

```javascript
    } else if (subcommand === 'sme') {
      // D-07: output template to stdout; caller redirects to target path
      const tmplPath = path.resolve(__dirname, '..', 'templates', 'sme.md');
      if (!fs.existsSync(tmplPath)) {
        error('SME template not found: ' + tmplPath);
      }
      const content = fs.readFileSync(tmplPath, 'utf-8');
      process.stdout.write(content);
    } else {
```

Also update the error message in the final `else` branch to include `sme`:
```javascript
      error('Unknown template subcommand. Available: select, fill, sme');
```

Key constraints per D-07:
- Use `process.stdout.write(content)` -- NOT `console.log()` (which adds a trailing newline) and NOT `fs.writeFileSync()` (which writes to disk)
- Use `path.resolve(__dirname, '..', 'templates', 'sme.md')` for path resolution -- `__dirname` is `get-shit-done/bin/`, template is at `get-shit-done/templates/sme.md`
- Use the existing `error()` function (already in scope) for the not-found case
- Do NOT call `template.cmdTemplateFill` -- that writes to disk, contradicting D-07
  </action>
  <verify>
    <automated>cd /home/mkline/Repos/get-shit-done && node get-shit-done/bin/gsd-tools.cjs template sme 2>/dev/null | head -1 | grep -q "^---$" && node get-shit-done/bin/gsd-tools.cjs template sme 2>/dev/null | grep -q "## Process Overview" && node get-shit-done/bin/gsd-tools.cjs template sme 2>/dev/null | grep -q "process_name:" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-tools.cjs` contains `subcommand === 'sme'` in the template case block
    - `gsd-tools.cjs` contains `path.resolve(__dirname, '..', 'templates', 'sme.md')`
    - `gsd-tools.cjs` contains `process.stdout.write(content)` (NOT console.log, NOT fs.writeFileSync)
    - `gsd-tools.cjs` error message for unknown template subcommand includes `sme`: `'Available: select, fill, sme'`
    - `node get-shit-done/bin/gsd-tools.cjs template sme` outputs content starting with `---` (YAML frontmatter delimiter)
    - `node get-shit-done/bin/gsd-tools.cjs template sme` output contains `## Process Overview`
    - `node get-shit-done/bin/gsd-tools.cjs template sme` output contains `process_name:`
    - `node get-shit-done/bin/gsd-tools.cjs template sme` output contains `## Known Blockers` (last section)
    - `node get-shit-done/bin/gsd-tools.cjs template sme` exits with code 0
  </acceptance_criteria>
  <done>`gsd-tools template sme` outputs the complete SME template to stdout. The template contains valid frontmatter and all six H2 sections. The error message for unknown subcommands lists `sme` as an option.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end validation of template + config integration</name>
  <files></files>
  <read_first>
    get-shit-done/bin/gsd-tools.cjs
    get-shit-done/bin/lib/config-schema.cjs
    sdk/src/query/config-schema.ts
  </read_first>
  <action>
Run the full validation suite to confirm all Phase 1 artifacts work together:

1. **Template output validation:**
   - Run `node get-shit-done/bin/gsd-tools.cjs template sme` and capture output
   - Verify output starts with `---` (YAML frontmatter)
   - Verify output contains all six H2 sections: `## Process Overview`, `## Identified Risks`, `## Test Gaps`, `## Outdated Logic`, `## Edge Cases`, `## Known Blockers`
   - Verify output contains `finding_counts:` in frontmatter
   - Verify output contains HTML-commented example findings (`<!-- [BLOCKER]`, `<!-- [WARNING]`, `<!-- [WATCH]`)

2. **Frontmatter round-trip validation:**
   - Write template output to a temp file: `node get-shit-done/bin/gsd-tools.cjs template sme > /tmp/test-sme.md`
   - Run `node get-shit-done/bin/gsd-tools.cjs frontmatter get /tmp/test-sme.md`
   - Verify the parsed frontmatter shows `process_name`, `last_analyzed_commit`, `block_mode`, `created_date`, and `finding_counts` as an object with `blocker`, `warning`, `watch` keys
   - Clean up temp file

3. **Config key validation:**
   - Run `node --test tests/config-schema-docs-parity.test.cjs` -- must exit 0
   - Run `node --test tests/config-schema-sdk-parity.test.cjs` -- must exit 0

4. **Full test suite smoke:**
   - Run `npx vitest run --project unit` and verify no failures introduced

If any validation fails, diagnose and fix before marking complete.
  </action>
  <verify>
    <automated>cd /home/mkline/Repos/get-shit-done && node get-shit-done/bin/gsd-tools.cjs template sme > /tmp/gsd-sme-test.md && node get-shit-done/bin/gsd-tools.cjs frontmatter get /tmp/gsd-sme-test.md 2>/dev/null | grep -q "process_name" && node --test tests/config-schema-docs-parity.test.cjs 2>&1 | tail -3 && node --test tests/config-schema-sdk-parity.test.cjs 2>&1 | tail -3 && rm -f /tmp/gsd-sme-test.md && echo "ALL VALIDATIONS PASS" || echo "VALIDATION FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-tools template sme > /tmp/test.md` creates a file with valid YAML frontmatter
    - `gsd-tools frontmatter get /tmp/test.md` parses frontmatter and shows `process_name`, `block_mode`, `finding_counts` fields
    - `node --test tests/config-schema-docs-parity.test.cjs` exits 0
    - `node --test tests/config-schema-sdk-parity.test.cjs` exits 0
    - No test regressions in the unit test suite
  </acceptance_criteria>
  <done>End-to-end validation confirms: template outputs correctly via `gsd-tools template sme`, frontmatter round-trips through `frontmatter.cjs`, and all config parity tests pass. Phase 1 contract is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> template subcommand routing | User-supplied subcommand string routed through if/else chain |
| Filesystem -> stdout | Template file read from disk and written to stdout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Template file path construction in gsd-tools.cjs | mitigate | Path is constructed using `path.resolve(__dirname, '..', 'templates', 'sme.md')` -- hardcoded relative path from known `__dirname`, no user input in path construction. No path traversal possible. |
| T-01-05 | Information Disclosure | Template content leaked to stdout | accept | Template is project-internal boilerplate with no secrets. Stdout output is the intended behavior per D-07. |
</threat_model>

<verification>
- `gsd-tools template sme` outputs complete template to stdout (SCHEMA-05)
- Template frontmatter round-trips through `frontmatter get` (SCHEMA-01, SCHEMA-03, SCHEMA-04)
- Config parity tests pass (CONFIG-01, CONFIG-03)
</verification>

<success_criteria>
- `gsd-tools template sme` is a working command that outputs the SME template
- All Phase 1 artifacts are validated end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-config/01-03-SUMMARY.md`
</output>
