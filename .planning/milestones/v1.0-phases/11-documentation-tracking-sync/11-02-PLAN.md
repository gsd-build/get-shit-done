---
phase: 11-documentation-tracking-sync
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/INVENTORY.md
  - docs/COMMANDS.md
  - docs/INVENTORY-MANIFEST.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "INVENTORY.md lists gsd-sme-auditor agent with correct description"
    - "INVENTORY.md lists /gsd-create-sme command with correct description"
    - "COMMANDS.md has a /gsd-create-sme section with usage examples"
    - "INVENTORY-MANIFEST.json includes gsd-sme-auditor, gsd-create-sme, and create-sme.md"
    - "All CJS doc parity tests pass (0 failures)"
  artifacts:
    - path: "docs/INVENTORY.md"
      provides: "Agent roster with gsd-sme-auditor, command roster with /gsd-create-sme"
      contains: "gsd-sme-auditor"
    - path: "docs/COMMANDS.md"
      provides: "/gsd-create-sme command documentation section"
      contains: "### `/gsd-create-sme`"
    - path: "docs/INVENTORY-MANIFEST.json"
      provides: "Complete manifest matching filesystem"
      contains: "gsd-sme-auditor"
  key_links:
    - from: "docs/INVENTORY.md"
      to: "agents/gsd-sme-auditor.md"
      via: "agent table row reference"
      pattern: "gsd-sme-auditor"
    - from: "docs/INVENTORY.md"
      to: "commands/gsd/create-sme.md"
      via: "command table row link"
      pattern: "gsd-create-sme"
---

<objective>
Fix CJS documentation parity test failures by adding missing agent, command, and workflow entries to INVENTORY.md, COMMANDS.md, and INVENTORY-MANIFEST.json.

Purpose: Ensure all shipped artifacts (agents, commands, workflows from Phases 3-5) are discoverable in documentation and pass automated parity tests.
Output: Updated docs/INVENTORY.md (agent + command rows, headline counts), docs/COMMANDS.md (/gsd-create-sme section), and regenerated docs/INVENTORY-MANIFEST.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentation-tracking-sync/11-RESEARCH.md
@.planning/phases/11-documentation-tracking-sync/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gsd-sme-auditor agent and /gsd-create-sme command to INVENTORY.md</name>
  <files>docs/INVENTORY.md</files>
  <read_first>
    - docs/INVENTORY.md
    - agents/gsd-sme-auditor.md (verify it exists and get accurate description)
    - commands/gsd/create-sme.md (verify it exists and get accurate description)
  </read_first>
  <action>
Make the following 4 changes to `docs/INVENTORY.md`:

1. **Update Agents headline** (line 13): Change `## Agents (35 shipped)` to `## Agents (36 shipped)`

2. **Add agent row** (after line 51, after gsd-sme-creator-analyzer row): Insert:
```markdown
| gsd-sme-auditor | Reviews PLAN.md against SME domain knowledge with adversarial stance; returns SME_APPROVED or SME_CONCERNS with severity-classified findings. | `/gsd-plan-phase` (step 12.6 SME gate) | inventory only |
```

3. **Update coverage note** (line 55): Change:
```
The 2 SME creator agents (`gsd-sme-creator`, `gsd-sme-creator-analyzer`) are inventory-only until Phase 4 integrates them into the create-sme workflow.
```
To:
```
The 3 SME agents (`gsd-sme-creator`, `gsd-sme-creator-analyzer`, `gsd-sme-auditor`) are inventory-only.
```

4. **Update Commands headline** (line 59): Change `## Commands (86 shipped)` to `## Commands (87 shipped)`

5. **Add command row** in the Commands table (in the Codebase Intelligence section near existing `/gsd-map-codebase` and `/gsd-scan` rows, or after the last command row in that section): Insert:
```markdown
| `/gsd-create-sme` | Create or refresh an SME document for a specified process by running the gsd-sme-creator agent. | [commands/gsd/create-sme.md](../commands/gsd/create-sme.md) |
```
  </action>
  <verify>
    <automated>grep "gsd-sme-auditor" docs/INVENTORY.md && grep "gsd-create-sme" docs/INVENTORY.md && grep "Agents (36 shipped)" docs/INVENTORY.md && grep "Commands (87 shipped)" docs/INVENTORY.md && node --test tests/agents-doc-parity.test.cjs tests/inventory-counts.test.cjs 2>&1 | grep -E "^# (pass|fail)" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "gsd-sme-auditor" docs/INVENTORY.md` returns a match (agent row present)
    - `grep "gsd-create-sme" docs/INVENTORY.md` returns a match (command row present)
    - `grep "Agents (36 shipped)" docs/INVENTORY.md` returns a match (headline updated)
    - `grep "Commands (87 shipped)" docs/INVENTORY.md` returns a match (headline updated)
    - `grep "3 SME agents" docs/INVENTORY.md` returns a match (coverage note updated)
    - `node --test tests/agents-doc-parity.test.cjs` exits 0 (all agent parity checks pass)
    - `node --test tests/inventory-counts.test.cjs` exits 0 (headline counts match filesystem)
  </acceptance_criteria>
  <done>INVENTORY.md has gsd-sme-auditor agent row, /gsd-create-sme command row, correct headline counts, and updated coverage note</done>
</task>

<task type="auto">
  <name>Task 2: Add /gsd-create-sme section to COMMANDS.md and regenerate INVENTORY-MANIFEST.json</name>
  <files>docs/COMMANDS.md, docs/INVENTORY-MANIFEST.json</files>
  <read_first>
    - docs/COMMANDS.md (lines 1160-1200, near /gsd-scan section)
    - commands/gsd/create-sme.md (to get accurate command description and flags)
  </read_first>
  <action>
**Part A: Add /gsd-create-sme section to COMMANDS.md**

Insert a new section after the `/gsd-scan` section (around line 1195, after the `---` separator following /gsd-scan). Use this exact content:

```markdown
### `/gsd-create-sme`

Create or refresh an SME (Subject Matter Expert) document for a specified process by running the `gsd-sme-creator` agent.

| Argument | Required | Description |
|----------|----------|-------------|
| `process-name` | No | Process to create an SME for (e.g., `contribution`) |

**Prerequisites:** `workflow.use_sme_agents: true` in `config.json`
**Produces:** `.planning/smes/{process-name}-SME.md`

```bash
/gsd-create-sme contribution        # Create SME for contribution process
/gsd-create-sme                     # Interactive menu of detected processes
```

---
```

**Part B: Regenerate INVENTORY-MANIFEST.json**

Run the project script to regenerate the manifest from the filesystem:
```bash
node scripts/gen-inventory-manifest.cjs --write
```

This will pick up all 3 missing entries: `agents/gsd-sme-auditor`, `commands//gsd-create-sme`, `workflows/create-sme.md`.

Verify the regenerated manifest includes these entries by inspecting the output.
  </action>
  <verify>
    <automated>grep "gsd-create-sme" docs/COMMANDS.md && node scripts/gen-inventory-manifest.cjs --write && node --test tests/commands-doc-parity.test.cjs tests/inventory-manifest-sync.test.cjs 2>&1 | grep -E "^# (pass|fail)" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "### \x60/gsd-create-sme\x60" docs/COMMANDS.md` returns a match (section header present)
    - `grep "gsd-sme-auditor" docs/INVENTORY-MANIFEST.json` returns a match
    - `grep "gsd-create-sme" docs/INVENTORY-MANIFEST.json` returns a match
    - `grep "create-sme.md" docs/INVENTORY-MANIFEST.json` returns a match (workflow entry)
    - `node --test tests/commands-doc-parity.test.cjs` exits 0
    - `node --test tests/inventory-manifest-sync.test.cjs` exits 0
    - Full parity test: `node --test tests/agents-doc-parity.test.cjs tests/commands-doc-parity.test.cjs tests/inventory-counts.test.cjs tests/inventory-manifest-sync.test.cjs` exits 0 with 0 failures
  </acceptance_criteria>
  <done>COMMANDS.md has /gsd-create-sme section, INVENTORY-MANIFEST.json is regenerated with all 3 missing entries, and all 4 CJS parity tests pass with 0 failures</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundaries — all changes are documentation files with no user input processing, no code execution paths, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | docs/INVENTORY-MANIFEST.json | accept | JSON manifest is auto-generated from filesystem by trusted project script; no user input; git-tracked |
| T-11-04 | Information Disclosure | docs/COMMANDS.md | accept | Command documentation is public-facing by design; contains no secrets |
</threat_model>

<verification>
1. `node --test tests/agents-doc-parity.test.cjs` passes (0 failures)
2. `node --test tests/commands-doc-parity.test.cjs` passes (0 failures)
3. `node --test tests/inventory-counts.test.cjs` passes (0 failures)
4. `node --test tests/inventory-manifest-sync.test.cjs` passes (0 failures)
5. `grep "gsd-sme-auditor" docs/INVENTORY.md` returns a match
6. `grep "gsd-create-sme" docs/COMMANDS.md` returns a match
</verification>

<success_criteria>
- All 4 CJS documentation parity tests pass with zero failures
- INVENTORY.md lists 36 agents (including gsd-sme-auditor) and 87 commands (including /gsd-create-sme)
- COMMANDS.md has a complete /gsd-create-sme section with description, arguments table, and usage examples
- INVENTORY-MANIFEST.json includes gsd-sme-auditor, gsd-create-sme, and create-sme.md workflow
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-tracking-sync/11-02-SUMMARY.md`
</output>
