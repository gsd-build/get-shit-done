---
phase: 03-instructions-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/copilot-install.test.cjs
autonomous: true
requirements: [INST-01, INST-02, LIFE-01, LIFE-02, LIFE-03]

must_haves:
  truths:
    - "mergeCopilotInstructions creates file from scratch when none exists"
    - "mergeCopilotInstructions replaces between markers preserving surrounding content"
    - "mergeCopilotInstructions appends to existing file without markers"
    - "stripGsdFromCopilotInstructions returns null for GSD-only content"
    - "stripGsdFromCopilotInstructions preserves user content around markers"
    - "Copilot uninstall removes skills/gsd-*/ directories"
    - "Copilot uninstall cleans instructions from copilot-instructions.md"
    - "writeManifest hashes Copilot skills in manifest"
    - "reportLocalPatches shows /gsd-reapply-patches for Copilot"
  artifacts:
    - path: "tests/copilot-install.test.cjs"
      provides: "Tests for all Phase 3 functions and fixes"
      min_lines: 800
  key_links:
    - from: "tests/copilot-install.test.cjs"
      to: "bin/install.js exports"
      via: "GSD_TEST_MODE require"
      pattern: "require.*install"
---

<objective>
Add comprehensive tests for all Phase 3 changes: instructions merge/strip functions, Copilot uninstall branch, manifest skill hashing, and patch reporting command format.

Purpose: Verify all 5 requirements (INST-01, INST-02, LIFE-01, LIFE-02, LIFE-03) with automated tests providing regression safety.
Output: ~15-20 new tests in tests/copilot-install.test.cjs
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-instructions-lifecycle/03-CONTEXT.md
@.planning/phases/03-instructions-lifecycle/03-RESEARCH.md
@.planning/phases/03-instructions-lifecycle/03-01-SUMMARY.md

<interfaces>
<!-- Functions exported from bin/install.js via GSD_TEST_MODE (after Plan 01) -->
From bin/install.js (new exports added in Plan 01):
```javascript
// Constants
GSD_COPILOT_INSTRUCTIONS_MARKER   // '<!-- GSD Configuration — managed by get-shit-done installer -->'
GSD_COPILOT_INSTRUCTIONS_CLOSE_MARKER  // '<!-- /GSD Configuration -->'

// Functions
mergeCopilotInstructions(filePath, gsdContent)  // 3-case merge: create/replace/append
stripGsdFromCopilotInstructions(content)         // returns cleaned string or null

// Existing exports also available:
writeManifest(configDir, runtime)                // writes gsd-file-manifest.json
reportLocalPatches(configDir, runtime)           // logs reapply command
```

Test file conventions (from tests/copilot-install.test.cjs):
```javascript
const { describe, it, before, after, beforeEach } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('fs');
const path = require('path');
const os = require('os');

// Temp directory pattern for integration tests:
const tmpDir = fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-test-'));
// Cleanup: fs.rmSync(tmpDir, { recursive: true });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for mergeCopilotInstructions and stripGsdFromCopilotInstructions</name>
  <files>tests/copilot-install.test.cjs</files>
  <action>
Add a new `describe('Copilot instructions merge/strip')` section at the end of `tests/copilot-install.test.cjs` (after the existing Phase 2 test sections).

Import the new exports from `bin/install.js` — add `mergeCopilotInstructions`, `stripGsdFromCopilotInstructions`, `GSD_COPILOT_INSTRUCTIONS_MARKER`, `GSD_COPILOT_INSTRUCTIONS_CLOSE_MARKER` to the existing destructured require at the top of the file.

**mergeCopilotInstructions tests** (use temp directories with `fs.mkdtempSync` + cleanup):

1. **"creates file from scratch when none exists"** — call with non-existent filePath, verify file created with content wrapped in opening + closing markers
2. **"replaces GSD section when both markers present"** — create file with user content + GSD markers + more user content, call merge, verify user content preserved and GSD section updated
3. **"appends to existing file when no markers present"** — create file with user-only content, call merge, verify original content preserved + GSD block appended with `\n\n` separator
4. **"handles file that is GSD-only (re-creates cleanly)"** — create file with just the GSD markers and content, call merge with new content, verify only new GSD block remains
5. **"preserves user content before and after markers"** — create file with `# My Setup\n\n<!-- GSD ... -->\nold content\n<!-- /GSD ... -->\n\n# My Notes`, call merge, verify `# My Setup` and `# My Notes` both preserved with new GSD content between

**stripGsdFromCopilotInstructions tests:**

6. **"returns null when content is GSD-only"** — content = just markers + GSD content, verify returns `null`
7. **"returns cleaned content when user content exists before markers"** — content = user text + markers + GSD, verify user text preserved, markers removed
8. **"returns cleaned content when user content exists after markers"** — content = markers + GSD + user text after, verify user text preserved
9. **"returns cleaned content preserving both before and after"** — user before + GSD block + user after, verify both preserved separated by `\n\n`
10. **"returns original content when no markers found"** — content with no GSD markers, verify returned unchanged

Each test should use `beforeEach` to create a fresh temp dir and `afterEach` to clean up (for merge tests that write files). Strip tests are pure functions — no filesystem needed.
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && node --test tests/copilot-install.test.cjs 2>&1 | grep -E "pass|fail|tests" | tail -5</automated>
  </verify>
  <done>10 new tests pass for merge (5 cases) and strip (5 cases). All verify marker-based content management with correct boundary handling.</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for uninstall, manifest, and reportLocalPatches Copilot fixes</name>
  <files>tests/copilot-install.test.cjs</files>
  <action>
Add two more `describe()` sections after the merge/strip tests:

**`describe('Copilot uninstall skill removal')`** — these test the uninstall behavior with Copilot directory layout:

1. **"removes skills/gsd-*/ directories during uninstall"** — create temp dir with `skills/gsd-foo/SKILL.md`, `skills/gsd-bar/SKILL.md`, `skills/custom-skill/SKILL.md` (non-GSD), `get-shit-done/` dir. Test: The uninstall logic for Copilot should remove `gsd-foo/` and `gsd-bar/` but preserve `custom-skill/`. Since `uninstall()` is a high-level function that requires CLI context, test the LOGIC by verifying the isCopilot branch pattern: create the dir structure, read the skills dir, filter `gsd-*` entries, verify correct ones are identified. (Alternative: if uninstall is exported, call it directly with a temp dir.)

2. **"cleans GSD section from copilot-instructions.md on uninstall"** — create temp dir with `copilot-instructions.md` containing user content + GSD markers, call `stripGsdFromCopilotInstructions()` on its content, verify user content preserved and GSD removed.

3. **"deletes copilot-instructions.md when GSD-only on uninstall"** — create temp dir with `copilot-instructions.md` that has ONLY GSD markers + content. Call `stripGsdFromCopilotInstructions()`, verify it returns `null` (signal to delete).

**`describe('Copilot manifest and patches fixes')`:**

4. **"writeManifest hashes skills for Copilot runtime"** — create temp dir with `get-shit-done/bin/verify.cjs` (minimal file), `skills/gsd-test/SKILL.md` (minimal file). Call `writeManifest(tmpDir, 'copilot')`. Read the generated `gsd-file-manifest.json`, verify it has a key matching `skills/gsd-test/SKILL.md` with a hash value. This proves the `(isCodex || isCopilot)` fix works.

5. **"reportLocalPatches shows /gsd-reapply-patches for Copilot"** — create temp dir with `gsd-local-patches/backup-meta.json` containing `{"from_version": "1.0", "files": ["skills/gsd-test/SKILL.md"]}`. Capture console output by temporarily replacing `console.log`. Call `reportLocalPatches(tmpDir, 'copilot')`. Verify output contains `/gsd-reapply-patches` (dash format) and does NOT contain `/gsd:reapply-patches` (colon format).

6. **"reportLocalPatches shows /gsd:reapply-patches for Claude (unchanged)"** — same setup but call with `'claude'`. Verify output contains `/gsd:reapply-patches` to confirm no regression.

Check if `writeManifest` and `reportLocalPatches` are already in the GSD_TEST_MODE exports. If not, they need to be added (note this in the test file as a comment and use the function from the require).

Run full test suite at end: `node scripts/run-tests.cjs`
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && node scripts/run-tests.cjs 2>&1 | tail -5</automated>
  </verify>
  <done>6+ new integration tests pass. writeManifest hashes Copilot skills in manifest JSON. reportLocalPatches shows correct command for Copilot. Uninstall strip logic verified. Full suite passes (540+ tests, 0 failures).</done>
</task>

</tasks>

<verification>
1. `node --test tests/copilot-install.test.cjs` — all tests pass including 16+ new ones
2. `node scripts/run-tests.cjs` — full suite passes (540+ tests)
3. `grep -c 'Copilot instructions\|Copilot uninstall\|Copilot manifest\|Copilot patches' tests/copilot-install.test.cjs` — confirms test sections exist
</verification>

<success_criteria>
- 16+ new tests added to tests/copilot-install.test.cjs
- mergeCopilotInstructions tested for all 3 merge scenarios + edge cases
- stripGsdFromCopilotInstructions tested for null return, content preservation, no-markers passthrough
- Uninstall skill removal logic verified for Copilot directory layout
- writeManifest Copilot skill hashing verified
- reportLocalPatches Copilot command format verified
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-instructions-lifecycle/03-02-SUMMARY.md`
</output>
