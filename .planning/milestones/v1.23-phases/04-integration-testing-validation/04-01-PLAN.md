---
phase: 04-integration-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/copilot-install.test.cjs
autonomous: true
requirements: [QUAL-01, QUAL-02]
# QUAL-02 (conflict detection) is OUT OF SCOPE per user decision — no task implements it.
# It is listed here for traceability only. The feature doesn't exist for any runtime.

must_haves:
  truths:
    - "Full Copilot install in temp dir produces all 31 skill directories with SKILL.md files"
    - "Full Copilot install produces all 11 agent files with .agent.md extension"
    - "copilot-instructions.md exists with GSD Configuration markers"
    - "gsd-file-manifest.json contains correct structure and SHA256 hashes match actual files"
    - "get-shit-done/ engine directory contains bin, references, templates, workflows, CHANGELOG.md, VERSION"
    - "Uninstall removes all GSD artifacts from .github/ without touching non-GSD content"
  artifacts:
    - path: "tests/copilot-install.test.cjs"
      provides: "E2E integration tests for Copilot install and uninstall"
      contains: "E2E: Copilot full install verification"
  key_links:
    - from: "tests/copilot-install.test.cjs"
      to: "bin/install.js"
      via: "execFileSync with GSD_TEST_MODE explicitly removed from env"
      pattern: "execFileSync.*install\\.js.*--copilot"
---

<objective>
Add end-to-end integration tests that run the actual `install --copilot --local` CLI binary in isolated `/tmp` directories, then verify all expected artifacts exist with correct structure, content integrity (SHA256), and clean uninstall behavior.

Purpose: QUAL-01 — confirm the full install pipeline produces correct output end-to-end (not just unit-level function correctness). This is the final validation gate for the v1.23 Copilot CLI milestone.

Output: E2E test blocks appended to `tests/copilot-install.test.cjs` (currently 975 lines, 81 tests). After completion, full test suite passes with new E2E coverage.

Note: QUAL-02 (non-GSD conflict detection/warning) is OUT OF SCOPE per user decision — the feature doesn't exist for any runtime and adding it would be new functionality, not testing.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-testing-validation/04-RESEARCH.md

@tests/copilot-install.test.cjs
@bin/install.js
</context>

<interfaces>
<!-- Key patterns the executor needs from the existing test file and installer -->

From tests/copilot-install.test.cjs (line 10):
```javascript
process.env.GSD_TEST_MODE = '1';
```
CRITICAL: The parent test file sets GSD_TEST_MODE. E2E tests must run via execFileSync which creates a NEW process. The child env must explicitly EXCLUDE GSD_TEST_MODE or the installer exports functions instead of running.

From tests/copilot-install.test.cjs (lines 12-13):
```javascript
const { test, describe, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert');
```

From bin/install.js (lines 20-21, marker constants):
```javascript
const GSD_COPILOT_INSTRUCTIONS_MARKER = '<!-- GSD Configuration — managed by get-shit-done installer -->';
const GSD_COPILOT_INSTRUCTIONS_CLOSE_MARKER = '<!-- /GSD Configuration -->';
```

From bin/install.js writeManifest() output structure:
```json
{
  "version": "string",
  "timestamp": "ISO string",
  "files": { "relative/path": "sha256hex", ... }
}
```

Verified facts from RESEARCH.md live execution:
- 31 skill directories (gsd-*/SKILL.md)
- 11 agent files (gsd-*.agent.md)
- 139 manifest entries (97 engine + 31 skills + 11 agents)
- Engine subdirs: bin, references, templates, workflows, CHANGELOG.md, VERSION
- Uninstall leaves empty skills/ and agents/ dirs but removes all gsd-* content
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: E2E Copilot full install verification tests</name>
  <files>tests/copilot-install.test.cjs</files>
  <action>
Append new E2E test blocks AFTER the existing test code (after line 975). Add two new top-level `describe` blocks. The first is the install verification block described here; the second (uninstall) is Task 2.

**Add required imports at the top of the new section:**
```javascript
const { execFileSync } = require('child_process');
const crypto = require('crypto');
```
Note: `path`, `os`, `fs`, `assert`, `test`, `describe`, `beforeEach`, `afterEach` are already imported at the top of the file.

**Add helper functions before the describe blocks:**

1. `runCopilotInstall(cwd)` — runs `execFileSync(process.execPath, [INSTALL_PATH, '--copilot', '--local'], { cwd, encoding: 'utf-8', stdio: ['pipe', 'pipe', 'pipe'], env })` where `env` is `process.env` with `GSD_TEST_MODE` explicitly deleted. `INSTALL_PATH` = `path.join(__dirname, '..', 'bin', 'install.js')`.

2. `runCopilotUninstall(cwd)` — same pattern but with `['--copilot', '--local', '--uninstall']` args.

**Add constants:**
```javascript
const EXPECTED_SKILLS = 31;   // 31 GSD commands → 31 skill directories
const EXPECTED_AGENTS = 11;   // 11 GSD agents → 11 .agent.md files
```

**Create describe block `'E2E: Copilot full install verification'`:**

Use `let tmpDir` with `beforeEach` creating `fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-e2e-'))` and running `runCopilotInstall(tmpDir)`, plus `afterEach` doing `fs.rmSync(tmpDir, { recursive: true, force: true })`.

Tests to include:

1. **'installs expected number of skill directories'** — Read `.github/skills/`, filter directories starting with `gsd-`, assert count equals EXPECTED_SKILLS (31).

2. **'each skill directory contains SKILL.md'** — For each `gsd-*` dir in skills/, assert `SKILL.md` exists inside it.

3. **'installs expected number of agent files'** — Read `.github/agents/`, filter files matching `gsd-*.agent.md`, assert count equals EXPECTED_AGENTS (11).

4. **'installs all expected agent files'** — Assert these specific 11 files exist: `gsd-codebase-mapper.agent.md`, `gsd-debugger.agent.md`, `gsd-executor.agent.md`, `gsd-integration-checker.agent.md`, `gsd-phase-researcher.agent.md`, `gsd-plan-checker.agent.md`, `gsd-planner.agent.md`, `gsd-project-researcher.agent.md`, `gsd-research-synthesizer.agent.md`, `gsd-roadmapper.agent.md`, `gsd-verifier.agent.md`. Use `deepStrictEqual` on sorted arrays.

5. **'generates copilot-instructions.md with GSD markers'** — Read `.github/copilot-instructions.md`, assert it contains `'<!-- GSD Configuration'` and `'<!-- /GSD Configuration -->'`.

6. **'creates manifest with correct structure'** — Parse `.github/gsd-file-manifest.json`, assert it has `version`, `timestamp`, and `files` properties. Assert `files` is a non-empty object.

7. **'manifest contains expected file categories'** — From manifest `files` keys, filter and count: `skills/` entries = 31, `agents/` entries = 11, `get-shit-done/` entries > 0.

8. **'manifest SHA256 hashes match actual file contents'** — For EVERY entry in `manifest.files`, read the file at `path.join(tmpDir, '.github', relPath)`, compute SHA256 hex digest, assert it equals the manifest hash. This is the core integrity check.

9. **'engine directory contains required subdirectories and files'** — Assert these exist under `.github/get-shit-done/`: directories `bin`, `references`, `templates`, `workflows`; files `CHANGELOG.md`, `VERSION`.

Do NOT test installer console output (per user decision).
Do NOT test content transformations (CONV-06/CONV-07 already covered by unit tests).
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && node --test tests/copilot-install.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <done>All E2E install verification tests pass. The describe block 'E2E: Copilot full install verification' contains 9 tests covering skills count/structure, agents count/names, instructions markers, manifest structure/categories/SHA256 integrity, and engine directory completeness.</done>
</task>

<task type="auto">
  <name>Task 2: E2E Copilot uninstall verification tests</name>
  <files>tests/copilot-install.test.cjs</files>
  <action>
Append a second describe block `'E2E: Copilot uninstall verification'` after the install verification block from Task 1.

Use `let tmpDir` with `beforeEach` creating temp dir, running `runCopilotInstall(tmpDir)` (install first), then running `runCopilotUninstall(tmpDir)`. `afterEach` cleans up with `fs.rmSync`.

Tests to include:

1. **'removes engine directory'** — Assert `.github/get-shit-done` does NOT exist after uninstall.

2. **'removes copilot-instructions.md'** — Assert `.github/copilot-instructions.md` does NOT exist after uninstall.

3. **'removes all GSD skill directories'** — If `.github/skills` still exists, read its contents and assert no directories matching `gsd-*` remain. (The empty `skills/` dir itself may persist — that's expected.)

4. **'removes all GSD agent files'** — If `.github/agents` still exists, read its contents and assert no files matching `gsd-*.agent.md` remain.

5. **'preserves non-GSD content in skills directory'** — Before uninstall (between install and uninstall in a dedicated test), create a non-GSD directory `.github/skills/my-custom-skill/` with a dummy `SKILL.md` inside it. Run uninstall. Assert `my-custom-skill/SKILL.md` still exists. This requires a separate test with its own temp dir lifecycle — create a standalone test (not using the shared beforeEach) that does: mkdtemp → install → create custom content → uninstall → verify custom content preserved → cleanup.

6. **'preserves non-GSD content in agents directory'** — Same pattern: install → create `.github/agents/my-agent.md` (no `gsd-` prefix) → uninstall → assert `my-agent.md` still exists. Use standalone test with own temp dir.

For tests 5-6, manage the temp dir lifecycle within the test function itself rather than relying on the describe-level beforeEach/afterEach, since these need custom setup between install and uninstall.
  </action>
  <verify>
    <automated>cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && node --test tests/copilot-install.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <done>All E2E uninstall verification tests pass. The describe block 'E2E: Copilot uninstall verification' contains 6 tests confirming clean removal of all GSD artifacts and preservation of non-GSD content. Full test suite (all existing + new E2E tests) passes with 0 failures.</done>
</task>

</tasks>

<verification>
Run the complete test suite to confirm no regressions:

```bash
cd /Users/rodolfo/cnx-gh/gsd-copilot/get-shit-done-copilot && node --test tests/copilot-install.test.cjs
```

Expected: All tests pass (81 existing + ~15 new E2E tests = ~96 total), 0 failures.

Additionally verify the full project test suite still passes:
```bash
node scripts/run-tests.cjs
```
</verification>

<success_criteria>
1. `node --test tests/copilot-install.test.cjs` passes with 0 failures
2. E2E install tests verify: 31 skills with SKILL.md, 11 agents with .agent.md, instructions with markers, manifest structure + SHA256 integrity, engine directory completeness
3. E2E uninstall tests verify: all GSD artifacts removed, non-GSD content preserved
4. No changes to bin/install.js or any other file — tests are purely additive to the test file
5. Full project test suite (`node scripts/run-tests.cjs`) passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-testing-validation/04-01-SUMMARY.md`
</output>
