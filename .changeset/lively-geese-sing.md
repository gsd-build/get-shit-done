---
type: Added
pr: 3428
---
**`--claude-plugin` install mode** — new Claude Code plugin format target alongside existing `--claude` flat install. Self-contained plugin directory, no `~/.claude/settings.json` mutation, can be toggled per project via `/plugin disable gsd`.
