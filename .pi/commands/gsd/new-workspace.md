---
name: gsd:new-workspace
description: Create a new GSD workspace
argument-hint: "<workspace-name>"
---

# GSD New Workspace

Create a new GSD workspace.

## Usage

```
/gsd:new-workspace <workspace-name>
```

## Process

This command uses `gsd-tools.cjs` to create workspaces.

## Reference

See `commands/gsd/new-workspace.md` in the GSD source for the full command definition.