---
name: gsd:remove-workspace
description: Remove a GSD workspace
argument-hint: "<workspace-name>"
---

# GSD Remove Workspace

Remove a GSD workspace.

## Usage

```
/gsd:remove-workspace <workspace-name>
```

## Process

This command uses `gsd-tools.cjs` to remove workspaces.

## Reference

See `commands/gsd/remove-workspace.md` in the GSD source for the full command definition.