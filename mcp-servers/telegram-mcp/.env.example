# Telegram MCP Server — Environment Variables
# Copy this file to .env and fill in your values.

# Required — Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=your-bot-token-here

# Required for forum thread Q&A (strongly recommended)
# Create a Telegram supergroup, enable Topics, add bot as admin with Manage Topics permission,
# then get the group ID (a negative number like -1001234567890).
TELEGRAM_GROUP_CHAT_ID=-1001234567890

# Optional — fallback for DM mode when TELEGRAM_GROUP_CHAT_ID is not configured
# Your Telegram user ID (send /start to @userinfobot to find it)
TELEGRAM_OWNER_ID=your-telegram-user-id

# Optional — webhook mode (default: long polling)
# Set to a public HTTPS URL to receive updates via webhook instead of polling.
TELEGRAM_WEBHOOK_URL=https://your-ngrok-url.ngrok.io
PORT=3000

# Optional — logging level (default: info)
# Options: trace, debug, info, warn, error, fatal
LOG_LEVEL=info
